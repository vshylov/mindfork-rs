remaining non-allowlisted Cyrillic: 3160 lines in 106 files

  186  src/app/orchestrator/tests/live.rs
        359: /// End-to-end зонд SelfModel на живой модели (две сессии, один профиль):
        360: /// 1) сессия 1 — сообщаем факты о себе и просим зафиксировать в «модели себя»
        361: ///    (ожидаем вызовы `update_self_model`/`update_user_model`, запись в БД);
        362: /// 2) сессия 2 (новый чат тем же профилем) — спрашиваем «что ты обо мне помнишь»;
        363: ///    «модель себя» подмешана в системный промпт → ожидаем припоминание.
        364: /// Поведение модели нестабильно — тест `#[ignore]`, гоняется вручную; ассертим
        365: /// **механизм** (БД заполнена), а текст припоминания печатаем для оценки.
        377: // --- Сессия 1: сообщаем факты и просим зафиксировать модель себя. ---
        388: // --- Сессия 2: новый чат тем же профилем, проверяем припоминание. ---
        405: // Механизм: после сессии 1 модель себя профиля непуста и сохранена на диск.
        414: // Хотя бы один из мутаторов реально вызван.
        423: /// End-to-end зонд наблюдений: просим модель зафиксировать наблюдение через
        424: /// `add_insight` — ожидаем self-заметку (@self) в БД (нарратив переехал в заметки,
        425: /// Ярус 1 «нарратив как заметки»). `#[ignore]`, вручную.
        451: // Наблюдение — self-заметка (@self), а не запись в блобе модели.
        468: /// End-to-end авто-рефлексии (Tier 3) на живой модели: `auto_reflect_every=1` →
        469: /// после первого же ответа ассистента в фоне запускается рефлексия, которая сама
        470: /// обновляет «модель себя». Рефлексия молчалива (нет UI-события) — ждём появления
        471: /// данных в БД опросом. `#[ignore]`, вручную.
        480: config.self_model.auto_reflect_every = 1; // рефлексия после каждого ответа
        485: // Обычная отправка: сообщаем факты, ассистент отвечает (а затем фоновая
        486: // рефлексия должна сама зафиксировать «модель себя»).
        495: // Ждём, пока фоновая рефлексия что-то запишет (опрос БД до ~60с): блоб модели
        496: // (summary/цели/собеседник) ИЛИ наблюдение self-заметкой (@self, Ярус 1).
        527: /// End-to-end авто-консолидации «модели себя» (этап A1) на живой модели:
        528: /// `auto_consolidate_every=1` → после ответа, когда наблюдений (`@self`) ≥ 2, в фоне
        529: /// запускается «сон» модели себя, который сам сводит дубли наблюдений (`note_merge`/
        530: /// `note_supersede`) и/или сжимает раздутое описание. «Сон» молчалив (нет UI-события) —
        531: /// наблюдаем результат опросом БД. Ассертим **механизм** (наблюдения создаются); факт
        532: /// сведения дублей печатаем для go/no-go (поведение нестабильно). `#[ignore]`, вручную:
        539: config.self_model.auto_consolidate_every = 1; // «сон» после каждого ответа
        544: // Собираем оркестратор с включённым «сном» + (по возможности) реальным эмбеддером.
        565: // Два похожих наблюдения (кандидаты в дубли) — записываем, пока НЕ объединяя.
        581: // После второго ответа наблюдений ≥ 2 → фоновый «сон» модели себя должен запуститься
        582: // и, возможно, свести дубли. Опрос БД до ~90с: считаем self-заметки.
        591: // Дожидаемся ≥2 наблюдений (обе записи легли), затем следим, не сведёт ли их «сон».
        600: // Триггерим ещё один ход (на случай, если «сон» после turn2 не успел из-за каденции):
        601: // каждый ответ инкрементирует счётчик, every=1 → «сон» пробуется снова.
        607: break; // дубли сведены «сном»
        619: // Механизм: наблюдения-заметки созданы (add_insight отработал).
        627: /// End-to-end зонд **ворот** (ядро гипотезы Яруса 1 «нарратив как заметки»): модель
        628: /// записывает наблюдение (`add_insight` → self-заметка @self), затем почти-дубль —
        629: /// ворота `add_insight` показывают похожее существующее наблюдение с подсказкой
        630: /// переписать его через `note_revise`/`note_supersede` вместо копии. Ассертим
        631: /// **механизм** (self-заметки создаются; ворота срабатывают детерминированно —
        632: /// эмбеддер в тестах `MockEmbedder`, наблюдение #1 уже есть); **решение** модели
        633: /// интегрировать печатаем для go/no-go (поведение нестабильно). Запуск (нужен
        634: /// живой сервер + возможно эмбеддер):
        640: // Реальный chat + эмбеддер (MINDFORK_ENGINE_URL / MINDFORK_EMBED_URL) — ворота
        641: // работают на настоящих эмбеддингах (bge-m3 и т.п.), а не на MockEmbedder.
        649: // Сессия 1: записываем наблюдение → self-заметка #1.
        658: // Сессия 2: почти-дубль — ворота add_insight должны показать наблюдение #1.
        673: // Механизм: add_insight в сессии 1 создал self-заметку (@self).
        696: // Ворота: результат add_insight в сессии 2 показал похожее наблюдение?
        700: // С тестовым MockEmbedder (MINDFORK_EMBED_URL не задан) ворота детерминированны:
        701: // если модель вызвала add_insight, они ОБЯЗАНЫ сработать (наблюдение #1 уже есть).
        702: // С реальным эмбеддером срабатывание зависит от его настройки (напр. llama-server
        703: // нужен `--embeddings`), а при недоступности ворота мягко деградируют в пусто —
        704: // поэтому там это лишь диагностика, не жёсткая проверка.
        712: // Интеграция почти-дубля: перепись/замещение/слияние наблюдений.
        720: // Модель должна была как-то тронуть наблюдения (иначе гипотеза не проверяется).
        729: /// Живой смоук Яруса 2 i18n (docs/history/i18n.md, группа 2a): en-зеркало
        730: /// [`self_model_gate_e2e_live`]. Профиль с языком каркаса `En` + все инструменты; ход
        731: /// на английском записывает наблюдение, почти-дубль поднимает ворота `add_insight` —
        732: /// **и текст ворот, и весь результат инструмента должны быть на английском** (ключевой
        733: /// критерий Яруса 2: результаты инструментов локализованы, без кириллицы). Реальный
        734: /// эмбеддер (`MINDFORK_EMBED_URL`) нужен, чтобы ворота сработали. Запуск:
        744: // Свежий профиль (bootstrap-профиль залочен на Ru) → ставим язык каркаса En.
        780: // Ход 1: записываем наблюдение (add_insight → self-заметка).
        789: // Ход 2: почти-дубль — ворота add_insight показывают наблюдение #1 (по-английски).
        807: // Результаты инструментов не содержат кириллицы (каркас переведён, Ярус 2).
        815: // Ворота (реальный эмбеддер): текст — английский шаблон «Similar observations …».
        829: /// End-to-end зонд **ворот размера summary** (этап 2, docs/summary-as-snapshot.md):
        830: /// в БД сеется раздутое описание себя (сверх ориентира по умолчанию 1000 симв.);
        831: /// модель видит подсказку сократить и в пассивной инъекции, и в `get_self_model`.
        832: /// Просим прочитать модель себя и сократить описание, вынеся событийное в наблюдения.
        833: /// Ассертим **механизм** (модель тронула модель себя: `update_self_model` и/или
        834: /// `add_insight`); фактическое сокращение печатаем для go/no-go (поведение нестабильно).
        835: /// `#[ignore]`, вручную:
        847: // Сеем раздутое описание себя (сверх ориентира по умолчанию 1000 симв.).
        857: // Ход: просим прочитать модель себя и сократить описание.
        871: // Ворота: результат get_self_model показал подсказку (детерминированно — summary
        872: // раздут сверх ориентира), если модель его вызвала.
        882: // Итоговый размер описания в БД.
        898: // Модель должна была как-то тронуть модель себя (иначе гипотеза не проверяется).
        907: /// End-to-end зонд **графа над наблюдениями** (Ярус 2, шаг B): модель записывает два
        908: /// соотносящихся наблюдения, затем связывает их (`note_link`). Ассертим механизм
        909: /// (наблюдения-заметки создаются; модель осмотрела модель себя / связала); появление
        910: /// связи в графе печатаем для go/no-go (поведение нестабильно). Инъекция по
        911: /// релевантности проверена детерминированно (`injection_recent_surfaces_relevant_over_fresh`)
        912: /// + ручной мульти-сессионный прогон пользователя. `#[ignore]`, вручную:
        925: // Два соотносящихся (противоречащих) наблюдения.
        940: // Просим осмотреть модель себя и связать противоречащие наблюдения.
        965: // Механизм: наблюдения-заметки созданы.
        972: // Модель должна была осмотреть себя и/или связать (иначе граф не проверен).
        981: /// End-to-end зонд **обзора self-консолидации** (Ярус 3, отложенный из Яруса 2 B):
        982: /// модель записывает два похожих наблюдения, затем зовёт `reflect` — его результат
        983: /// теперь несёт блок «Обзор наблюдений для консолидации» (похожие пары / contradicts /
        984: /// без связей), под который модель сводит дубли (`note_merge`/`note_supersede`/
        985: /// `note_revise`). Ассертим **механизм** (≥1 наблюдение-заметка; при вызове `reflect`
        986: /// и ≥2 наблюдениях его результат содержит обзор — детерминированно, заголовок/счётчики
        987: /// строятся без векторов, не зависят от порога эмбеддера); фактическое сведение дублей
        988: /// печатаем для go/no-go (поведение нестабильно). `#[ignore]`, вручную:
        1001: // Два похожих наблюдения (кандидаты в дубли) — пока НЕ объединяем.
        1018: // Просим отрефлексировать и свести дубли — reflect несёт обзор self-консолидации.
        1044: // Механизм: наблюдения-заметки созданы.
        1050: // Обзор self-консолидации: если модель вызвала reflect и наблюдений ≥2, его
        1051: // результат ОБЯЗАН нести блок «Обзор наблюдений» (детерминированно — заголовок и
        1052: // счётчики строятся без векторов, порог эмбеддера влияет лишь на список похожих пар).
        1053: // Если модель свела дубли к одному ещё в сессии 2, наблюдений < 2 и обзора нет — ок.
        1064: // Сведение дублей — go/no-go (нестабильно): печатаем.
        1069: // Модель должна была отрефлексировать и/или тронуть наблюдения.
        1078: /// End-to-end зонд **ворот родственных черт** `user_model` (Ярус 2, шаг C): модель
        1079: /// добавляет черту собеседника (`update_user_model` с `add_traits`), затем близкую —
        1080: /// ворота `add_traits` показывают родственную черту и просят решить (дубль/противоречие).
        1081: /// Ассертим **механизм** (черта записана в `perceived_traits`; во второй сессии снова
        1082: /// вызван `update_user_model`); срабатывание ворот и решение модели печатаем для
        1083: /// go/no-go. **Порог ворот 0.72** (откалиброван на bge-m3, в отличие от беспороговых
        1084: /// ворот `add_insight`), поэтому на реальном эмбеддере срабатывание зависит от близости
        1085: /// сгенерированных моделью формулировок — здесь это диагностика, не жёсткая проверка.
        1086: /// `#[ignore]`, вручную:
        1098: // Сессия 1: добавляем черту собеседника → user_model.perceived_traits.
        1108: // Сессия 2: очень похожая черта — ворота add_traits должны предупредить о дубле.
        1122: // Механизм: update_user_model в сессии 1 записал черту.
        1142: // Ворота: результат update_user_model в сессии 2 показал родственную черту?
        1143: // (`remove_traits` — аргумент update_user_model, не отдельное имя инструмента,
        1144: // поэтому интеграцию читаем по итоговому состоянию БД, а не по имени вызова.)
        1148: // Интеграция почти-дубля: модель свела перефразы к одной черте (не оставила обе).
        1155: // Модель должна была снова тронуть модель собеседника (иначе ворота не проверены).
        1162: /// End-to-end зонд **кросс-органных связей** (Ярус 3, Путь 1): модель записывает факт
        1163: /// «о собеседнике» (`note_save`) и наблюдение «о себе» (`add_insight`), затем связывает
        1164: /// их (`note_link`) — ребро между органами памяти. Ассертим **механизм** (обе заметки
        1165: /// созданы; модель осмотрела оба органа и/или связала); появление **кросс-органного
        1166: /// ребра** (один конец `@self`, другой — пользовательская заметка) печатаем для
        1167: /// go/no-go. `#[ignore]`, вручную:
        1180: // Факт «о собеседнике» → пользовательская заметка.
        1187: // Наблюдение «о себе» → self-заметка.
        1196: // Просим связать наблюдение «о себе» с фактом «о собеседнике» (кросс-органно).
        1216: // Кросс-органное ребро: один конец @self, другой — пользовательская заметка.
        1243: // Механизм: обе заметки созданы (наблюдение @self + пользовательская).
        1255: // Модель должна была осмотреть органы и/или связать (иначе кросс-связь не проверена).
        1264: /// End-to-end зонд **смешения выдачи** (Ярус 3, Путь 2): при включённом тумблере
        1265: /// `notes.recall_includes_self` наблюдения «о себе» (`@self`) входят в общий
        1266: /// `note_recall` с пометкой `[о себе]`. Ассертим **механизм** (тумблер применён; модель
        1267: /// вызвала `note_recall`); появление self-наблюдения с пометкой и **ответ модели**
        1268: /// (не «загрязняет» ли — go/no-go по безопасности смешения) печатаем. `#[ignore]`,
        1269: /// вручную: `MINDFORK_ENGINE_URL=…/v1 MINDFORK_EMBED_URL=…/v1 cargo test recall_includes_self_e2e_l
        1278: // Включаем смешение выдачи (Путь 2). MockSupervisor игнорирует настройки серверов,
        1279: // так что живой backend/embedder остаются; меняется лишь recall_includes_self.
        1297: // Факт «о собеседнике» + наблюдение «о себе».
        1311: // Поиск: при включённом тумблере note_recall должен вернуть и заметку, и наблюдение
        1312: // «о себе» (с пометкой [о себе]).
        1324: // Механизм: note_recall при включённом тумблере вернул self-наблюдение с пометкой.
        1335: /// End-to-end зонд **связи заметок с RAG-источниками** (Ярус 3, Путь 3): модель
        1336: /// добавляет документ в базу знаний, находит его (`rag_search`), записывает вывод
        1337: /// заметкой (`note_save`) и связывает её с источником (`note_cite_source`). Ассертим
        1338: /// **механизм** (источник в базе; модель искала/связывала); появление связи заметка↔
        1339: /// источник в БД печатаем для go/no-go. `#[ignore]`, вручную:
        1351: // База знаний: добавляем документ под источником «факты».
        1360: // Модель ищет, записывает вывод и связывает его с источником.
        1375: // Механизм: источник «факты» в базе знаний.
        1380: // Связь заметка↔источник (обратный путь): заметки, ссылающиеся на «факты».
        1388: // Модель должна была искать и/или связать (иначе путь не проверен).
        1397: /// **Калибровочный** смоук порога A2 (`SUMMARY_OBS_SIMILARITY` в `overview.rs`): против
        1398: /// РЕАЛЬНОГО эмбеддера (bge-m3 через `MINDFORK_EMBED_URL`) эмбеддит размеченные пары
        1399: /// «абзац описания себя ↔ наблюдение» и **печатает** их косинусы, чтобы родитель выбрал
        1400: /// порог по числам (как `TRAIT_SIMILARITY`). Жёсткого порога не ассертит — лишь что
        1401: /// перефразы в среднем ближе несвязанных пар. `#[ignore]`, вручную:
        1411: // Перефразы одного и того же факта о себе (should-match).
        1426: // Абзац описания себя vs несвязанное наблюдение (should-NOT-match).
        1470: /// End-to-end зонд поведения A2: описание себя с абзацем, дублирующим (иными словами)
        1471: /// хранимое наблюдение (`@self`); против РЕАЛЬНОГО эмбеддера
        1472: /// `summary_observation_overlaps` возвращает `Some` и называет наблюдение. Печатает
        1473: /// измеренный косинус (для калибровки). `#[ignore]`, вручную:
        1496: // Наблюдение (@self) с реальным эмбеддингом в БД.
        1511: // Измеренный косинус (диагностика для калибровки порога).
        1533: /// Живой e2e озвучивания (spec §11.9): команда `/tts` на чате с сообщениями идёт
        1534: /// **через оркестратор** — отбор сообщений → речевой экстрактор → чанки → синтез у
        1535: /// облачного провайдера → очередь воспроизведения. Проверяет всю цепочку и то, что
        1536: /// задача корректно завершается (чип гаснет). Требует ключ **и звуковую карту**;
        1537: /// само звучание — ручная проверка (тест слышит только тайминги).
        1539: /// Провайдер выбирается по заданной переменной: `MINDFORK_OPENAI_KEY` (по умолчанию)
        1540: /// либо `MINDFORK_GEMINI_KEY`. Без них тихо пропускается.
        1559: // Роли озвучиваем — заодно проверяем префиксы на языке профиля.
        1577: // Озвучиваем последний обмен (реплика пользователя + ответ ассистента).
        1583: // Ждём естественного завершения; ошибки синтеза/звука всплывут как Error.
  182  src/screens/chat/tests.rs
        1: //! Тесты экрана чата (через handle_key/render). См. mod.rs.
        11: /// Снимок статусов с готовым чат-сервером (эмбеддинги/имперсонация не настроены).
        22: // Регрессия: пометка (AppEvent::Error о лимите раундов) посреди генерации
        23: // делала `last` заметкой, и последующий стрим финального ответа дописывался в
        24: // неё (простой текст, без markdown). Теперь чанк открывает новый пузырь.
        29: // Ассистент вызвал инструмент (пузырь ассистента пуст по тексту)...
        31: // ...достигнут лимит — в ленту уходит пометка.
        33: // Форс-синтез стримит финальный ответ.
        37: // Заметка отдельным элементом; финальный текст — в пузыре ассистента (markdown),
        38: // а не в заметке.
        76: // Live: текст раунда 1 → вызов инструмента → текст раунда 2 (финал).
        95: // Reload: те же раунды как доменные сообщения (assistant+tool / assistant).
        113: // Один слитый блок ассистента, тот же текст и то же смещение вызова.
        124: // Live: текст 1 → followup → текст 2.
        133: // Два отдельных пузыря ассистента.
        143: // Reload: A1 (с управляющим вызовом) → tool → A2 (new_bubble).
        168: // Live: частичный неверный текст → rewrite → переписанный ответ.
        178: // Ровно один пузырь ассистента с переписанным текстом (частичный отброшен).
        213: s.begin_generation(prev); // как будто шла генерация
        214: s.set_token_usage(prev, 42, Some(123), true, Some(7)); // счётчик токенов прежнего чата
        225: // счётчик токенов прежнего чата сброшен (иначе висел бы в статус-баре)
        229: // системное сообщение не попадает в ленту
        235: // Прокрутка ленты с чистым текстом не требует полной перерисовки (не мигает),
        236: // а с VS16-эмодзи (`🗂️`) — требует (стирает «висячий» артефакт на conhost).
        246: // флаг забран однократно
        256: // без новой прокрутки повторно не запрашиваем
        259: // Петля забирает флаг обобщённым `take_full_redraw` — источник ленты входит в него
        260: // (иначе фикс VS16-артефакта тихо отвалился бы при добавлении второго источника).
        278: // поле очищено
        292: // Shift+Enter — перенос строки, не отправка.
        300: // Alt+Enter — тот же перенос (запасной вариант для терминалов без kitty-протокола).
        309: // Голый Enter по-прежнему отправляет весь многострочный ввод.
        319: // Активация чата с сохранённым черновиком загружает его в поле ввода…
        322: // …но не помечает черновик «грязным» (иначе тут же отправили бы его обратно).
        324: // Переключение на чат без черновика очищает поле ввода.
        334: // Первый забор отдаёт набранный текст…
        336: // …повторный — None, пока ввод снова не изменится.
        345: let _ = s.take_dirty_draft(); // забрали черновик при наборе
        348: // Отправка очистила поле — черновик стал пустым (UI отправит SetDraft("")).
        355: // Пустое поле — просто заполняется.
        358: // Непустое — текст добавляется в начало, существующий ввод сохраняется.
        375: // Во время генерации — подавляется.
        393: // Текст реплики (затравка + сгенерированное) — в поле ввода.
        404: // Esc во время имперсонации — намерение отмены.
        409: // Отмена (Cancelled) отбрасывает сгенерированное — поле сохраняет затравку.
        417: // Таймаут имперсонации приходит как `Length` (не `Cancelled`) — обрезанная
        418: // реплика должна сохраниться в поле, а не исчезнуть.
        433: // Обычная клавиша не печатается в поле (поле скрыто).
        438: // Ctrl+Q / F10 всё ещё выходят (выход переехал с Ctrl+C).
        488: /// Включает подтверждение `Ctrl+R`/`Ctrl+E` через снимок настроек.
        500: // Первое нажатие не отдаёт намерение — открывает попап подтверждения.
        506: // Enter подтверждает и закрывает попап.
        522: // Esc отменяет — попап закрыт, намерения нет.
        534: // Произвольная клавиша не закрывает попап и не печатается в поле ввода.
        557: // По умолчанию (без снимка настроек) подтверждение выключено.
        569: // Без генерации Esc просит открыть экран списка чатов (его создаёт `app`).
        574: // Во время генерации Esc сперва отменяет её.
        593: // Ctrl+C без выделения — no-op (освобождён под копирование, не выход).
        605: .on_key(KeyEvent::new(KeyCode::Home, KeyModifiers::CONTROL)); // курсор в начало
        608: .on_key(KeyEvent::new(KeyCode::Right, KeyModifiers::SHIFT)); // выделить "hello"
        610: // Ctrl+C → намерение записать выделение в буфер; выделение снято, текст цел.
        617: // Выделяем следующие 5 символов (" worl") и вырезаем — текст укорачивается.
        635: // Открывается на вкладке «Горячие клавиши».
        637: // Прочая клавиша не закрывает диалог (в нём вкладки/навигация) и не печатается.
        642: // Esc закрывает.
        650: // Пустой ввод → `?` открывает диалог.
        653: s.handle_key(KeyEvent::new(KeyCode::Esc, KeyModifiers::NONE)); // закрыть
        654: // Непустой ввод → `?` печатается, диалог не открывается.
        665: // ↑↓/PgUp/PgDn прокручивают активную вкладку, не закрывая диалог.
        674: // Прокрутим и переключим вкладку → прокрутка сбрасывается (Tab — следующая,
        675: // порядок About/Hotkeys/Commands/License/Components: следующая за Hotkeys — Commands).
        681: // ← возвращает на предыдущую вкладку.
        684: // Esc закрывает; повторное открытие — на той же вкладке (запоминается), с нулём
        685: // прокрутки. Здесь вернулись на Hotkeys, значит и откроется на Hotkeys.
        693: /// Диалог справки запоминает последнюю выбранную вкладку и открывается на ней.
        697: // Открываем (Hotkeys по умолчанию), переключаемся на «Компоненты», закрываем.
        699: // About ← Hotkeys ← ... : два `←` от Hotkeys → Components (по кругу: Hotkeys→About→Components).
        705: // Повторное открытие — снова на «Компонентах».
        716: s.help.as_mut().unwrap().scroll = 10_000; // «перекручено» — рендер клампит
        719: // Список «Горячих клавиш» не помещается в невысокий диалог → прокрутка клампится
        720: // к максимуму (заведомо меньше запрошенного) и рисуется бегунок скроллбара.
        752: // Без снимка настроек — Ctrl+P ничего не делает.
        772: // При русской раскладке физические клавиши дают кириллицу: Ctrl+з (физ. P),
        773: // Ctrl+й (физ. Q) — шорткаты обязаны срабатывать.
        801: // Чужой чат не трогает шапку активного.
        809: // Без активного чата F5 — no-op.
        824: // Когда экран списка закрыт, поздние результаты операций списка (копия/
        825: // авто-название) `app` кладёт заметкой в ленту через push_note/push_error.
        860: // Рендерим, чтобы поле ввода запомнило свою область (last_area).
        864: // Клик в начало поля — курсор туда, выделения ещё нет.
        872: // Драг вправо на 5 колонок растит выделение "hello".
        891: // Клик в ленту (верх экрана, вне поля ввода) — курсор поля не двигается.
        893: assert_eq!(s.input.cursor(), (0, 5)); // курсор остался в конце текста
        911: // По умолчанию захват выключен → первое нажатие включает (true).
        916: // Второе — выключает (false).
        921: // Работает и при русской раскладке: Ctrl+ц (физ. W).
        952: // Правка ЛЕВЕЕ VS16-эмодзи в поле ввода двигает его на место чужого символа:
        953: // diff шлёт хвостовую половину, бэкенд печатает её без `MoveTo` и ряд едет
        954: // вправо (ratatui#2651, механика — в `shared::ui`). Поэтому поле с таким глифом
        955: // рисуется полной перерисовкой; на обычном тексте — нет.
        966: // И последующие правки тоже — глиф всё ещё в поле.
        970: // Очистка поля снимает требование.
        984: type_str(&mut s, "helo"); // курсор в конце слова с ошибкой
        987: // последний пункт — «добавить в словарь»
        998: // первый пункт — подсказка «hello»; Enter применяет
        1015: // переходим на «добавить в словарь» и применяем
        1021: // слово теперь в персональном словаре — больше не ошибка
        1057: // Выделение в попапе орфографии — как в списке чатов, настройках и «модели
        1058: // себя»: мягкая подложка `keycap_bg` + зелёный рейл `▌`, а не инверсия строки.
        1082: // Инверсии в выделенной строке быть не должно (реверс свапал бы fg↔bg поспаново).
        1099: // курсор между 'a' и 'b'
        1101: // Ctrl+B открывает попап (в т.ч. при русской раскладке: Ctrl+и → физ. B).
        1104: // Enter вставляет первый эмодзи на месте курсора и закрывает попап.
        1114: // Открываем, сдвигаем выделение и вставляем.
        1120: // Повторное открытие восстанавливает прежнее выделение (индекс 2).
        1139: // Классы риска: VS16, ZWJ, тон кожи, supplementary-пиктограммы, BMP-эмодзи ширины 2.
        1152: // Обычный текст, пунктуация, типографика и CJK — нет (иначе полная перерисовка
        1153: // гонялась бы на каждый чанк китайского/японского текста без всякой пользы).
        1164: // Артефакты на legacy-терминалах появлялись при ИЗМЕНЕНИИ содержимого (стрим,
        1165: // добавленная заметка), а «чинила» их только прокрутка — она была единственным
        1166: // триггером полной перерисовки. Теперь триггером стало и изменение ленты.
        1171: // Чистый текст: стрим не требует полной перерисовки.
        1183: // Эмодзи в ленте: стрим требует.
        1194: // Повторная отрисовка без изменений — запрос не возобновляется (иначе петля
        1195: // перерисовывала бы экран целиком вечно).
        1202: // Заметка в ленту (F5 «Переписка скопирована…») — тоже изменение содержимого.
        1213: // Гейт на подход «флаг ставят мутаторы»: забытый вызов `mark_feed_changed` вернул
        1214: // бы мелькающий артефакт на legacy-терминалах. Лента с самого начала содержит
        1215: // эмодзи, поэтому ЛЮБОЕ изменение обязано заказать полную перерисовку.
        1249: // Сворачивание «мыслей» (`Ctrl+T`) перекраивает все блоки — тоже изменение.
        1256: // Тот же класс, что у попапа эмодзи: пункт «➕ добавить в словарь» несёт широкий
        1257: // глиф. При ЗАКРЫТИИ попапа его хвостовую ячейку diff шлёт только у стилизованной
        1258: // строки, поэтому страхуемся полной перерисовкой; при сдвиге выделения она пользы
        1259: // не даёт (сентинел пропускает хвост широкого глифа, ratatui#2651).
        1265: s.take_full_redraw(); // сбросить флаг от набора текста
        1267: // Сдвиг выделения перерисовки НЕ требует: глиф остаётся широким, сентинел обязан
        1268: // пропускать его хвост (ratatui#2651) — подсветку снимает перепечатка глифа.
        1275: // Клавиша, которая попап не меняет, перерисовку не просит.
        1282: // Закрытие отменой (`Esc`).
        1287: // Закрытие применением подсказки (`Enter`).
        1300: // Широкий глиф эмодзи оставляет на conhost «висячую» хвостовую половину, когда
        1301: // ИСЧЕЗАЕТ с экрана: хвосты нестилизованных глифов сетки diff не шлёт (канарейка
        1302: // на границу апстрим-фикса — в `widgets::emoji_picker`). Поэтому полную
        1303: // перерисовку просит закрытие попапа — но не сдвиг выделения, где глиф остаётся
        1304: // широким и сентинел его хвост пропускает (ratatui#2651).
        1308: // Открытие само по себе перерисовки не требует — глиф ниоткуда не уходит.
        1315: // Сдвиг выделения перерисовки НЕ требует: глиф остаётся широким, сентинел обязан
        1316: // пропускать его хвост (ratatui#2651) — подложку снимает перепечатка глифа.
        1323: // Закрытие вставкой (`Enter`).
        1332: // Закрытие отменой (`Esc`).
        1382: // Файл только начат (chunks_total=0) — суффикса чанков в баннере нет.
        1397: // Прогресс по чанкам (chunks_total>0) — баннер несёт счётчик чанков.
        1429: // Обычный текст с ошибкой → проверяется (есть подчёркивания).
        1431: s.last_edit = None; // снять дебаунс, чтобы перепроверка прошла сразу
        1437: // Делаем из строки команду — орфография снимается.
        1472: // Ноль — понятная заметка «ничего не найдено».
        1502: // Пустая база — понятная заметка.
        1509: // С источниками — счётчик чанков и имя источника.
        1549: /// Лockup в шапке справки рисуется, когда высота терминала позволяет показать и
        1550: /// знак, и весь список клавиш; знак прижат влево по полю списка (docs/branding.md §5).
        1561: // Запас: рамка (2) + все клавиши + блок лockup'а (отбивки сверху и снизу).
        1576: // Ствол глифа фирменного цвета есть в каждой его строке, плюс «fork» в слове.
        1582: // Левая рамка попапа: скруглённый угол (палитра по умолчанию — Auto). Панели
        1583: // самого чата рисуются во всю ширину, т.е. их углы — в колонке 0; попап
        1584: // центрирован и вставлен внутрь, поэтому его угол — самый правый из найденных.
        1590: // Знак прижат влево: ствол глифа (колонки 4–5 его чернил) стоит ровно на поле
        1591: // списка клавиш — рамка + два пробела. При центрировании он уехал бы вправо.
        1598: // «fork» вордмарка — справа от глифа, за его правым краем.
        1606: /// На невысоком терминале логотип не рисуется вовсе — список клавиш не сдвигается
        1607: /// и не требует лишней прокрутки (жёсткая деградация, docs/branding.md §5).
        1617: // Диалог высотой 13 (внутри 11 рядов) не вмещает лockup с отбивками → он не
        1618: // рисуется, вкладки не сдвигаются вниз.
        1631: /// Каждая вкладка диалога справки рисует своё характерное содержимое, а таб-стрип —
        1632: /// все четыре вкладки.
        1655: // Таб-стрип на любой вкладке несёт все пять ярлыков.
        1666: // «О программе»: бренд-имя, автор, версия, ссылки.
        1669: assert!(about.contains("https://mindfork.io"), "нет ссылки на сайт");
        1675: // «Горячие клавиши»: подпись из HELP_KEYS, но НЕ команды (они на своей вкладке).
        1686: // «Команды»: команды поля ввода.
        1691: // «Лицензия»: текст MIT.
        1696: // «Компоненты»: имя, версия и лицензия (берём из начала списка — он длинный и
        1697: // прокручивается, дальние крейты за пределами экрана).
  159  src/features/tools/web.rs
        1: //! Инструмент `web_search` (spec §9.3.1): поиск в интернете собственным
        2: //! `reqwest`-клиентом + парсинг HTML-выдачи (`scraper`). Под глобальным
        3: //! выключателем `tools.web_enabled` (приватность, §9.4).
        5: //! **Несколько независимых провайдеров с фоллбэком** (см. [`PROVIDERS`]). По
        6: //! порядку: DuckDuckGo lite (`POST q=`, простейшая разметка, ADR-решение M7) →
        7: //! DuckDuckGo html (иная разметка) → **Mojeek** → **Ecosia** (`GET ?q=`, у каждого
        8: //! своя инфраструктура и разметка). Первый, кто вернул непустую выдачу, выигрывает.
        10: //! **Анти-бот троттлинг.** При нескольких быстрых запросах подряд (что бывает в
        11: //! agentic-loop на сложной/длинной задаче) поисковики режут трафик по IP:
        12: //! DuckDuckGo отдаёт `HTTP 202` со страницей-вызовом («anomaly»), Mojeek/прочие —
        13: //! `403`/`429`, а не результаты. Раньше `202` считался «успехом» (`error_for_status`
        14: //! пропускает 2xx) → парсилась пустая страница → модель видела «Поиск не дал
        15: //! результатов» (хотя запрос корректен), а `403` всплывал как фатальная ошибка
        16: //! «провайдер недоступен». Теперь троттлинг распознаётся ([`is_throttled`]) и при
        17: //! нём сразу пробуется следующий провайдер (троттл липкий per-IP — ретраи его лишь
        18: //! углубляют; разные провайдеры режут независимо, поэтому почти всегда отвечает
        19: //! кто-то один). Если **все** недоступны/троттлят — возвращается явная ошибка (а не
        20: //! «нет результатов»), чтобы модель повторила запрос позже, а не сообщила, что
        21: //! ничего не нашла.
        23: //! **Извлечение контента + реранкинг** (spec §9.3.1, по умолчанию включены,
        24: //! отключаются аргументом `fetch_content`). После получения выдачи страницы
        25: //! результатов загружаются и из них извлекается читаемый текст ([`extract_readable`]
        26: //! на `scraper`: содержимое `<article>`/`<main>`/абзацев, без script/nav-мусора) —
        27: //! «лучшее усилие»: ошибка загрузки одной страницы не валит поиск. Затем результаты
        28: //! **переупорядочиваются эмбеддингами** (через `ctx.embedder`, ADR 0002): запрос и
        29: //! контент каждого результата эмбеддятся, сортировка по убыванию косинусной близости
        30: //! ([`rerank_order`]). Эмбеддер не настроен/недоступен (RAG выключен) → реранкинг
        31: //! пропускается, остаётся порядок провайдера (мягкая деградация, как у RAG).
        45: /// UA, чтобы поисковики отдавали нормальную разметку (а не «лёгкую»/пустую).
        46: /// `pub(crate)` — переиспользуется `fetch_url` (см. `tools/fetch.rs`).
        49: /// `Accept` для загрузки страниц контента (как у браузера).
        52: /// `Accept-Language` для загрузки страниц контента.
        54: /// Результатов по умолчанию.
        56: /// Жёсткий потолок результатов.
        58: /// Таймаут одного HTTP-запроса к поисковику.
        60: /// Потолок извлекаемого читаемого текста одной страницы (символы). Ограничивает
        61: /// раздувание контекста и размер эмбеддинг-запроса.
        63: /// Минимальная длина фрагмента (абзаца) при извлечении: короче — вероятно
        64: /// навигация/меню/кнопки, а не контент.
        66: /// Сколько символов контента результата идёт в эмбеддинг при реранкинге (хватает
        67: /// репрезентативного начала; не раздувает запрос к эмбеддеру).
        70: /// HTTP-метод запроса к поисковику.
        73: /// `q` в теле формы (DuckDuckGo).
        75: /// `q` в query-строке (Mojeek).
        79: /// Описание поискового провайдера: эндпоинт, метод и CSS-селекторы выдачи.
        81: /// Имя для логов/ошибок.
        85: /// Селектор ссылки-результата (откуда берётся `href`).
        87: /// Селектор заголовка (его текст). У DDG/Mojeek совпадает с `link_sel` (заголовок
        88: /// и ссылка — один тег `<a>`); у Ecosia заголовок лежит отдельно от ссылки.
        90: /// Селектор сниппета. Списки ссылок/заголовков/сниппетов выравниваются по
        91: /// индексу (i-й результат = i-я ссылка + i-й заголовок + i-й сниппет).
        95: /// Провайдеры в порядке предпочтения, каждый со своей разметкой и (важно)
        96: /// инфраструктурой. DuckDuckGo (два варианта разметки) — основной; далее
        97: /// независимые **Mojeek** и **Ecosia**. Анти-бот троттлинг у каждого свой и
        98: /// кратковременный (per-IP); он липкий, ретраить один и тот же провайдер
        99: /// бессмысленно — поэтому при троттлинге сразу уходим к следующему. Несколько
        100: /// независимых провайдеров → при недоступности одного почти всегда отвечает другой.
        130: // Стабильные семантические `data-test-id` (а не хешированные css-классы).
        137: /// Один результат поиска.
        143: /// Извлечённый читаемый текст страницы (пусто, если не загружали/не вышло).
        147: /// `web_search` — поиск в интернете (DuckDuckGo → Mojeek → Ecosia, см. [`PROVIDERS`]).
        150: /// Значение по умолчанию для аргумента `fetch_content` (из `config.tools`).
        162: // Таймаут на запрос: иначе зависший ответ DDG держал бы весь ход.
        173: /// Один HTTP-запрос к провайдеру: `Ok(Some(html))` — нормальная страница;
        174: /// `Ok(None)` — анти-бот троттлинг (202/anomaly/403/429); `Err` — сеть/прочий HTTP.
        175: /// `loc` — язык каркаса для текстов ошибок (уходят модели при полном отказе).
        214: /// Загружает страницу результата и извлекает читаемый текст. `None` при любой
        215: /// ошибке/не-HTML — извлечение «лучшее усилие», поиск не должен падать из-за
        216: /// одной недоступной страницы.
        221: // Браузероподобные заголовки: часть сайтов отдаёт пустую/блок-страницу
        222: // на «голый» запрос без Accept/Accept-Language.
        231: tracing::debug!(url, error = %err, "web-поиск: страница не загрузилась");
        236: tracing::debug!(url, status = %resp.status(), "web-поиск: страница вернула не-2xx");
        239: // Берём только HTML (PDF/изображения/прочее извлекать нечем). Заголовок
        240: // может отсутствовать — тогда пробуем как HTML.
        253: tracing::debug!(url, "web-поиск: из страницы не извлечён читаемый текст");
        258: /// Параллельно загружает страницы результатов и проставляет извлечённый текст в
        259: /// `content`. Каждая загрузка независима и отказоустойчива (см. [`Self::fetch_content`]).
        272: /// Переупорядочивает результаты по убыванию близости их контента к запросу
        273: /// (реранкинг эмбеддингами, spec §9.3.1). Эмбеддер недоступен/вернул нестыкующееся
        274: /// число векторов → результаты не трогаем (мягкая деградация). `query` и контент
        275: /// каждого результата эмбеддятся одним запросом.
        282: return; // нечего переупорядочивать
        289: Ok(_) => return, // несоответствие — не рискуем перемешать
        291: tracing::debug!(error = %err, "web-поиск: реранкинг недоступен, порядок провайдера");
        300: /// Текст результата для эмбеддинга при реранкинге: контент (если извлечён) с
        301: /// заголовком/сниппетом в качестве контекста; контент усечён до [`RERANK_EMBED_CHARS`].
        311: /// Порядок индексов `doc_vecs` по убыванию косинусной близости к `query_vec`.
        315: // Стабильная сортировка: при равной близости сохраняется порядок провайдера.
        320: /// Косинусная близость двух векторов (0.0 при несовпадении длин/нулевой норме).
        339: /// Извлекает читаемый текст HTML-страницы (упрощённый readability): берёт абзацы и
        340: /// списки из `<article>`/`<main>` (если есть), иначе — из всего документа; короткие
        341: /// фрагменты и всё внутри навигации/шапки/подвала/сайдбара ([`in_boilerplate`])
        342: /// отбрасываются (иначе на сайтах без семантической разметки в контент попадает
        343: /// мега-меню). script/style не попадают (их текст не внутри `<p>`/`<li>`). Результат
        344: /// усечён до `max_chars` символов.
        346: /// `pub(crate)` — переиспользуется `fetch_url` (см. `tools/fetch.rs`).
        360: // Предпочитаем основное содержимое (article/main) — меньше навигационного шума.
        382: /// `true`, если элемент лежит внутри навигации/шапки/подвала/сайдбара — это
        383: /// boilerplate (меню/ссылки), а не основной контент.
        393: /// Усекает строку до `max` символов (по границе символа, не байта).
        394: /// `pub(crate)` — переиспользуется `fetch_url` (см. `tools/fetch.rs`).
        411: "поиск в интернете"
        449: // Перебираем провайдеров по порядку: первый, кто отдал непустую выдачу,
        450: // выигрывает. При троттлинге сразу уходим к следующему (ретраить липкий
        451: // per-IP троттл бессмысленно). `got_clean_page` — хоть один провайдер
        452: // вернул нормальную (не вызов-)страницу: тогда пустота — настоящее «нет
        453: // результатов», а не троттлинг.
        476: "web-поиск: троттлинг, пробуем следующего провайдера"
        480: tracing::warn!(provider = provider.name, error = %err, "web-поиск: ошибка провайдера");
        488: // Нормальная страница без результатов — это действительно пусто.
        493: // Ни один провайдер не отдал нормальную страницу: троттлинг и/или
        494: // сетевые ошибки. Возвращаем ошибку (а не «нет результатов»), чтобы
        495: // модель повторила запрос позже, а не сообщила, что ничего не нашла.
        503: // Извлечение контента + реранкинг (если не отключено аргументом).
        504: // Загрузка страниц и эмбеддинги — «лучшее усилие»: при сбое остаётся
        505: // обычная выдача (заголовки/сниппеты, порядок провайдера).
        535: /// Признак анти-бот троттлинга/блокировки провайдера: `HTTP 202` (DDG
        536: /// страница-вызов), `403`/`429` (Mojeek/прочие при перегрузе по IP) либо маркер
        537: /// `anomaly` в теле DDG. Нормальная выдача `anomaly` не содержит. Такие ответы
        538: /// кратковременны — это не «нет результатов» и не фатальная ошибка.
        546: /// Парсит выдачу провайдера: списки ссылок (`link_q` → `href`), заголовков
        547: /// (`title_q` → текст) и сниппетов (`snippet_q` → текст) выравниваются по индексу.
        548: /// У DDG/Mojeek `link_q == title_q` (один тег `<a>`); у Ecosia — разные теги.
        549: /// Реальный URL извлекается из редиректа `uddg=...` (DDG) либо берётся как есть.
        595: /// Достаёт настоящий URL из ссылки DDG: декодирует параметр `uddg`, либо
        596: /// нормализует протокол-относительный `//host/...`.
        609: /// Минимальное percent-декодирование значения query-параметра (`%XX`, `+`→пробел).
        640: /// Схлопывает пробелы/переводы строк в один пробел и обрезает края.
        651: // Описание web_search различно на ru/en (ловит забытый `_loc`), en без
        652: // кириллицы. §3.5 docs/history/i18n.md.
        668: <a rel="nofollow" href="//duckduckgo.com/l/?uddg=https%3A%2F%2Fexample.com%2Fa&amp;rut=x" class="res
        672: <a rel="nofollow" href="//duckduckgo.com/l/?uddg=https%3A%2F%2Fexample.org%2Fb" class="result-link">
        678: /// Фикстура html-эндпоинта DDG (разметка `result__a` / `result__snippet`).
        682: <a class="result__a" href="//duckduckgo.com/l/?uddg=https%3A%2F%2Fexample.net%2Fx&amp;rut=y">Пример 
        688: /// Фикстура Mojeek (прямые ссылки `a.title`, сниппет `p.s`).
        691: <li><h2><a class="title" title="https://example.io/m" href="https://example.io/m">Пример M</a></h2>
        696: /// Фикстура Ecosia: заголовок и ссылка — РАЗНЫЕ теги (по `data-test-id`).
        707: /// Селекторы DDG-lite (link == title, как в [`PROVIDERS`]).
        722: // Запасная разметка html-эндпоинта DDG тоже распознаётся.
        738: // Запасной провайдер Mojeek (прямые ссылки, иная разметка).
        748: // У Ecosia заголовок и ссылка — разные теги; парсер выравнивает по индексу.
        765: // Селекторы из PROVIDERS совпадают с тем, что парсят фикстуры.
        791: // HTTP 202 — анти-бот троттлинг DDG (страница-вызов), даже без маркера.
        796: // 403/429 — троттлинг/блокировка по IP (Mojeek и прочие).
        799: // Маркер anomaly в теле — тоже троттлинг.
        804: // Нормальная выдача (200, без маркера) — не троттлинг.
        841: // Скрипты/стили и короткие фрагменты (nav/«Короткий.») отброшены.
        849: // Длинный абзац внутри <nav> (нет <main>) — это меню, не контент: отброшен,
        850: // а абзац вне навигации — взят.
        881: // Несовпадение длин / нулевой вектор → 0.0.
        890: vec![0.0, 1.0], // ортогонален — наименее похож
        891: vec![1.0, 0.0], // совпадает — наиболее похож
        892: vec![0.7, 0.7], // средне
        900: // При равной близости сохраняется исходный порядок (стабильная сортировка).
        918: // Без контента — берётся сниппет.
        945: // Релевантный запросу результат поднялся наверх.
        967: // Эмбеддер недоступен → порядок не изменился.
        972: /// Реальный сетевой смоук (вручную: `cargo test -- --ignored`).
  140  src/screens/self_model.rs
        1: //! Экран «модели себя» (FSD "page"): просмотр и **ручная правка** представления
        2: //! агента о себе для активного профиля — описание, цели, модель собеседника и
        3: //! нарратив инсайтов. Открывается из чата по `F3`, закрывается `Esc`.
        5: //! Данные приходят снимком от оркестратора (он владеет `Storage`) событием
        6: //! `AppEvent::SelfModelView`. Правки экран отдаёт намерением `SelfModelIntent::Edit`
        7: //! (→ `AppCommand::UpdateSelfModel`); оркестратор применяет, сохраняет и переэмитит
        8: //! обновлённый снимок (экран обновляется на месте). Сам экран про `app`/`Storage`
        9: //! не знает (FSD). См. docs/history/self-model-mvp.md.
        27: /// Намерение экрана «модели себя» (транслируется `app`). Параллель к
        31: /// Закрыть вид, вернуться к чату (`Esc`).
        33: /// Выйти из приложения (`Ctrl+Q`/`F10`).
        35: /// Применить ручную правку (оркестратор сохранит и переэмитит снимок).
        39: /// На что указывает выбранная строка (для действий правки).
        49: /// Декоративная строка (пустой разделитель или заголовок секции) — на ней
        50: /// нельзя стоять курсором; навигация её пропускает.
        54: /// Что именно редактируется в открытом текстовом редакторе.
        65: /// Активный текстовый редактор поля (попап). Везде многострочный
        66: /// (`Shift+Enter` — перенос строки, `Enter` — коммит): модели пишут длинный текст.
        72: /// Экран «модели себя»: снимок модели + состояние навигации/правки.
        76: /// Локаль интерфейса (обновляется при `Settings`). См. docs/i18n-ui.md.
        79: /// Первый видимый визуальный ряд (для прокрутки длинного списка). Держится
        80: /// между кадрами; пересчитывается в [`Self::render`] так, чтобы выбранная строка
        81: /// оставалась видимой (а если не влезает целиком — был виден её верх).
        84: /// Подтверждение очистки всей модели (`Ctrl+K` дважды).
        89: /// Открывает экран со снимком модели.
        102: /// Обновляет снимок модели (после правки/рефлексии — событие `SelfModelView`),
        103: /// сохраняя выделение по возможности и закрывая подтверждение очистки.
        109: // Снимок мог сдвинуть/убрать строки — увести курсор с декорации, если попал.
        113: /// Обновляет палитру темы (событие `AppEvent::Settings`).
        118: /// Обновляет локаль интерфейса (событие `AppEvent::Settings`).
        123: /// Строит навигируемые строки (вид + цель действия). Базовые поля присутствуют
        124: /// всегда (правка возможна и на пустой модели); цели и инсайты — по наличию.
        132: // Декоративные строки-разделители: пустая строка и заголовок секции
        133: // (жирным акцентом). На них курсор не встаёт — лишь визуально делят секции.
        146: // Описание себя.
        157: // Цели (с маркером статуса) + строка добавления. Дата — в локальной зоне:
        158: // для активной — создание, для закрытой — момент закрытия (`closed_at`).
        160: // Активная — `●` (WGL4-безопасен, без замены); закрытые — из набора
        161: // глифов палитры (компат: `√`/`×`).
        191: // Модель собеседника — отделена пустой строкой и заголовком от секции «о себе».
        226: // Нарратив (новые сверху) — удаление по `Del`. Тоже за разделителем и
        227: // заголовком, чтобы наблюдения не сливались с моделью собеседника.
        236: // Дата — в локальной зоне: `created_at` хранится в UTC, и без перевода
        237: // наблюдение, добавленное после локальной полуночи, показывало бы
        238: // вчерашний день (в зонах впереди UTC ещё идут «вчерашние» сутки).
        255: /// Действие выбранной строки (или `None`, если индекс вне диапазона).
        260: /// Индексы строк, на которых может стоять курсор (всё, кроме декораций).
        270: /// Сдвигает выделение на `delta` позиций среди выбираемых строк (декорации
        271: /// пропускаются). `isize::MIN`/`MAX` — в начало/конец. Если текущая строка —
        272: /// декорация (после смены модели), стартуем от ближайшей выбираемой.
        288: /// Обрабатывает нажатие клавиши, возвращая намерение для `app`.
        299: // Подтверждение очистки: повторный Ctrl+K — да; любая другая клавиша — отмена.
        307: // Выход переехал на Ctrl+Q/F10 (Ctrl+C освобождён). См. docs/history/input-selection-undo-mouse.md 
        316: KeyCode::F(10) => return Some(SelfModelIntent::Quit), // второй вариант выхода
        344: /// Открывает редактор для выбранной строки (или ничего — для инсайта).
        372: RowAction::Insight(_) => return None, // инсайты не правим, только удаляем
        373: RowAction::Decoration => return None, // разделитель/заголовок — не редактируется
        375: // Поле многострочное (по умолчанию `InputBox` уже такой) — текст переносится.
        389: // Shift+Enter (или Alt+Enter — запасной перенос для терминалов без
        390: // kitty-протокола, см. п.11) — перевод строки; Enter — коммит.
        399: // Ctrl+K (очистка поля, возврат — `Ctrl+Z`) и прочие Ctrl-комбинации поля
        400: // обрабатывает сам `InputBox` в `on_key` (раскладко-независимо).
        408: /// Вставка из буфера обмена — в активный редактор поля (иначе no-op).
        415: /// Строка хоткеев экрана (пары «клавиша — описание — опасная ли»). Раскладывается
        416: /// в сетку общим хелпером [`Palette::hotkey_grid`] — та же логика переноса, что в
        417: /// оверлее списка чатов (слева-направо, столбцы совпадают по вертикали).
        418: /// Пары «клавиша — ключ описания — опасная ли»; описания резолвятся через локаль
        419: /// в [`Self::render`].
        428: /// Рисует экран во весь экран: список полей + строка хоткеев (под панелью, вне
        429: /// рамки); редактор — попапом.
        434: // Строка хоткеев — под панелью (вне рамки), переносится сеткой как в оверлее
        435: // списка чатов. При подтверждении очистки на её месте — предупреждение (1 ряд).
        436: // Высоту считаем заранее, чтобы отвести под неё ровно нужное число рядов.
        437: // Локализуем описания хоткеев (клавиши и «опасность» — как в const).
        486: // Перенос по словам: длинные значения (модели пишут много текста) не влезают
        487: // в одну строку. Каждая логическая строка списка заворачивается на несколько
        488: // визуальных рядов; затем рисуем визуальные ряды вручную (а не виджетом
        489: // `List`). Причина: `List` целиком ПРОПУСКАЕТ многострочный элемент, который не
        490: // вмещается в остаток высоты по высоте (его `get_items_bounds` прерывается на
        491: // `height + item.height() > max_height`), оставляя пустоту — длинный пункт
        492: // внизу выглядит как «конец списка». Ручной рендер обрезает хвостовой пункт по
        493: // высоте области, показывая его верхнюю часть. Ширина содержимого = область
        494: // минус колонка маркера выделения `▌ ` (2 колонки).
        500: // Разворачиваем каждую логическую строку в визуальные ряды, помня для каждого
        501: // ряда индекс его логической строки и где начинается выбранная строка.
        512: wrapped.push(Line::from(String::new())); // пустой разделитель
        525: // Рисуем видимые визуальные ряды. У выбранной строки — подложка на всю ширину
        526: // ряда (база `Paragraph` красит всю область) и маркер `▌`, у прочих — отступ.
        550: // Нижняя область (под панелью, вне рамки): подтверждение очистки или сетка
        551: // хоткеев (рассчитана выше).
        562: // Редактор поверх — с реальным курсором. Везде многострочный (перенос текста).
        575: /// Формирует намерение правки из коммита редактора (или `None`, если правка пуста).
        593: /// Список → вектор непустых обрезанных элементов. Разделители — запятая **и**
        594: /// перевод строки (поле многострочное: элементы можно вводить как через запятую,
        595: /// так и по одному на строку).
        603: /// Пересчитывает прокрутку так, чтобы выбранная строка (визуальные ряды
        604: /// `[sel_start, sel_start + sel_height)`) была видна в окне высотой `view_h`:
        605: /// - если строка выше окна — поднимаем верх окна к её началу;
        606: /// - если её низ за окном — опускаем окно к её низу;
        607: /// - но если строка целиком не вмещается (выше окна) — прижимаем к её **верху**
        608: ///   (видна верхняя часть длинного пункта, а не низ).
        610: /// Иначе прокрутка не меняется.
        629: /// Центрированный прямоугольник в процентах ширины/высоты.
        655: /// Добавляет наблюдение в снимок для тестов. Наблюдения переехали в заметки, а
        656: /// снимок `F3` несёт их реконструированными в поле `narrative` (оркестратор
        657: /// заполняет из self-заметок), поэтому в тестах кладём прямо в поле.
        696: // Первая строка — описание себя.
        699: // Печать и коммит → намерение правки summary.
        711: // Запасной перенос строки для терминалов без kitty-протокола (Shift+Enter там
        712: // неотличим от Enter). См. п.11 аудита InputBox.
        714: s.handle_key(key(KeyCode::Enter)); // открыть редактор описания себя
        729: s.handle_key(key(KeyCode::Down)); // на цель
        746: // Строки пустой модели: [Summary, AddGoal, ·spacer·, ·Собеседник·, Traits,
        747: // Interests, Relationship] — декорации курсор пропускает.
        752: // Пустой коммит → no-op.
        755: s.handle_key(key(KeyCode::Enter)); // снова открыть
        772: // Повторный Ctrl+K подтверждает.
        785: assert_eq!(s.handle_key(key(KeyCode::Down)), None); // отмена
        802: // Многострочное поле: элементы можно вводить по одному на строку.
        814: // Модель с целью и инсайтом: между AddGoal и Traits — spacer+header,
        815: // между Relationship и инсайтом — ещё spacer+header. Курсор по `Down`
        816: // должен перескакивать декорации и не вставать на них.
        824: break; // достигли конца
        831: // Прошли все выбираемые строки сверху донизу.
        847: // Строка хоткеев — под панелью (вне рамки): её ряды начинаются с пробела в
        848: // колонке 0 (левая рамка панели — символ рамки, поэтому её ряды не начинаются
        849: // с пробела). Считаем хвостовые ряды-хоткеи.
        870: // Широко — одна строка хоткеев под панелью; над ней — нижняя рамка панели.
        879: // Узко — хоткеи переносятся сеткой на несколько строк.
        886: // Подтверждение очистки занимает нижнюю область (вне рамки) одной строкой.
        901: // Выбранная строка целиком в окне — прокрутка не меняется.
        903: // Строка выше окна — поднимаем верх окна к её началу.
        905: // Низ строки за окном (строка ниже окна) — опускаем окно к её низу.
        907: // Длинный пункт, не влезающий по высоте: прижимаем к его ВЕРХУ.
        909: // Нулевая высота окна — без изменений.
        915: // Длинный последний инсайт в маленьком окне: ранее `List` пропускал бы такой
        916: // пункт целиком; теперь рисуется его верхняя часть. Проверяем отсутствие
        917: // паники при переносе на много рядов в тесной высоте.
        924: // Перейдём в самый низ (на длинный инсайт) и перерисуем — прокрутка должна
        925: // показать его верх без паники.
  113  src/shared/sandbox.rs
        1: //! Сайдкар-песочница Python на **Wasmer/WASIX**. Отдельный процесс `wasmer`
        2: //! (бандленный рядом с приложением, в `data/sandbox/`), исполняющий код в
        3: //! WASM-изоляции: у гостя нет доступа к хост-ФС (видит только смонтированное), сеть —
        4: //! по явному флагу `--net`. Прерывание — kill процесса (чисто и быстро, проверено
        5: //! в Фазе 0). См. [docs/research/python-wasmer-sandbox.md](../../docs/research/python-wasmer-sandbo
        6: //! (решение §9.7: сайдкар `wasmer` за этим контрактом, а не embed V8 в dll).
        8: //! Слой `shared` (FSD): контракт [`SandboxRunner`] за трейтом (mock в тестах —
        9: //! паттерн `EngineBackend`); реальная реализация [`WasmerSandbox`] строит команду и
        10: //! запускает бинарь. Инструмент `python_exec` (`features/tools/python.rs`)
        11: //! использует этот контракт.
        25: /// Имя бинаря `wasmer` в каталоге песочницы (по платформе).
        31: /// Пакет CPython по умолчанию, если локального `python.webc` нет (скачивается
        32: /// `wasmer` из реестра при первом запуске; Фаза 2 кладёт его в `data/sandbox/`).
        34: /// Гостевая точка монтирования рабочего каталога (со скриптом задачи).
        36: /// Гостевая точка монтирования `site-packages` (предустановленные пакеты).
        38: /// Переменная окружения: путь/имя бинаря `wasmer` (override поиска).
        40: /// Переменная окружения: путь к `python.webc` или ссылка на пакет (override).
        43: /// Шим, подмешиваемый перед пользовательским кодом: глушит неподдержанные в
        44: /// WASIX опции сокета. Без него `http.client`/`urllib`/`requests` падают — WASIX не
        45: /// реализует `setsockopt(TCP_NODELAY)` и бросает `EINVAL`, а http-клиенты его всегда
        46: /// ставят (находка Фазы 0, §9.3). Обёрнуто в функцию, чтобы не сорить именами в
        47: /// глобальном пространстве пользовательского кода.
        61: /// Сырой результат исполнения кода в песочнице (форматирование — на стороне
        62: /// инструмента, чтобы совпадать с локальным режимом).
        67: /// Код возврата процесса (`None` — не завершился нормально/убит).
        69: /// Исполнение прервано по таймауту (процесс убит).
        73: /// Готовность песочницы к запуску (дёшево, без запуска процесса).
        76: /// Бинарь `wasmer` найден — можно запускать.
        78: /// Не установлена/не найдена — человекочитаемая причина (уходит модели).
        82: /// Запуск кода в изолированной песочнице. За трейтом — ради mock в тестах
        83: /// (`features/tools/python.rs`) и заменяемости реализации.
        86: /// Проверка готовности (наличие бинаря `wasmer`). Без запуска процесса. `loc` —
        87: /// язык причины недоступности (её показывает вызывающий: `python_exec` — на языке
        88: /// профиля, ось A; warmup провизии — на языке интерфейса).
        91: /// Исполнить `code` (Python) в песочнице с сетью `net` и таймаутом `timeout`.
        92: /// По таймауту процесс убивается, возвращается `timed_out = true`. Ошибка —
        93: /// только на уровне запуска процесса (не на ненулевом коде возврата гостя). `loc` —
        94: /// язык текста ошибки (её встраивает вызывающий: `python_exec` — язык профиля,
        95: /// warmup — язык интерфейса).
        105: /// Реальная песочница: драйвит бандленный `wasmer` как дочерний процесс.
        107: /// Каталог песочницы (`data/sandbox/`): `wasmer[.exe]`, `python.webc`,
        108: /// `site-packages/`. `None` — только через env-override (тесты/дефолт).
        110: /// Гейт «одна задача за раз» (одно разрешение). Защита от утечки процессов/
        111: /// потоков и предсказуемая нагрузка: параллельный вызов сразу отклоняется.
        112: /// В штатном agentic-loop вызовы и так последовательны — это защита в глубину.
        114: /// Жёсткий лимит памяти процесса (МБ; `None` — без лимита). Применяется только
        115: /// на Windows (Job Object). См. [`WasmerSandbox::with_memory_limit`].
        120: /// Создаёт песочницу с каталогом ассетов (`data/sandbox/`; `None` — без него,
        121: /// тогда бинарь берётся только из env-override).
        130: /// Задаёт жёсткий лимит памяти (МБ; `Some(0)`/`None` — без лимита). Только
        131: /// Windows: процесс `wasmer` помещается в Job Object с
        132: /// `JOB_OBJECT_LIMIT_PROCESS_MEMORY`; превышение убивает процесс (защита хоста
        133: /// от OOM). На Unix поле игнорируется (rlimit ненадёжен с V8 — резервирует
        134: /// большое виртуальное пространство). См. ADR 0005.
        140: /// Путь/имя бинаря `wasmer`: env-override → каталог песочницы ([`locate_wasmer`]).
        151: /// Источник CPython: env-override → `<dir>/python.webc` → пакет реестра.
        165: /// Каталог `site-packages` для монтирования (если существует).
        189: // Гейт «одна задача»: параллельный запуск сразу отклоняется (до спавна).
        199: // Скрипт задачи в уникальном временном каталоге (авто-очистка через Drop).
        206: // Монтируем рабочий каталог и (если есть) site-packages; PYTHONPATH на гостя.
        224: // Python исполняется ВНУТРИ процесса wasmer (V8 in-process, не дочерний
        225: // процесс), поэтому kill самого wasmer останавливает и код. На таймауте/
        226: // отмене future дропается → процесс убивается.
        228: // Кэш скомпилированных модулей — под каталогом песочницы (самодостаточно,
        229: // не в ~/.wasmer): первый запуск компилирует python.wasm (секунды), дальше
        230: // тёплый старт из кэша. См. docs/research/python-wasmer-sandbox.md §2.3.
        239: // Жёсткий лимит памяти (Windows Job Object) — сразу после спавна, до того как
        240: // V8 закоммитит существенную память. «Лучшее усилие»: сбой лишь логируется.
        263: /// Ищет бинарь `wasmer` в каталоге песочницы (чистая, тестируемая). Порядок:
        264: /// прямое размещение `<dir>/wasmer[.exe]` (ручная установка) → распаковка setup'ом
        265: /// `<dir>/wasmer-dist/bin/wasmer[.exe]`. Используется и рантаймом ([`WasmerSandbox`]),
        266: /// и провизией (`features::sandbox_setup`) — единый источник истины о раскладке.
        276: /// Непустое значение env-переменной как `OsString` (override пути/имени).
        281: /// Применяет жёсткий лимит памяти к процессу `wasmer` (Windows Job Object). При
        282: /// превышении процесс убивается — защита хоста от OOM. «Лучшее усилие»: сбой winapi
        283: /// лишь логируется. Проверено вживую (§9.6 исследования): лимит держится и после
        284: /// закрытия хэндла job'а (job живёт, пока процесс — его член), поэтому HANDLE не
        285: /// удерживается через `await` (важно для `Send`-фьючи).
        296: tracing::warn!("песочница: нет хэндла процесса — лимит памяти не применён");
        299: // SAFETY: `raw` — валидный хэндл только что запущенного процесса; job создаётся и
        300: // закрывается в пределах этого блока, поля структуры инициализированы нулями.
        304: tracing::warn!("песочница: CreateJobObjectW не удался");
        317: tracing::warn!("песочница: SetInformationJobObject не удался");
        322: tracing::warn!("песочница: AssignProcessToJobObject не удался");
        324: // Хэндл можно закрыть сразу: лимит остаётся, пока процесс — член job'а.
        329: /// На не-Windows жёсткий лимит памяти не применяется: `rlimit`/`RLIMIT_AS`
        330: /// ненадёжен с бэкендом V8 (он резервирует большое виртуальное адресное
        331: /// пространство, из-за чего низкий лимит ломает сам старт). Полагаемся на таймаут
        332: /// и wasm32 (~4 ГБ). См. ADR 0005.
        335: tracing::debug!("песочница: лимит памяти поддержан только на Windows — пропуск");
        338: /// Оборачивает пользовательский код шимом `setsockopt` (чистая, тестируемая).
        343: /// Собирает аргументы командной строки `wasmer` (чистая, тестируемая). Форма:
        345: /// Порядок и хост:гость-монтирование проверены живьём в Фазе 0 (в т.ч. с
        346: /// Windows-путём, где двоеточие драйва `C:` не ломает разбор `--volume`).
        375: /// Временный каталог для скрипта задачи (авто-очистка при `Drop`). Живёт в системном
        376: /// tmp; уникален по UUID — без зависимости `tempfile` в рантайме.
        395: /// Мок песочницы для тестов инструмента `python_exec`.
        400: /// Записи вызовов `run`: (код, признак сети).
        406: /// Готовая песочница, возвращающая заданный вывод.
        415: /// Недоступная песочница с причиной.
        454: /// Референсная локаль для тестов (ru байт-в-байт — прежние ассерты подстрок целы).
        464: // Пользовательский код идёт после шима.
        487: // python-источник, затем разделитель, затем скрипт — в самом конце.
        524: // Распаковка setup'ом: <dir>/wasmer-dist/bin/wasmer[.exe].
        529: // Прямое размещение имеет приоритет.
        537: // Каталог без бинаря → Missing (при отсутствии env-override в окружении CI).
        539: return; // окружение задаёт override — тест неинформативен
        564: // Держим единственное разрешение — эмулируем «уже идёт задача».
        566: // Второй запуск отклоняется мгновенно (до resolve_wasmer/спавна процесса).
        576: // Регрессия против забытого `loc`: причина «занята» на языке локали.
        591: // После завершения run (тут — ошибкой «нет бинаря») разрешение возвращается,
        592: // и следующий вызов снова доходит до логики (а не упирается в гейт).
        596: return; // окружение задаёт бинарь — этот тест про отсутствие бинаря
   74  src/app/runtime/dispatch.rs
        1: //! Runtime — применение AppEvent к экрану + трансляция Intent → AppCommand. Часть модуля [`super`];
        2: //! runtime.rs (см. docs/history/refactoring-god-objects.md, этап 7).
        6: /// Применяет событие оркестратора к экрану чата (read-only-проекция). Снимок
        7: /// настроек при открытом экране настроек дополнительно обновляет его рабочую
        8: /// копию (отражает создание/удаление профилей).
        17: // Статус серверов — в чат (строка статуса) и, если открыт, в экран настроек
        18: // (чипы в секции «Модель/сервер»: подключение → готов видно на месте).
        25: // Снимок списка применяем к чату всегда (для следующего открытия/`Ctrl+N`),
        26: // а при открытом экране списка — ещё и к нему (живое обновление).
        34: // Ошибка операции списка: в его область статуса, если экран открыт; иначе
        35: // (поздний ответ авто-названия при закрытом списке) — заметкой в ленту.
        40: // Запись в буфер обмена — side-effect UI-слоя; сама запись и маршрутизация
        41: // подтверждения/ошибки вынесены в `deliver_clipboard` (`apply_event` не знает
        42: // про `arboard`).
        58: // Тема/режим совместимости/язык UI могли смениться — обновим палитру
        59: // и локаль открытых overlay-экранов (список/модель себя) одним broadcast.
        76: // Пришла активация только что созданного чата (`Ctrl+N` в
        77: // списке) — закрываем список и показываем новый чат. Так
        78: // переход прежний→новый атомарен, без промежуточного мигания.
        81: // Удаление активного чата при открытом списке меняет активный —
        82: // обновим его метку в списке.
        130: // Ответ на запрос/правку модели себя (`F3`): открываем экран либо обновляем
        131: // уже открытый на месте (сохраняя выделение — важно при правках).
        142: // «Модель себя» изменилась фоном/инструментами — обновляем ТОЛЬКО открытый
        143: // экран `F3` (перезапрос свежего снимка); при закрытом — игнор.
        159: /// Пишет переписку в буфер обмена и направляет подтверждение/ошибку в статус
        160: /// экрана списка чатов (его открывали для копирования) либо, если он закрыт, —
        161: /// заметкой в ленту. Инкапсулирует единственное обращение к `arboard`
        162: /// ([`write_clipboard`]), чтобы `apply_event` не знал про буфер обмена.
        172: Ok(()) => list.set_notice("Переписка скопирована в буфер обмена".into()),
        173: Err(err) => list.set_error(format!("Не удалось скопировать в буфер обмена: {err}")),
        176: Ok(()) => screen.push_note("Переписка скопирована в буфер обмена"),
        177: Err(err) => screen.push_error(&format!("Не удалось скопировать в буфер обмена: {err}")),
        182: /// Намерение любого из экранов — единый тип, чтобы снятое из активного экрана
        183: /// намерение можно было диспетчеризовать одним владением (без 4 параллельных
        184: /// `Option` и 4 почти одинаковых `if`-блоков, конфликтовавших по заимствованиям).
        192: /// Диспетчеризует намерение активного экрана в соответствующий транслятор.
        193: /// Возвращает `true`, если запрошен выход.
        208: /// Транслирует намерение чата в команду оркестратору (или открывает экран
        209: /// настроек/списка чатов). Возвращает `true` для [`ChatIntent::Quit`].
        239: // Начальный снимок статусов серверов (чипы в секции «Модель/сервер»);
        240: // дальше их обновляет `apply_event` из события `ServerStatus`.
        242: // Снимок MCP-хоста (каталог инструментов + статусы серверов);
        243: // дальше его обновляет `apply_event` из события `Settings`.
        245: // Какие ключи сохранены на этой машине (поле-статус «API-ключ»).
        251: // Список чатов открывается из снимка, который чат держит актуальным.
        261: // Просмотр модели себя: данными владеет оркестратор — запрашиваем снимок,
        262: // экран откроется по событию `SelfModelView` (см. `apply_event`).
        267: // Тумблер прокрутки колесом: включаем/выключаем захват мыши терминала.
        268: // Это чисто терминальный side-effect (FSD: экран про терминал не знает,
        269: // только сообщает желаемое состояние). При включённом захвате выделение
        270: // текста доступно с зажатым Shift.
        279: // Копирование в буфер обмена перехватывается раньше — в `process_input_batch`
        280: // (там есть слот `arboard`, а здесь `screen` иммутабелен). Сюда не доходит.
        287: /// Транслирует намерение экрана списка чатов в команду (или управление экранами).
        288: /// Возвращает `true` для [`ChatListIntent::Quit`] (петля завершается).
        296: // Закрытие/переход к чату возвращает базовый экран.
        306: // Создание чата запускает поток нового чата на экране чата (там живёт
        307: // выбор профиля — оверлей при >1 профиле).
        310: // Один профиль: чат создаёт оркестратор (round-trip). Список
        311: // ОСТАВЛЯЕМ открытым до прихода `ChatActivated` нового чата —
        312: // иначе на время round-trip мигнул бы прежний активный чат. По
        313: // приходу активации `apply_event` переключит на новый чат.
        320: // >1 профиля: `request_new_chat` открыл оверлей выбора профиля в
        321: // экране чата — показываем чат, чтобы оверлей был виден.
        330: // Копирование/удаление/переименование/авто-название не закрывают список:
        331: // подтверждение/ошибка прилетят в его область статуса, обновлённый набор —
        332: // событием `ChatList`.
        342: /// Транслирует намерение экрана настроек в команду (или закрывает его).
        343: /// Возвращает `true` для [`SettingsIntent::Quit`] (петля завершается).
        372: /// Транслирует намерение экрана просмотра модели себя: закрытие возвращает к чату,
        373: /// `Quit` завершает петлю (`true`). Команд оркестратору не шлёт (read-only).
        385: // Правка: команда оркестратору; экран остаётся открытым и обновится по
        386: // ответному `SelfModelView` (см. `apply_event`).
   71  artwork/build-wordmarks.py
        2: """Генератор вордмарков mindfork: текст → кривые (self-contained SVG).
        4: Зачем: SVG с `<text>` зависит от шрифта на машине зрителя. GitHub, чужой браузер
        5: и просмотрщики в Linux нужного моноширинного шрифта не имеют — начертание
        6: подменяется, и вордмарк выглядит сломанным. Поэтому глифы переводятся в `<path>`:
        7: файл становится самодостаточным и рендерится одинаково везде.
        9: Шрифт: **JetBrains Mono ExtraBold** (SIL Open Font License 1.1) — определён по
        10: эталону `wordmark-example.png` подгонкой метрик (см. `artwork/README.md §Шрифт`).
        11: OFL разрешает использование шрифта для создания артворка и распространение
        12: полученных кривых; сам файл шрифта в репозиторий не кладём — он нужен только для
        13: перегенерации.
        15: Геометрия лockup'а (пропорции иконки к тексту) измерена с эталона — константы
        16: `ASC_RATIO`/`GAP_RATIO`/`BASE_RATIO`/`TRACKING` ниже.
        18: Запуск:
        19: python artwork/build-wordmarks.py [--font <путь к JetBrainsMono-ExtraBold.ttf>]
        21: Без `--font` шрифт ищется в стандартных местах (системные шрифты, бандлы IDE
        22: JetBrains). Перезаписывает `artwork/mindfork-wordmark*.svg`.
        36: # --- Палитра бренда (см. artwork/README.md) ---------------------------------
        37: ORANGE = "#c25a27"  # акцент: «fork» и ствол глифа
        38: GRAY = "#5c6370"  # ветви глифа
        39: PLATE = "#09090b"  # подложка иконки
        40: LIGHT_TEXT = "#e4e4e7"  # «mind» на тёмном
        41: DARK_TEXT = "#18181b"  # «mind» на светлом
        42: MUTED = "#71717a"  # тэглайн
        44: # --- Глиф иконки: 5 прямоугольников на сетке 16×16 ---------------------------
        45: # Единственный источник истины — mindfork-icon-transparent.svg; здесь дубль для
        46: # генерации. Тест widgets/logo.rs сверяет таблицу в коде с тем же SVG.
        55: # --- Геометрия лockup'а, измеренная с wordmark-example.png --------------------
        56: # Иконка в эталоне: 388×388 px; базовая линия текста, высота выносного элемента и
        57: # зазор пересчитаны в доли от размера иконки, поэтому масштабируются на любой S.
        58: ASC_RATIO = 0.5227  # высота выносного элемента ('d','f','k') / размер иконки
        59: GAP_RATIO = 0.3608  # зазор «правый край иконки → левый край чернил текста» / S
        60: BASE_RATIO = 0.7418  # базовая линия ниже верха иконки / S
        61: TRACKING = -35  # межбуквенный трекинг, единицы шрифта (≈ -0.035em)
        63: ASCENDER = 730  # высота 'd'/'f'/'k' в единицах шрифта
        65: NAT_ADV = 600  # натуральный шаг моноширинного шрифта
        66: ADV = NAT_ADV + TRACKING  # фактический шаг с трекингом
        67: LSB_M = 38  # левый боковой отступ 'm' — начало чернил
        81: sys.exit(f"шрифт не найден: {explicit}")
        88: "JetBrainsMono-ExtraBold.ttf не найден.\n"
        89: "Скачать: https://github.com/JetBrains/JetBrainsMono/releases (OFL 1.1)\n"
        90: "или указать путь: --font <путь>"
        95: """Кривые подстроки одним `d`, в единицах шрифта, с учётом трекинга.
        97: `start_index` — позиция подстроки в целом слове (чтобы «fork» встал на свои
        98: места, а не начинался с нуля).
        110: """Ширина чернил слова в единицах шрифта (для раскладки и viewBox)."""
        111: # 'm'.xMin=38 … 'k'.xMax=580 (замерено по шрифту; см. artwork/README.md)
        116: """Иконка: опциональная скруглённая подложка + глиф на сетке 16×16."""
        143: """Текст «mindfork» кривыми.
        145: `split=True` — два пути: «mind» цветом `text_fill` и «fork» акцентом; иначе
        146: одно слово в `currentColor` (одноцветный вариант). Кривые остаются в единицах
        147: шрифта, а масштаб и переворот оси Y задаёт трансформация группы — так `d`
        148: читаем и правится вручную.
        151: # Перо в нуле даёт чернила от LSB_M — сдвигаем так, чтобы они легли на ink_x.
        174: """Горизонтальный лockup: иконка слева, «mindfork» справа."""
        188: # Вертикальный лockup: в отличие от горизонтального, эталона нет — пропорции
        189: # выведены. Ширина слова = 2× размера иконки (иначе моноширинное слово из 8 букв
        190: # уезжает в 3.2× и лockup становится нижне-тяжёлым); вертикальный зазор взят в той
        191: # же доле от высоты выносного элемента, что и горизонтальный (0.3608/0.5227).
        197: """Вертикальный лockup: иконка сверху, «mindfork» под ней по центру."""
        208: # запас снизу под выносной вылет 'o'/'d' (-10 единиц), иначе их срежет viewBox
        213: """Горизонтальный лockup + тэглайн под словом, выключенный по его ширине."""
        221: # Тэглайн: подбираем кегль и трекинг так, чтобы строка выключилась ровно по
        222: # ширине вордмарка (классический приём — тэглайн «под словом», край в край).
        228: # ширина = (n-1)*(600+track) + xMax_last - xMin_first, решаем относительно track
        259: # Консоль Windows по умолчанию cp1252/cp866 — русский вывод её роняет.
        266: ap = argparse.ArgumentParser(description="Сборка вордмарков mindfork (текст → кривые)")
        267: ap.add_argument("--font", help="путь к JetBrainsMono-ExtraBold.ttf")
        268: ap.add_argument("--tag-font", help="путь к JetBrainsMono-Bold.ttf (тэглайн)")
        276: print(f"шрифт:   {path}")
        277: print(f"тэглайн: {tag_path}")
        293: print(f"  ✓ {name}  ({len(content)} байт)")
   71  src/screens/chat/feed.rs
        1: //! Экран чата — проекция AppEvent в ленту (сообщения, генерация, tool-блоки, токены). Часть модуля 
        2: //! монолита chat.rs (см. docs/history/refactoring-god-objects.md, этап 2).
        7: /// Помечает, что содержимое ленты изменилось (стрим, новое сообщение, заметка,
        8: /// правка): если в ленте есть глифы группы риска, следующий кадр рисуем **полной
        9: /// перерисовкой** — иначе на legacy-терминалах остаются артефакты от изменившихся
        10: /// строк с эмодзи. См. [`is_risky_glyph`], [`crate::shared::ui::prime_full_redraw`].
        12: /// Вызывается **мутаторами**, а не по факту рендера: артефакт нельзя показывать
        13: /// даже на кадр. Определение «есть риск» по факту отрисовки опаздывало бы на кадр
        14: /// (промах кэша виден только там), и артефакт успевал мелькнуть — спрятать это за
        15: /// синхронизированный вывод нельзя, conhost режим 2026 игнорирует.
        17: /// Флаг риска кэшируется и **только накапливается**: правки всегда затрагивают
        18: /// последний блок, поэтому проверяем его, а полную пересборку ленты
        19: /// ([`Self::activate_chat`]) пересчитывает с нуля. Пере-оценка в большую сторону
        20: /// безопасна — лишняя перерисовка не видна, пропущенная оставляет артефакт.
        32: /// Обновляет заголовок чата в проекции (после ручного/авто-переименования).
        33: /// Меняет заголовок в шапке ленты, если это активный чат. Список и оверлей
        34: /// дополнительно обновляются событием `ChatList` (`set_chat_list`).
        45: // Смена чата сбрасывает состояние генерации: «осиротевшие» чанки прежней
        46: // генерации не должны попадать в ленту нового чата.
        51: // Счётчик токенов относится к прежнему чату — гасим его, чтобы он не висел
        52: // в строке статуса после переключения (статус-бар скрывает счётчик при
        58: // Склейка раундов agentic-loop в один блок «Ассистент:» с инлайн tool-блоками.
        60: // Лента заменена целиком — пересчитываем «есть риск» с нуля (дальше флаг
        61: // только накапливается по последнему блоку).
        65: // Загружаем сохранённый черновик чата в поле ввода (пустой у нового чата).
        66: // НЕ помечаем `draft_dirty` — иначе тут же отправили бы его обратно тем же
        67: // `SetDraft`; перепроверку орфографии запускаем напрямую.
        73: /// Возвращает текст в поле ввода после удаления последнего обмена. Если поле
        74: /// непустое — текст добавляется в его начало (существующий ввод не теряется).
        75: /// См. spec §11.7.
        119: /// Добавляет tool-блок к текущему сообщению ассистента (live во время хода).
        130: // Вызов сделан после уже накопленного текста ответа — фиксируем позицию,
        131: // чтобы tool-блок встал на месте вызова, а не в «шапке».
        139: // Текст/мысли следующего раунда отделяем разделителем (как при перезагрузке).
        147: /// Ассистент написал сообщение и продолжает вторым (инструмент
        148: /// `send_followup_message`): завершаем текущий пузырь и добавляем новый
        149: /// стримящийся пузырь ассистента — в него пойдёт текст следующего раунда.
        150: /// Так live-лента совпадает с перезагрузкой (`from_messages` не склеивает
        151: /// сообщение с `new_bubble`). См. spec §9.3.
        172: /// Ассистент решил переписать текущее сообщение (инструмент
        173: /// `rewrite_current_message`): отбрасываем уже накопленный текст/мысли/вызовы
        174: /// текущего пузыря — переписанный ответ пойдёт в него же. См. spec §9.3.
        191: /// Гарантирует, что `last` — стримящийся пузырь ассистента (цель для чанков).
        192: /// Если посреди генерации в ленту вклинилась заметка (напр. `AppEvent::Error`
        193: /// о достижении лимита раундов перед финальным синтезом), `last` оказывается
        194: /// заметкой — тогда открываем новый пузырь ассистента, иначе стрим уходил бы в
        195: /// заметку и рендерился простым текстом без markdown.
        218: // Первый текст раунда после вызова инструмента — с пустой строкой-
        219: // разделителем (совпадение с `FeedMessage::from_messages`).
        248: /// Обновляет счётчик токенов текущей генерации (live). Игнорирует устаревшие
        249: /// события (по `generation_id`). Контекст (переписку) обновляет только когда он
        250: /// задан (`Some`), запоминая, точное это число или оценка.
        265: // Reasoning-токены известны только из `usage` (Some) — иначе не трогаем.
        291: /// Добавляет нейтральную заметку в ленту (напр. подтверждение операции списка
        292: /// чатов, когда экран списка уже закрыт — поздний ответ авто-названия/копии).
        300: /// Символ, чья отрисовка на legacy-терминалах (conhost/Command Prompt) расходится с
        301: /// моделью `ratatui` настолько, что после изменения содержимого остаются «висячие»
        302: /// артефакты — половинки широких глифов, куски подложки, съехавшие ряды.
        304: /// Классы риска (все — про эмодзи, а не про CJK: иероглифы шириной 2 терминалы
        305: /// рисуют согласованно, и гонять из-за них полную перерисовку незачем):
        306: /// - **VS16** (U+FE0F) — `🗂️`: `ratatui` шлёт хвостовую ячейку такого кластера
        307: ///   отдельно, см. [`crate::shared::ui::prime_full_redraw`];
        308: /// - **ZWJ** (U+200D) — `👨‍👩‍👧`: составной кластер, ширина модели и терминала расходятся
        309: ///   сильнее всего (осознанная граница, см. доку `shared/wrap.rs`);
        310: /// - **модификаторы тона кожи** (U+1F3FB..=U+1F3FF) — `👍🏽`;
        311: /// - **пиктограммы supplementary-плоскости** (≥ U+1F000) — `😀`, `🔥`;
        312: /// - **эмодзи-символы BMP шириной 2** (`✅`, `⭐`, `✨`) — обычные стрелки/типографика
        313: ///   из тех же блоков шириной 1 сюда не попадают.
        322: /// Есть ли в элементе ленты глиф из группы риска (в тексте, «мыслях» или
        323: /// аргументах/результате вызова инструмента) — тогда изменение содержимого и
        324: /// прокрутка требуют полной перерисовки. См. [`is_risky_glyph`].
   68  src/features/tools/python.rs
        1: //! Инструмент `python_exec` (spec §9.3, §13.2): исполнение Python в одном из двух
        2: //! режимов ([`PythonMode`]):
        4: //! - **Wasmer** (по умолчанию) — изолированная песочница WASIX за сайдкаром `wasmer`
        5: //!   (`shared::sandbox`): нет доступа к хост-ФС, сеть по флагу, предустановленные
        6: //!   пакеты. См. docs/research/python-wasmer-sandbox.md.
        7: //! - **Local** — прежнее поведение: системный интерпретатор отдельным процессом с
        8: //!   таймаутом. Без OS-песочницы (код исполняется на машине пользователя).
        10: //! Мастер-выключатель `tools.python_enabled` гейтит инструмент целиком (по умолчанию
        11: //! выключен). Id инструмента (`python_exec`) от режима не зависит.
        25: /// Таймаут исполнения в локальном режиме.
        27: /// Максимальный размер захваченного вывода (символов) — защита от лавины.
        30: /// `python_exec` — исполняет переданный код Python и возвращает stdout/stderr.
        32: /// Режим исполнения (песочница/локально).
        34: /// Путь к интерпретатору (Local; `None` → системный `python3`/`python`).
        36: /// Реализация песочницы (Wasmer).
        38: /// Разрешить сеть в песочнице (Wasmer).
        40: /// Таймаут исполнения в песочнице (Wasmer).
        61: /// Имя/путь интерпретатора с разумным дефолтом по платформе (Local).
        72: /// Локальный режим: системный интерпретатор отдельным процессом. Возвращает уже
        73: /// отформатированный текст результата (успех/ошибка/таймаут) на языке `loc`.
        75: // Аргумент передаётся напрямую (без шелла) — нет проблем с экранированием.
        82: // Вывод идёт в pipe, а не в консоль, поэтому Python на Windows выбирает
        83: // кодировку по локали (часто cp1252) и падает на кириллице в `print`
        84: // (`UnicodeEncodeError`). Мы читаем вывод как UTF-8, поэтому и Python
        85: // просим писать UTF-8. См. CLAUDE.md (M7).
        100: // На таймауте future дропается → процесс убивается (kill_on_drop).
        117: /// Режим песочницы Wasmer. Возвращает уже отформатированный текст результата на
        118: /// языке `loc`.
        157: "исполнить Python"
        198: /// Форматирует результат исполнения (stdout/stderr/код возврата) — единый вид для
        199: /// обоих режимов, чтобы презентер ленты (`present::parse_console`) распознавал
        200: /// консоль по меткам. Метки `stdout:`/`stderr:` — универсальные (не переводятся);
        201: /// метку кода возврата (`python.console.exit`) и служебные строки локализуем по `loc`,
        202: /// а `parse_console` распознаёт метку кода по всем локалям.
        237: /// Обрезает строку до `max` символов с пометкой об усечении (на языке `loc`).
        255: /// Нет ли кириллицы в строке (утечка русского на en-профиле).
        261: /// Референсная локаль (ru) для прямых вызовов форматирования вывода.
        266: /// Инструмент в локальном режиме с заданным путём интерпретатора.
        277: /// Инструмент в режиме песочницы с заданным mock-раннером.
        344: // Раннер вызван ровно раз, с флагом сети.
        399: /// Реальное исполнение в песочнице (вручную): требует установленного `wasmer`
        400: /// (env `MINDFORK_SANDBOX_WASMER` или бинарь в `data/sandbox/`) и сети для
        401: /// первого скачивания `python/python`. `cargo test -- --ignored`.
        421: /// Инструмент над **провизионированной** песочницей (`mindfork sandbox setup`):
        422: /// каталог задаётся env `MINDFORK_SANDBOX_DIR` (в нём wasmer-dist/python.webc/
        423: /// site-packages). `None` — env не задан, смоук пропускается.
        436: /// numpy (нативные `.so` через динлинковку WASIX) в провизионированной песочнице.
        454: /// pandas (нативное wasix-колесо + чистые зависимости) в провизионированной песочнице.
        473: /// requests по HTTPS при включённой сети.
        490: /// Без сети запрос должен провалиться (сокетов в песочнице нет) — не 200.
        511: /// Кириллица в `print` не должна падать (гость WASIX — UTF-8).
        526: /// Бесконечный цикл прерывается по таймауту (kill процесса wasmer).
        545: /// Инструмент над провизионированной песочницей с лимитом памяти (Windows).
        562: /// Лимит памяти (Windows Job Object) не даёт рантайм-скрипту выесть память хоста:
        563: /// большой allocation под низким лимитом не проходит (процесс убит).
        572: // Выделение 3 ГБ под лимитом 1 ГБ обязано провалиться — либо graceful
        573: // MemoryError, либо фатальный крах V8, либо ненулевой код возврата.
        584: // И 3 ГиБ точно не выделены (число байт в stdout не появилось).
        588: /// Разумный лимит (2 ГБ) не мешает лёгкой работе.
        604: /// На en-профиле недоступность песочницы объясняется **по-английски** (ось A):
        605: /// обёртка `python_exec` + вложенная причина из `sandbox.rs` — обе английские, без
        606: /// утечки русского. Не-ignored (реального `wasmer` не требует — путь Missing).
        611: return; // окружение задаёт бинарь — путь Missing не воспроизведётся
        632: /// На en-профиле вывод и **метка кода возврата** — английские (ось A). Реальная
        633: /// песочница (провизионированная): `print` + ненулевой `sys.exit`.
        652: /// На en-профиле сообщение таймаута — английское (ось A). Реальная песочница.
        669: /// Реальное локальное исполнение (вручную, если установлен Python).
        681: /// Кириллица в `print` не должна падать с `UnicodeEncodeError` (Windows cp1252).
   67  src/app/runtime/input.rs
        1: //! Runtime — батчинг ввода и вставка из буфера (Windows-путь): коалесинг клавиш, Chunk, сверка с бу
        2: //! runtime.rs (см. docs/history/refactoring-god-objects.md, этап 7).
        6: /// Порог «всплеска» событий за один дренаж, при котором подозреваем вставку и
        7: /// добираем её хвост (см. `run_loop`). Человек не набирает столько за один
        8: /// zero-timeout-дренаж — несколько событий разом означают вставку.
        11: /// Пауза-детектор конца вставки. На Windows крупная вставка приходит несколькими
        12: /// порциями через консольный буфер, и петля дренирует их за разные итерации — на
        13: /// стыке порций серия символов рвалась бы, а одиночный `Enter` на стыке уезжал бы
        14: /// как отправка. Пока события идут с зазором < этого порога — считаем их одной
        15: /// вставкой; реальный ввод человека имеет паузы много больше (>100мс реакции).
        18: /// Сверяет реконструированную из key-событий вставку с буфером обмена и, если это та
        19: /// же вставка, возвращает её полную версию из буфера (с восстановленными эмодзи).
        21: /// **Зачем (Windows):** при вставке из буфера обмена консоль Windows доставляет текст
        22: /// как обычные key-события, а crossterm 0.29 **теряет** символы supplementary-плоскости
        23: /// (эмодзи вроде 😊, U+1F60A): они кодируются UTF-16 суррогатной парой, а записи
        24: /// key-down/key-up консоли ломают сборку пары в crossterm — символ пропадает ещё до
        25: /// нашего слоя. BMP-символы (буквы, ❤ U+2764, селектор U+FE0F) проходят. Поэтому
        26: /// реконструкция = вставка без supplementary-эмодзи.
        28: /// Чтобы вернуть эмодзи, читаем буфер обмена и сверяем: если выбросить из него ровно те
        29: /// символы, что теряет консоль (кодпойнты > U+FFFF), и нормализовать переводы строк/
        30: /// табы, совпадает ли он с реконструкцией? Совпал → это та же вставка, отдаём полный
        31: /// текст буфера. Не совпал (буфер устарел/не та вставка/недоступен) → реконструкцию
        32: /// (без эмодзи, но без риска вставить чужое). На не-Windows — тождественно (там приходит
        33: /// корректная bracketed-вставка `Event::Paste`).
        45: /// На не-Windows вставка приходит корректным UTF-8 (`Event::Paste`) — сверка не нужна.
        54: /// Совпадает ли буфер обмена с реконструкцией вставки по «BMP-проекции» (чистая
        55: /// функция, тестируема без буфера/терминала). Из буфера выбрасываются символы, которые
        56: /// консоль Windows теряет (кодпойнты > U+FFFF — суррогатные пары), затем обе стороны
        57: /// нормализуются по переводам строк/табам (как [`InputBox::insert_str`]). См.
        73: /// Кладёт событие в пачку, отбрасывая key-события «отпускания»/повтора: приложение
        74: /// их и так игнорирует (`handle_key` берёт только `Press`), а при коалесинге вставки
        75: /// они разрывали бы серию символов между нажатиями (на Windows вставка идёт как
        76: /// пары down/up). Мышь/ресайз/`Paste` пропускаются как есть.
        86: /// Символ для коалесинга вставки: печатная клавиша / Enter / Tab без Ctrl/Alt.
        87: /// `Enter → '\r'` (CRLF из буфера затем схлопывается в `InputBox::insert_str`),
        88: /// `Tab → '\t'`. Возвращает `None` для всего остального (стрелки, Ctrl-шорткаты,
        89: /// функц. клавиши) — оно разрывает серию вставки.
        106: /// Кусок пачки ввода: либо собранная вставка (серия текстовых клавиш ≥2), либо
        107: /// одиночное событие (обычная клавиша/мышь/unix-`Paste`).
        113: /// Разбивает пачку событий на куски, коалесируя подряд идущие текстовые клавиши
        114: /// (см. [`paste_char`]) в одну вставку, если их ≥2. Чистая функция — тестируема без
        115: /// экрана/терминала. Серия длиной 1 (обычный ввод символа / одиночный Enter)
        116: /// остаётся одиночным событием, чтобы Enter работал как отправка.
        139: // Серия из одной клавиши — отдаём обычным событием (ниже).
        146: /// Обрабатывает пачку терминальных событий за один проход. Коалесированные вставки
        147: /// идут в активный редактор (экран настроек) или в поле ввода чата текстом — без
        148: /// отправки, даже если содержат переводы строк. Одиночные события — обычным путём.
        149: /// Возвращает `true`, если запрошен выход.
        160: // Вставка из буфера: на экране настроек — в активный редактор поля; в
        161: // чате — в поле ввода (никогда не отправляет); в списке цели вставки нет.
        162: // `Chunk::Paste` — реконструкция из key-событий (Windows); сверяем её с
        163: // буфером обмена, чтобы восстановить потерянные crossterm эмодзи
        164: // supplementary-плоскости (см. [`reconcile_paste`]). `Event::Paste` —
        165: // настоящая bracketed-вставка (unix), уже корректный UTF-8.
        167: // `Chunk::Paste` — реконструкция из key-событий (Windows); сверяем её с
        168: // буфером, чтобы восстановить эмодзи (см. [`reconcile_paste`]).
        169: // Маршрутизацию по экранам держит `ActiveScreen::handle_paste`.
        174: // Снимаем намерение из активного экрана (борроу заканчивается на
        175: // owned-значении `AnyIntent`), затем диспетчеризуем одним владением —
        176: // иначе конфликт заимствований `active`/`screen`.
        186: // Копирование выделения в буфер обмена (`Ctrl+C`/`Ctrl+X`) —
        187: // side-effect UI-слоя: текст уже у нас, в оркестратор не идём.
        188: // Слот `arboard` есть тут (в `dispatch` его нет). Успех молчалив,
        189: // сбой (headless-Linux без X11) — заметкой в ленту.
        193: .push_error(&format!("Не удалось скопировать в буфер обмена: {e}"));
        204: // Колесо мыши прокручивает ленту чата. На списке/настройках (своя
        205: // навигация) прокрутку игнорируем.
   67  src/features/tools/present.rs
        1: //! Презентация вызовов инструментов для ленты (spec §11.3): как показать
        2: //! аргументы и результат конкретного инструмента — подсвеченный код, консольный
        3: //! вывод, компактный заголовок вместо сырого JSON.
        5: //! **Чистый слой** (без ratatui): отдаёт структуру [`ToolPresentation`], которую
        6: //! рисует [`crate::widgets::message_feed`] (навешивая цвета/гуттеры/подсветку).
        7: //! Знание про конкретные инструменты (какое поле — код, на каком языке, как
        8: //! разобрать консольный вывод) живёт здесь, в слое инструментов; виджет остаётся
        9: //! generic. `arguments` приходит уже сериализованным JSON-текстом (как в
        10: //! [`crate::widgets::message_feed::FeedToolCall`]) — презентер парсит его сам, при
        11: //! сбое разбора мягко деградирует к прежнему inline-виду.
        13: //! Имена инструментов сверяются строковыми литералами — это стабильный
        14: //! wire-протокол (их видит и модель), меняются крайне редко.
        18: /// Порог «крупного» строкового аргумента: многострочный ИЛИ длиннее этого числа
        19: /// символов — показывается отдельным блоком под заголовком, а не в `name(...)`.
        21: /// Потолок длины суффикса заголовка (символов) — длинное значение усекается «…».
        24: /// Инструменты, чей результат — структурированная проза (URL, пассажи, заметки):
        25: /// рендерим его как markdown, а не плоским текстом. Простые подтверждения
        26: /// («Заметка сохранена») сюда не входят — они остаются приглушённым `Plain`.
        29: /// Блок содержимого tool-карточки (аргумент или результат).
        32: /// Подсвеченный код на языке `lang` (пустой `lang` → без подсветки).
        34: /// Консольный вывод процесса (stdout/stderr/код возврата).
        36: /// Плоский текст (обёрнутый, приглушённый) — результат по умолчанию.
        38: /// Markdown-рендер (для текстовых результатов-прозы).
        42: /// Разобранный консольный вывод `python_exec`.
        50: /// Как показать вызов инструмента в ленте.
        53: /// Суффикс заголовка: `name(suffix)`. `None` → показываем просто `name`.
        55: /// Блоки под заголовком (крупные аргументы — код/текст).
        57: /// Блоки результата.
        61: /// Строит презентацию вызова `name` с сериализованными `arguments` и `result`.
        73: /// Заголовок + блоки аргументов.
        75: // Аргументы — не JSON-объект (сбой разбора, массив, скаляр): показываем сырой
        76: // текст inline, как раньше, без блоков.
        89: // Спец. «код-поле» инструмента: python — `code` (python), fs_write — `content`
        90: // (язык по расширению `path`).
        109: // Нет спец. поля → крупное строковое поле показываем отдельным Plain-блоком
        110: // (напр. `content` у note_save, `text` у rag_add).
        123: // Оставшиеся короткие скалярные поля → компактный заголовок.
        138: /// Блоки результата.
        148: // Результат `fs_read` — содержимое файла: подсвечиваем по расширению пути
        149: // (кроме сообщений об ошибке).
        150: "fs_read" if !result.starts_with("Не удалось") => {
        162: /// Метка кода возврата (`python.console.exit`) во всех известных локалях (вшитые +
        163: /// внешние). Формат вывода `python::format_output_parts` локализован (ось A), поэтому
        164: /// парсер распознаёт метку на любом языке профиля, включая добавленные внешне. Метки
        165: /// `stdout:`/`stderr:` универсальны (не переводятся).
        173: /// Разбирает вывод `python_exec` (см. `python::format_output_parts`) в секции stdout/
        174: /// stderr/код возврата. `None` — если текст не похож на этот формат (сообщения об
        175: /// ошибке запуска, «(пустой вывод, успех)») → показываем его плоским текстом.
        200: // Строка вне известной секции → это не наш формат.
        208: // Секции склеены через join("\n\n") — снимаем хвостовые пустые строки-разделители.
        214: /// Склеивает строки секции, отбрасывая хвостовые пустые (разделитель `\n\n`).
        223: /// Компактный заголовок из коротких пар: 0 — нет; 1 — только значение (путь/запрос/
        224: /// id — самодостаточны); ≥2 — `k=v, …` (иначе значения неоднозначны).
        239: /// Скалярное значение JSON в строку (объекты/массивы/null → `None`).
        249: /// «Крупная» ли строка (многострочная или длиннее порога).
        254: /// Расширение файла из JSON-значения `path` (нижним регистром; `""` — если нет).
        262: /// Однострочит и усекает суффикс заголовка до [`HEADER_MAX_CHARS`] символов.
        304: // Формат `python::format_output`: секции через "\n\n".
        314: // «(пустой вывод, успех)» и сообщения об ошибке — не наш формат → Plain.
        325: // Формат вывода локализован (ось A) — parse_console распознаёт метку кода
        326: // возврата на любом языке (здесь en «exit code:»).
        336: // Пустые строки ВНУТРИ stdout не должны обрывать секцию (парсер по меткам).
        394: // web_search — «проза» → markdown-результат.
        405: // serde_json::Map (BTreeMap) → ключи отсортированы.
        418: // Крупное `content` уехало в блок; одиночное оставшееся поле — значением.
        441: // Невалидный JSON → сырой аргумент inline; длинный усекается «…».
        451: // Длинное однострочное текстовое поле уходит блоком целиком (не усекается).
   64  src/shared/mcp.rs
        148: bail!("MCP {method}: вызов отменён")
        155: Ok(Err(_)) => bail!("MCP {method}: соединение закрыто сервером"),
        158: bail!("MCP {method}: таймаут {}с", timeout.as_secs())
        261: text.push_str(&format!("[{other}-содержимое опущено]"));
        307: tracing::warn!(line = %clip_line(&line), "MCP: не-JSON строка в stdout, пропущена");
        322: .unwrap_or("ошибка без описания")
        411: "команда MCP-сервера не может быть .bat/.cmd (BatBadBut, \
        412: CVE-2024-24576); используйте `cmd /c …` или прямой exe-путь"
        430: .with_context(|| format!("запуск MCP-сервера: {program}"))?;
        521: drop(self); // Drop: kill.cancel() + дроп Arc соединения (stdin закрыт)
        526: /// Монитор-задача процесса сервера: ждёт его завершения (взводит `exited`) или
        527: /// сигнала `kill` (grace-период на самостоятельный выход после закрытия stdin →
        528: /// `start_kill`). Владеет [`Child`] и Job-хэндлом ([`JobGuard`]) — `kill_on_drop`
        529: /// и kill-on-close добивают процесс/дерево даже при сбросе задачи рантаймом.
        537: // Job-хэндл живёт до конца задачи: его закрытие (уже после завершения
        538: // ребёнка) добьёт kill-on-close'ом всё дерево процессов (Windows; на
        539: // прочих ОС — пустышка). Привязка вместо `drop(job)` в конце — на unix
        540: // у пустышки нет Drop, и явный drop ловил бы clippy::drop_non_drop.
        545: Ok(s) => tracing::warn!(status = ?s, "MCP-сервер завершился сам"),
        546: Err(e) => tracing::warn!(error = %e, "ошибка ожидания MCP-сервера"),
        550: // stdin закрывается дропом соединения (может отставать от kill на
        551: // мгновение) — grace-период на штатный выход, затем kill.
        562: /// Опора Job Object'а kill-on-close (Windows): пока хэндл открыт — дерево живёт;
        563: /// закрытие хэндла (штатное в мониторе или крах приложения) убивает всё дерево
        564: /// процессов сервера (включая внуков `cmd /c npx` → `node`). На прочих ОС — no-op.
        565: /// Поле держится только ради `Drop` (закрытие хэндла) — не читается.
        574: // SAFETY: HANDLE Job Object'а — просто опора ядра; передача между потоками безопасна.
        581: // SAFETY: хэндл создан нами в `assign` и ещё не закрывался.
        587: /// Помещает процесс в Job Object c `KILL_ON_JOB_CLOSE`. «Лучшее усилие»:
        588: /// сбой лишь логируется (сервер работает без job'а — как на unix).
        597: tracing::warn!("MCP: нет хэндла процесса — Job Object не назначен");
        600: // SAFETY: `raw` — валидный хэндл только что запущенного процесса; структура
        601: // инициализирована нулями; job закрывается через JobHandle::drop.
        605: tracing::warn!("MCP: CreateJobObjectW не удался");
        617: tracing::warn!("MCP: не удалось назначить Job Object kill-on-close");
        636: /// Скриптованный фейк-сервер поверх duplex: на каждый запрос — замыкание.
        637: /// Возвращает соединение клиента + JoinHandle фейка (для ассертов внутри).
        668: /// Стандартный скрипт: initialize/tools/list/tools/call.
        678: // Две страницы: пагинация по nextCursor.
        722: // Встречная версия сервера (иная, чем наша) принимается.
        725: // Пагинация: две страницы склеены.
        741: // Вызов: текст склеен, image → плейсхолдер добавит уровень клиента
        742: // (здесь — сырой результат).
        756: // Прямой duplex: сервер печатает баннер, затем валидный ответ.
        787: // Сервер шлёт нам ping и sampling-запрос; проверяем ответы клиентской стороны.
        811: // Фейк молчит на tools/call → клиент таймаутится и шлёт cancelled.
        827: drop(conn); // закрыть поток → фейк вернёт полученное
        840: // Фейк молчит на tools/call → отмена токеном прерывает вызов и шлёт
        841: // notifications/cancelled с reason=cancelled (Esc пользователя).
        872: // call_tool на соединении: текстовые блоки склеены, image → плейсхолдер.
        894: // BatBadBut (CVE-2024-24576): .bat/.cmd как команда сервера запрещены,
        895: // включая пути и регистр; `cmd` (шелл для npx) и exe — разрешены.
        933: /// Живой смоук зонда (go/no-go, docs/research/plugin-system.md §7 этап 2):
        934: /// реальный сторонний MCP-сервер (`npx @modelcontextprotocol/server-filesystem`)
        935: /// плюс живая модель (`MINDFORK_ENGINE_URL`). Ручной мини-agentic-loop повторяет
        936: /// механику оркестратора (стрим → tool_calls → исполнение → следующий раунд);
        937: /// требует `npx` в PATH (Windows: запускается как `cmd /c npx …` — питфолл §4.6).
        950: // npx на Windows — .cmd-шим: без шелла спавн даёт ENOENT (§4.6).
        975: /// Критерий GO: модель сама вызывает MCP-инструмент чтения файла и использует
        976: /// его результат в ответе; вызовы/результаты ходят через наш клиент; ход
        977: /// завершается штатно, сервер гасится shutdown-лестницей.
        987: // Секрет в файле внутри разрешённого каталога.
        1008: // Каталог инструментов → OpenAI-схемы (как сделает этап 3).
        1029: // Ручной agentic-loop (механика orchestrator/generation.rs, вручную).
   63  src/app/orchestrator/engines.rs
        1: //! [`EngineManager`] — жизненный цикл серверов инференса/эмбеддингов: владеет
        2: //! движками (`backend`/`imp_backend`/`embedder`), опорами на managed-процессы
        3: //! (`*_handle`, `kill_on_drop`), статусами готовности и каналами фонового probe.
        4: //! Выделен из оркестратора (Фаза 3): группирует ~11 полей и серверную логику в
        5: //! когезивную единицу, пара к трейту [`ServerSupervisor`]. Оркестратор остаётся
        6: //! единственным владельцем `Chat`; здесь — только серверы, без доменного состояния.
        22: /// Расшифровывает сохранённый ключ провайдера (запись **этой** машины). `None` —
        23: /// локальный режим (провайдера нет), ключ не сохранён или запись чужая → супервайзер
        24: /// возьмёт env-фолбэк. Резолв живёт здесь, чтобы супервайзер не знал про формат
        25: /// хранения секретов (`shared::secrets`). См. docs/research/api-key-storage.md.
        31: /// Супервайзер серверов (для перезапуска при смене модели/сервера).
        33: /// Движок ассистента. `None` — external/не настроен.
        35: /// Опора на managed chat-процесс (drop → kill).
        37: /// Опора на managed embedding-процесс.
        39: /// Движок имперсонации для режимов managed/external (`None` в режиме `shared` —
        40: /// тогда используется `backend` ассистента). См. spec §11.8.
        42: /// Опора на managed-процесс сервера имперсонации.
        44: /// Текущий статус chat-сервера. Генерация стартует только в `Ready`: запрос к
        45: /// ещё загружающемуся (`Connecting`) managed-серверу вернул бы 503 («error
        46: /// status»), а для перегенерации — ещё и снёс бы прежний ответ впустую.
        48: /// Статус сервера имперсонации (для managed/external; в `shared` —
        49: /// `NotConfigured`, чип в строке статуса скрыт).
        51: /// Статус сервера эмбеддингов. Probe пока нет (RAG ленив) — двухзначен:
        52: /// `Ready` (эмбеддер настроен) / `NotConfigured` (`UnavailableEmbedder`).
        54: /// Канал статуса chat-сервера для фонового probe супервайзера.
        56: /// Канал статуса сервера имперсонации (фоновый probe).
        58: /// Токен инвалидации фонового probe текущего chat-сервера: смена режима/модели
        59: /// помечает прежний probe устаревшим, чтобы его поздний результат (напр. таймаут
        60: /// промежуточного external при перещёлкивании managed→external→openai) не
        61: /// перезаписал статус нового сервера.
        63: /// Токен инвалидации фонового probe сервера имперсонации (аналогично).
        65: /// Источник эмбеддингов для RAG (выделенный сервер — ADR 0002).
        70: /// Создаёт менеджер без поднятых серверов (статусы `NotConfigured`, эмбеддер —
        71: /// [`UnavailableEmbedder`]). Серверы поднимаются последующими `apply_*`.
        95: /// (Пере)поднимает chat-сервер по настройкам: гасит прежний managed-процесс,
        96: /// просит супервайзер настроить новый, сохраняет немедленный статус. Снимок
        97: /// статусов для UI вызывающий берёт через [`Self::statuses`]. `api_keys` —
        98: /// сохранённые ключи из конфига (см. [`stored_key`]).
        105: self.chat_handle = None; // drop старого managed-процесса (kill_on_drop)
        106: // Инвалидируем probe прежнего сервера и заводим новый токен.
        125: /// (Пере)поднимает embedding-сервер по настройкам.
        135: /// (Пере)поднимает сервер имперсонации. В режиме `shared` отдельный сервер не
        136: /// нужен — переиспользуется chat-сервер ассистента.
        143: self.imp_handle = None; // drop прежнего managed-процесса (kill_on_drop)
        144: // Инвалидируем probe прежнего сервера имперсонации (как у chat-сервера).
        171: /// Обновляет статус chat-сервера (из фонового probe).
        176: /// Обновляет статус сервера имперсонации (из фонового probe).
        181: /// Снимок статусов всех серверов для строки статуса (чат всегда, эмбеддинги/
        182: /// имперсонация — чипами, скрытыми при `NotConfigured`). См. spec §11.1.
        191: /// Возвращает движок ассистента, если chat-сервер готов (`Ready`); иначе — `Err`
        192: /// с понятным текстом (не настроен / ещё подключается / недоступен). Сам ничего
        193: /// не эмитит — вызывающий решает, куда направить ошибку. См. spec §7.
        199: .ok_or_else(|| "LLM-сервер не настроен".to_string()),
        201: Err("Сервер ещё подключается — дождитесь готовности и повторите".into())
        203: ServerStatus::NotConfigured => Err("LLM-сервер не настроен".into()),
        204: ServerStatus::Disconnected(reason) => Err(format!("Сервер недоступен: {reason}")),
        208: /// Возвращает движок имперсонации, если он готов; иначе — `Err` с понятным
        209: /// текстом. В режиме `shared` используется chat-сервер ассистента.
        221: .ok_or_else(|| "Сервер имперсонации не настроен".to_string()),
        223: Err("Сервер имперсонации ещё подключается — повторите позже".into())
        225: ServerStatus::NotConfigured => Err("Сервер имперсонации не настроен".into()),
        227: Err(format!("Сервер имперсонации недоступен: {reason}"))
        232: /// Клон источника эмбеддингов для фоновых задач RAG / контекста инструментов.
   63  src/features/rag_ingest.rs
        1: //! Сканирование файлов для индексации в базу знаний (RAG, команда `/rag add`).
        2: //! Чистая, тестируемая файловая логика: обход пути, отбор поддерживаемых
        3: //! расширений (txt/md/html/pdf/docx), опциональная рекурсия и чтение содержимого.
        4: //! Сам процесс индексации (эмбеддинг + запись) ведёт фоновая задача оркестратора.
        5: //! См. spec §9.3. Извлечение текста из HTML/PDF/DOCX — обязанность слоя `app`
        6: //! (`orchestrator/rag.rs`), а не этого модуля (иначе `features → features/tools` —
        7: //! боковой импорт, запрещённый FSD); здесь лишь опознаётся расширение.
        9: //! Здесь же живёт [`RagProgress`] — тип прогресса индексации. Он определён в слое
        10: //! `features`, чтобы им могли пользоваться и `app` (эмитит события), и `screens`
        11: //! (рисует индикатор), не нарушая направление зависимостей FSD.
        15: /// Поддерживаемые расширения файлов (нижний регистр, без точки): текст, markdown,
        16: /// HTML, PDF и DOCX. Из HTML/PDF/DOCX извлекается простой текст на слое `app`
        17: /// (`orchestrator/rag.rs`), этот модуль лишь опознаёт расширение.
        20: /// Прогресс фоновой индексации файлов в RAG. Шлётся задачей оркестратора и
        21: /// отображается экраном чата (баннер со спиннером + итоговая заметка).
        24: /// Сканирование завершено — начинаем индексацию `total` файлов.
        26: /// Индексируется файл `index` из `total` (1-based) с именем `name` из `dir`.
        27: /// `chunks_done`/`chunks_total` — прогресс эмбеддинга **внутри** этого файла:
        28: /// сколько чанков уже эмбеддировано и записано и сколько всего (эмбеддинг идёт
        29: /// под-батчами, см. `orchestrator/rag.rs::EMBED_BATCH_CHUNKS`). `chunks_total == 0`
        30: /// — файл только начат (ещё не чанкован) либо у него нет чанков.
        39: /// Индексация завершена (или прервана при `cancelled`).
        46: /// Удаление из базы завершено (`/rag remove`): снято `chunks` фрагментов
        47: /// (0 — по указанному пути ничего не найдено).
        49: /// Перечень источников базы знаний (`/rag list`): по источнику — счётчик чанков
        50: /// и дата. Пустой список — база пуста.
        54: /// Не удалось выполнить операцию (путь недоступен, нет файлов, эмбеддер не
        55: /// настроен и т.п.).
        59: /// Поддерживается ли файл по расширению (регистронезависимо).
        66: /// Совпадает ли расширение пути с `ext` (регистронезависимо)?
        73: /// HTML-файл по расширению (`html`/`htm`, регистронезависимо)? Слой `app` по этому
        74: /// признаку решает, извлекать ли читаемый текст (иначе читает содержимое как есть).
        79: /// PDF-файл по расширению (`pdf`, регистронезависимо)? Слой `app` извлекает из него
        80: /// текст крейтом `pdf-extract` (см. `orchestrator/rag.rs`, `features/doc_extract.rs`).
        85: /// DOCX-файл по расширению (`docx`, регистронезависимо)? Слой `app` извлекает из него
        86: /// текст (ZIP + `word/document.xml`, см. `features/doc_extract.rs`).
        91: /// Собирает список поддерживаемых файлов по пути:
        92: /// - путь-файл → он сам, если расширение поддерживается (иначе пусто);
        93: /// - путь-директория → все поддерживаемые файлы (рекурсивно при `recursive`).
        95: /// Результат отсортирован для детерминизма. Ошибки чтения вложенных директорий
        96: /// пропускаются (обход устойчив), а недоступность корневого пути — это ошибка.
        111: /// Рекурсивный обход директории (ошибки отдельных входов пропускаются).
        131: /// Канонический строковый ключ источника для записи в RAG: абсолютный путь без
        132: /// вербатим-префикса `\\?\` (Windows). Используется и при добавлении (стабильный
        133: /// `source`), и при удалении (тот же ключ независимо от того, как путь введён —
        134: /// относительно, иным регистром или разделителем). Если канонизация не удалась
        135: /// (файла уже нет на диске) — путь как есть (lossy).
        143: /// Снимает вербатим-префикс `\\?\`, который `canonicalize` добавляет на Windows
        144: /// (чтобы хранимый/показываемый путь был обычным).
        152: /// Читает текстовый файл в строку, отбрасывая ведущий UTF-8 BOM. Не-UTF-8 или
        153: /// нечитаемый файл → ошибка (вызывающий пропускает такой файл).
        185: // PDF/DOCX поддержаны наравне с txt/md/html.
        189: // is_pdf/is_docx выделяют именно свои расширения (регистронезависимо)...
        194: // ...и не срабатывают на чужих/неподдержанных расширениях.
        200: // .doc (legacy) намеренно не поддержан.
        207: // HTML поддержан наравне с txt/md.
        211: // is_html выделяет именно HTML-расширения (регистронезависимо)...
        215: // ...и не срабатывает на прочих поддержанных/неподдержанных расширениях.
        229: // Неподдерживаемый файл → пустой список (не ошибка).
        240: write(&dir.path().join("c.rtf"), "c"); // не поддержан
        241: write(&dir.path().join("sub/d.txt"), "d"); // в подпапке
        283: // Каноничный ключ совпадает для одного и того же файла.
        285: // Несуществующий путь → возвращается как есть (не паникует).
   59  src/features/tools/mcp.rs
        1: //! Обёртка [`McpTool`] — инструмент MCP-сервера за трейтом [`Tool`]
        2: //! (docs/research/plugin-system.md §4.4, этап 3). Описание и JSON-схема — снимок
        3: //! из `tools/list` сервера (**не локализуются** — граница i18n, как probe-ошибки
        4: //! движка); `invoke` → `tools/call` с per-call таймаутом и отменой (`ctx.cancel`);
        5: //! результат клипуется (`max_result_chars`) — ограничение входа в промпт.
        7: //! Id инструмента — `mcp__<server>__<tool>` (конвенция Claude Code), нормализован
        8: //! под лимиты провайдеров function-имён ([`mcp_tool_id`]): `[A-Za-z0-9_-]`,
        9: //! ≤ 64 символов (усечение + hex-хвост от полного имени против коллизий).
        10: //! `enabled_by_default = false` — двойной opt-in (мастер-гейт `config.mcp.enabled`
        11: //! и тумблер в профиле); в статический `CATALOG` эти инструменты не входят
        12: //! (динамический каталог едет снимком в `AppEvent::Settings`).
        28: /// Снимок MCP-хоста для UI (едет в `AppEvent::Settings`): динамический каталог
        29: /// инструментов (тумблеры профиля) + статусы серверов (строки в секции
        30: /// «Инструменты»). FSD: живёт в `features` — `screens` не импортирует `app`.
        33: /// Метаданные инструментов всех готовых серверов (с полными описаниями).
        35: /// Статусы серверов (по id).
        39: /// Снимок одного MCP-сервера для UI.
        44: /// Число зарегистрированных инструментов (0 — сервер не готов).
        46: /// Каталог сервера изменился против TOFU-пина — инструменты не
        47: /// зарегистрированы, ждём подтверждения пользователя (Enter в настройках).
        51: /// TOFU-хэш каталога инструментов сервера: sha256 по отсортированным
        52: /// (имя, описание, JSON-схема) — любое изменение любого поля (rug-pull, tool
        53: /// poisoning через описания/схемы) меняет хэш. Детерминизм: инструменты
        54: /// сортируются по имени, ключи JSON-объектов у `serde_json` упорядочены (BTreeMap).
        75: /// Префикс id инструментов MCP-серверов. По нему [`super::effective_tool_ids`]
        76: /// гейтит их мастер-выключателем `config.mcp.enabled` (инструменты динамические —
        77: /// в статическом `CATALOG` их нет, lookup гейта невозможен).
        80: /// Потолок длины имени function-инструмента у провайдеров (исторически
        81: /// `^[a-zA-Z0-9_-]{1,64}$` у OpenAI/Anthropic).
        84: /// Полный id инструмента MCP-сервера: `mcp__<server>__<tool>`, санитизированный
        85: /// под лимиты провайдеров: символы вне `[A-Za-z0-9_-]` → `_`; длиннее 64 —
        86: /// усечение + `_`-разделитель + 8 hex от хеша **полного** имени (коллизии
        87: /// длинных имён не склеиваются). Чистая функция — тестируема.
        110: /// Интернирует строку в `&'static str` (дедуп через глобальный набор).
        111: /// Нужен для `Tool::ui_label`/`ToolInfo.label` (`&'static str`): лейблы MCP —
        112: /// динамические имена инструментов; их конечное число, утечка ограничена
        113: /// (прецедент — интернирование кодов языков в `shared/i18n`).
        126: /// Инструмент MCP-сервера за трейтом [`Tool`]. Держит разделяемый транспорт
        127: /// соединения ([`McpConnection`]) — жизненный цикл процесса сервера у
        131: /// Имя инструмента на сервере (оригинальное, без префикса/санитизации).
        133: /// Снимок описания из `tools/list` (не локализуется — текст сервера).
        135: /// Снимок JSON-схемы аргументов (`inputSchema`).
        137: /// Короткий лейбл для тумблера профиля (интернированное имя инструмента).
        140: /// Per-call таймаут (`tool_timeout_secs` сервера).
        142: /// Клип результата в символах (`max_result_chars` сервера).
        147: /// Обёртка над инструментом `info` сервера `server_id` на соединении `conn`.
        168: /// Клипует текст до `max` символов (по символам, не байтам — кириллица), с
        169: /// пометкой об усечении на языке каркаса профиля (ось A).
        185: // Текст сервера — граница i18n (см. docs/history/i18n.md §2.3).
        198: // `isError:true` — ошибка исполнения на сервере: текст отдаётся модели как
        199: // результат (не протокольная ошибка, spec tools §error handling). Пустой
        200: // текст ошибки подменяем пометкой — модель должна понять, что вызов не удался.
        226: // Двойной opt-in (развилка Р7): включается вручную в профиле.
        246: // Точки/прочие символы (спека допускает `.`) → `_`.
        255: // Детерминированность и различимость: другое полное имя → другой хвост.
        281: /// Фейковый MCP-сервер поверх duplex, отвечающий на tools/call заданным
        282: /// содержимым; возвращает соединение клиента.
        315: let conn = conn_with_call_reply("0123456789".repeat(10)); // 100 симв.
        336: // Сервер молчит → отмена ctx.cancel прерывает вызов (Esc не блокируется).
   56  src/app/orchestrator/self_consolidation.rs
        1: //! Авто-консолидация «модели себя» («сон» модели себя, этап A1): каждые N ответов
        2: //! ассистента в чате фоновая задача просит модель пересмотреть свою «модель себя» и
        3: //! **самой** её консолидировать — слить дубли наблюдений (`@self`-заметок), сжать
        4: //! раздутое описание (`summary`), связать противоречия. Как авто-рефлексия и
        5: //! авто-консолидация заметок, это **мини agentic-loop**: модель вызывает
        6: //! self-model/note-инструменты, петля их исполняет (пишут напрямую в `Storage`). Чат не
        7: //! мутируется, в UI ничего не стримится — «сон» молчалив и опционален
        8: //! (`config.self_model.auto_consolidate_every`, по умолчанию выкл). Отдельный тумблер
        9: //! от авто-рефлексии: гейты/данные модели себя и заметок уже разведены, свой счётчик
        10: //! точнее. См. docs/history/self-model-consolidation.md (этап A1).
        27: /// Потолок токенов ответа на раунд «сна» модели себя (с запасом на «мысли» перед вызовом).
        29: /// Лимит раундов мини agentic-loop «сна» модели себя (бэкстоп от зацикливания).
        31: /// Лимит времени на всю консолидацию модели себя.
        34: /// Инструменты, доступные «сну» модели себя (пересекаются с набором профиля). Над
        35: /// наблюдениями-заметками (`@self`): переписать почти-дубль (`note_revise`), заместить
        36: /// со «шрамом» (`note_supersede`), слить (`note_merge`); граф — связать
        37: /// противоречащие/уточняющие (`note_link`/`note_neighbors`). Плюс `update_self_model`
        38: /// (сжать раздутый `summary`) и `update_user_model` (привести собеседника).
        39: /// `get_self_model` даёт полные id наблюдений и текущий `summary`. `note_recall`
        40: /// **не даём** — он скрывает `@self`; полные id модель берёт из `get_self_model`
        41: /// (как рефлексия). См. docs/history/self-model-consolidation.md (этап A1).
        53: /// Системное сообщение фоновой консолидации модели себя: обрамление + единый
        54: /// `POLICY_CORE` (те же правила ведения, что у протокола/рефлексии). Строится в
        55: /// рантайме, поскольку склеивает `const`-фрагмент с константой правил.
        64: /// Вызывается после успешной генерации (`handle_done`): считает ответы ассистента
        65: /// и при достижении порога запускает фоновую консолидацию «модели себя». Тихо
        66: /// ничего не делает, если фича выключена, профиль не включил инструменты модели
        67: /// себя, «сон» уже идёт, нечего консолидировать (наблюдений < 2 и `summary` не
        68: /// раздут) или сервер не готов. Счётчик каденции сбрасывается **только при
        69: /// фактическом спавне** — пропуск по гейту не теряет накопленный цикл.
        77: let lang; // язык служебного каркаса профиля (ось A)
        90: // Гейт: профиль включает инструменты модели себя (как и инъекция/рефлексия).
        107: // Счётчик ответов с прошлого «сна»: инкремент; если порог не достигнут — выходим
        108: // (счётчик копится дальше). Сброс — только при фактическом спавне (ниже), чтобы
        109: // пропуск по гейту не терял накопленный цикл.
        118: return; // уже идёт — пропускаем без сброса (повторим на след. ходу)
        121: // Нечего консолидировать? Сигнал есть, если наблюдений (`@self`) ≥ 2 (обзор
        122: // self-консолидации непуст) ИЛИ описание себя раздуто сверх ориентира. Иначе —
        123: // выходим без сброса счётчика (повторим позже).
        143: // Сервер готов? Иначе тихо пропускаем (счётчик не сброшен).
        147: // Все гейты пройдены — сбрасываем счётчик и запускаем.
        150: // Дайджест: обзор self-консолидации (похожие пары наблюдений / contradicts / без
        151: // связей; `None` при наблюдениях < 2) + подсказка о раздутом описании (если есть).
        157: // Гейт выше это исключает; защитно — нечего консолидировать.
        161: // Токен отмены — до контекста: его клон едет в `ToolContext.cancel`.
        188: // Спавним задачу и фиксируем слот (флаг «идёт» + тихий индикатор в статус-баре).
        198: label: "авто-консолидация себя",
        202: // A2: семантика summary↔наблюдения (эмбеддинг абзацев summary на лету в
        203: // задаче). См. docs/history/self-model-consolidation.md §A2.
        219: /// Референсная локаль (ru) для ассертов на русские подстроки (пинят ru-бандл).
        227: // Собрано из единого POLICY_CORE (те же правила, что у протокола ведения).
        229: // Плюс специфичное обрамление «сна» модели себя.
        235: /// Per-language (§3.5 docs/history/i18n.md): системное сообщение собирается на КАЖДОМ
        236: /// вшитом языке, встраивает `policy_core` того же языка, плейсхолдер `{core}`
        237: /// подставлен (без остатка), несёт tool-имена (стабильны, не переводятся).
        262: // Наблюдения (граф/слияние) + сжатие summary — есть; note_recall нет (скрывает @self).
   55  src/app/orchestrator/settings.rs
        1: //! Конфигурация приложения и (пере)запуск серверов инференса/эмбеддингов через
        2: //! [`ServerSupervisor`]. Оркестратор — единственный писатель в `settings.json`.
        10: /// Применяет правки конфигурации: сохраняет, перезапускает сервер/реестр при
        11: /// необходимости и переэмитит настройки. Единственный писатель в `settings.json`.
        14: // «Последний открытый чат» — свойство оркестратора, а не редактируемая на
        15: // экране настроек величина. Снимок конфига из UI может нести устаревшее
        16: // значение (например `None` со старта) — сохраняем актуальное, чтобы правка
        17: // настроек не стёрла память о чате.
        19: // Сохранённые API-ключи — тоже свойство оркестратора (`handle_set_api_key`):
        20: // экран настроек шлёт сам ключ отдельной командой, а в снимке конфига их
        21: // не несёт. Без восстановления правка любой настройки стёрла бы ключи.
        23: // TOFU-пины каталогов MCP — тоже свойство оркестратора (persist_mcp_pin),
        24: // в UI не редактируются: наследуем по id сервера, если снимок из UI их не
        25: // несёт (устаревшая копия) — правка настроек не сбрасывает доверие и не
        26: // провоцирует ложный diff `config.mcp` (лишний рестарт серверов).
        39: self.config = old; // откат к прежнему состоянию
        42: // Смена настроек chat-сервера (модель/режим/порт/…) — перезапуск (spec
        43: // §11.6), но отложенный: экран настроек применяет правку при коммите
        44: // каждого поля, и дебаунс коалесит серию быстрых правок в один рестарт
        45: // ([`super::restart_queue::RestartQueue`], флаш по дедлайну в петле).
        48: // Смена режима меняет набор доступных параметров семплинга
        49: // (get_sampling/set_sampling) — пересобираем реестр под новый
        50: // провайдер немедленно (дёшево, in-memory; схема должна быть
        51: // актуальна уже со следующего хода).
        56: // Смена настроек сервера имперсонации — отложенное пере-подключение.
        60: // Смена настроек embedding-сервера — отложенное пере-подключение.
        64: // Смена настроек MCP-серверов — отложенное пере-поднятие (дебаунс, как
        65: // движки): гашение/спавн процессов — дорогая операция.
        69: // Смена параметров инструментов — пересборка реестра (python_path, лимиты).
        76: /// Сохраняет введённый в настройках API-ключ провайдера: шифрует машинным
        77: /// ключом (`shared::secrets`) и кладёт в `config.api_keys` записью **этой**
        78: /// машины. Пустой ключ — удаление. Затрагивает все слоты (чат/имперсонация/
        79: /// эмбеддинги), у которых активен этот провайдер: они помечаются на отложенный
        80: /// (пере)подъём — ключ подхватится следующим `flush_restarts`.
        82: /// Плейнтекст живёт только в аргументе и в HTTP-клиенте: на диск уходит
        83: /// шифротекст, в UI-снимок конфига ключи не попадают вовсе (UI получает лишь
        84: /// признак «настроен»). См. docs/research/api-key-storage.md.
        111: self.config.api_keys = old; // откат к прежнему состоянию
        114: // Пере-поднимаем только те серверы, чей активный провайдер сменил ключ
        115: // (дебаунс тот же, что у правок движка — см. `flush_restarts`).
        129: /// Применяет отложенные дебаунсом (пере)запуски серверов (дедлайн истёк):
        130: /// по одному `apply_*` на каждый помеченный сервер и один общий снимок
        131: /// статусов. Читает **финальный** `self.config` — конфиг заменяется ещё при
        132: /// правке, так что серия правок даёт один рестарт с итоговыми значениями.
        159: /// (Пере)поднимает chat-сервер по `config.engine` и эмитит снимок статусов.
        160: /// Немедленный путь стартового подъёма (до петли `run`); правки настроек
        161: /// идут через дебаунс-очередь `restarts` → [`Self::flush_restarts`].
        169: /// (Пере)поднимает embedding-сервер по `config.embed` и эмитит снимок статусов
        170: /// (чип эмбеддингов в строке статуса появляется/исчезает по настройке).
        177: /// (Пере)поднимает сервер имперсонации по `config.impersonation_engine`. В режиме
        178: /// `shared` отдельный сервер не нужен — переиспользуется chat-сервер ассистента.
        189: /// (Пере)поднимает MCP-серверы по `config.mcp`: прежние гасятся, включённые
        190: /// спавнятся заново; их инструменты придут событиями `Ready` (см.
        191: /// [`super::mcp::McpManager`]). Реестр пересобирается сразу — обёртки прежнего
        192: /// поколения (мёртвые соединения) уходят из него немедленно.
   54  src/features/sandbox_setup.rs
        1: //! Провизия ассетов песочницы Python (`mindfork sandbox setup`, Фаза 2 —
        3: //! Скачивает в `data/sandbox/`: бинарь `wasmer` (платформенный tar.gz с GitHub),
        4: //! `python.webc` (через сам `wasmer`), колёса пакетов (numpy с wasix-индекса,
        5: //! requests-стек с PyPI) — всё по **lock-списку с точными URL + sha256** (устойчиво
        6: //! к «latest»). Провизия идемпотентна; сетевой слой тонкий, логика — чистая/тестируемая.
        8: //! Слой `features`: без TUI (команда CLI печатает прогресс в stdout). Расположение
        9: //! бинаря после распаковки совпадает с тем, что ищет `shared::sandbox` (общий
        10: //! резолвер [`crate::shared::sandbox::locate_wasmer`]).
        24: /// Версия `wasmer`, к которой привязан lock-список (GitHub release tag `v<...>`).
        26: /// Пакет CPython в реестре Wasmer (скачивается в `python.webc`).
        28: /// User-Agent для скачиваний (GitHub/PyPI иногда отвергают пустой UA).
        31: /// Платформенный архив `wasmer` с GitHub (tar.gz). `os`/`arch` — из
        32: /// [`std::env::consts`]. Распаковывается целиком в `<dir>/wasmer-dist/`.
        40: /// Lock-список архивов `wasmer` v7.2.0 (sha256 — из GitHub release assets `digest`).
        68: /// Колесо Python-пакета (`.whl` = zip). `dir` — имя каталога пакета в `site-packages`
        69: /// (для идемпотентности: есть каталог → колесо уже распаковано).
        76: /// Lock-список колёс. **numpy**/**pandas** — нативные wasix-колёса с
        77: /// `pythonindex.wasix.org`; их чистые зависимости (dateutil/six/pytz/tzdata) и
        78: /// **requests-стек** (requests/urllib3/certifi/idna/charset_normalizer) — чистый
        79: /// Python с PyPI (`py3-none-any`). Версии закреплены; sha256 — из индекса/PyPI.
        86: // pandas (нативное wasix-колесо) + его чистые зависимости.
        139: /// Параметры провизии.
        142: /// Перекачать/переустановить всё, даже если уже на месте.
        146: /// Провизия всей песочницы в каталог `dir` (`data/sandbox/`). `progress` — колбэк
        147: /// строк для stdout. Идемпотентно: уже установленное пропускается (кроме `force`).
        171: /// Прогрев кэша компиляции: один прогон компилирует `python.wasm` (+ нативные `.so`
        172: /// numpy/pandas) в `<dir>/cache`, чтобы **первый реальный вызов** инструмента был
        173: /// тёплым — без многосекундной компиляции на глазах у пользователя (заменяет «баннер
        174: /// первого запуска»). «Лучшее усилие»: сбой прогрева не проваливает установку. Идёт
        175: /// через реальный [`WasmerSandbox`], так что кэш и пути совпадают с рантаймом.
        179: // `import pandas` тянет и интерпретатор, и нативные модули numpy/pandas (самый
        180: // тяжёлый путь компиляции); даже при сбое импорта интерпретатор уже в кэше.
        191: /// Архив `wasmer` для платформы `(os, arch)` (чистая, тестируемая).
        196: /// Гарантирует наличие бинаря `wasmer` в `dir`; возвращает путь к нему.
        239: // Чистая переустановка каталога распаковки (устойчиво к прерванной прошлой).
        256: /// Гарантирует наличие `python.webc` (скачивается самим `wasmer` из реестра).
        270: // Дом/кэш wasmer — под каталогом песочницы (самодостаточно, не в ~/.wasmer).
        306: /// Гарантирует распаковку всех колёс в `site-packages/`.
        331: /// HTTP-клиент с User-Agent (rustls, как в остальном проекте).
        339: /// Скачивает `url` в файл `dest` потоком (крупные архивы не буферим в память),
        340: /// считая sha256 на лету; сверяет с `expected`.
        366: let mut next_report: u64 = 32 * 1024 * 1024; // отчёт каждые ~32 МБ
        397: /// Скачивает `url` целиком в память (небольшие колёса), сверяет sha256.
        420: /// Сверяет хэш (сырые байты дайджеста) с ожидаемым hex; ошибка — с указанием URL.
        436: /// Байты → строка hex (нижний регистр), без крейта `hex`.
        446: /// Распаковывает tar.gz-архив в каталог `dest`.
        466: /// Распаковывает колесо (zip) в каталог `site` (защита от zip-slip через
        467: /// `enclosed_name`, как в `features::backup`).
        499: /// Референсная локаль для тестов (тексты ошибок не проверяются — ru байт-в-байт).
        510: // Неизвестная платформа — нет автоскачивания.
        531: // Регрессия против забытого `loc`: en-сообщение без кириллицы, ru — русское.
        552: // Все URL — https, все sha256 — 64 hex-символа.
        561: // Крафт-zip как «колесо»: файл пакета + dist-info.
        581: // Собираем маленький tar.gz с файлом bin/wasmer и распаковываем.
   53  src/app/orchestrator/tests/generation.rs
        1: //! Тесты оркестратора — генерация, agentic-loop, отмена, гейты готовности. Часть модуля [`super`]
        2: //! (фикстуры в mod.rs). См. docs/history/refactoring-god-objects.md, этап 3.
        19: // override = None → берётся дефолт профиля.
        22: // override = Some → берётся он (приоритет чата).
        29: // нет ни override, ни дефолта профиля → глобальный.
        45: // Ждём активации и узнаём id активного чата.
        61: // Завершаем и проверяем, что чат сохранён с двумя сообщениями.
        79: // Облачный режим (OpenAI) + имя модели; глобальный семплинг с top_k, который
        80: // строгий облачный диалект не принимает → в снимок метаданных он попасть не должен.
        130: // Доступные в режиме поля сохранены; недоступные top_k и temperature (её
        131: // отвергают GPT 5.5/5.6) — обнулены.
        140: // Две текстовые дельты (live-счёт = 2), затем точный usage от сервера (= 5).
        160: // Сразу после старта — оценка переписки (промпта): context=Some, неточная,
        161: // ответа ещё нет (completion=0).
        178: // Дельты ответа → счётчик ответа растёт, оценку переписки не трогают (None).
        196: // Точный счётчик из usage сервера приходит до завершения: completion=5, context=12.
        227: // Два разных ответа по очереди: исходный ход → «первый», перегенерация → «второй».
        254: // ChatList после Finished — признак, что handle_done применил ответ (state Idle).
        270: // Старый ответ заменён новым; сообщение пользователя не дублируется.
        315: // Обмен удалён полностью (дефолтный чат без приветствия → пусто).
        319: // Удалённый обмен сохранён для ручного восстановления (сообщение пользователя +
        320: // ответ ассистента), черновик ввода был пуст (spec §11.7).
        332: // Чат с приветствием-ассистентом, но без сообщения пользователя — нечего
        333: // перегенерировать; команда не должна стартовать генерацию.
        349: // Состояние осталось Idle (генерация не запущена), история не тронута.
        356: // Сервер ещё подключается (managed грузит модель) — перегенерация не должна
        357: // ни сносить прежний ответ, ни уходить запросом на не-готовый сервер (иначе
        358: // 503 «engine returned an error status» и потеря ответа). Регресс-тест.
        364: orch.engines.server_status = ServerStatus::Connecting; // ещё не готов
        376: // Ответ сохранён, генерация не стартовала (история не усечена).
        388: // Сервер ещё подключается — отправка отклоняется, но текст возвращается в
        389: // поле ввода (RestoreInput), а в чат сообщение не добавляется.
        405: // Среди эмитнутых событий — ошибка и возврат текста в поле ввода.
        474: // Раунд 1: вызов note_save → раунд 2: финальный текст.
        506: // Событие исполнения инструмента доходит до UI.
        518: // История: user → assistant(tool_calls) → tool → assistant(финал).
        529: // Заметка действительно сохранена инструментом (изоляция по профилю).
        541: // python_exec выключен глобально (spawn_orch: python_enabled = false) —
        542: // даже если модель его вызовет, loop откажет, не исполняя.
        588: // Движок всегда просит инструмент — должен сработать лимит раундов.
        612: // Дойдём до Finished; лимит породит ошибку-пометку, но генерация завершится.
        623: // При лимите раундов модель не должна оставить пользователя без ответа: после
        624: // исчерпания раундов делается финальный раунд БЕЗ инструментов, где модель
        625: // сводит итог. Скрипты: раунд 1 и 2 — вызовы инструмента, 3-й (форс-синтез) — текст.
        666: // Последнее сообщение — финальный текст ассистента (свод), а не пустота.
        683: // Раунд 1: текст + вызов send_followup_message → раунд 2: второе сообщение.
        709: // UI получает сигнал «начать новый пузырь».
        721: // История: user → assistant(followup tool_call) → tool → assistant(2-е, new_bubble).
        741: // Управляющий инструмент ничего не отбрасывает.
        748: // Раунд 1: неверный текст + вызов rewrite_current_message → раунд 2: переписанный.
        774: // UI получает сигнал «отбросить текущий пузырь».
        786: // История: user → assistant(переписанный). Неверная версия — в архиве удалённого.
        799: // Отброшенный (неверный) ответ + его tool-сообщение сохранены для восстановления.
   52  Cargo.toml
        11: # Буфер обмена (Windows + Linux). `default-features = false` отключает image-data
        12: # (тянет крейт `image`): нам нужен только текст. См. копирование переписки чата.
        16: # Декодирование аудио из ответов TTS-провайдеров (Gemini отдаёт PCM в base64
        17: # внутри `inlineData.data`). Уже в графе транзитивно; версия пришпилена к той, что
        18: # в Cargo.lock, чтобы не плодить дубль (прецедент — quick-xml). См. shared/tts.
        21: # AEAD-шифрование сохранённых API-ключей на Linux (схема `machine-key-v1`: ключ
        22: # выводится из /etc/machine-id через HKDF). RustCrypto, чистый Rust. На Windows
        23: # та же задача решается системным DPAPI (windows-sys ниже). См. shared/secrets.rs,
        28: # Стандартные ОС-папки для непортабельного режима хранения данных
        29: # (Windows %APPDATA%\mindfork-rs, Linux ~/.local/share/mindfork-rs). См. shared/paths.rs.
        32: # Распаковка архива `wasmer` (tar.gz) в `mindfork sandbox setup` (Фаза 2 песочницы
        33: # Python). flate2 — чистый Rust (miniz_oxide, без C-зависимостей), tar — чистый Rust.
        36: # Вывод ключа шифрования API-ключей из machine-id (HKDF-SHA256; `sha2` уже есть).
        37: # Паттерн предписан документацией systemd — machine-id не используется сырым.
        39: # Рендер ```mermaid-блоков ленты в Unicode/ASCII-графику (flowchart/sequence,
        40: # whitelist + жёсткий фолбэк на исходник). Чистый Rust (net-new только ascii-dag);
        41: # ≥0.56.1 обязательно — 0.56.0 паниковал/портил подписи на кириллице (наш апстрим-фикс,
        42: # leboiko/markdown-reader#29/#30). 0.57.0 закрыл наш feature request #32 (max_width как
        43: # жёсткий бюджет: RenderOptions::max_width_strict + Error::TooWide) — мы его НЕ используем,
        44: # наш пост-чек по display_width точнее (см. mermaid.rs). См. docs/research/mermaid-ascii-rendering.m
        46: # Извлечение текста из PDF для индексации в RAG (`/rag add`, этап B1b). Чистый Rust
        47: # (lopdf + парсеры шрифтов/CFF/CMap, без C-зависимостей); качество извлечения
        48: # «лучшее усилие» — сканированный/картиночный PDF даёт пустой текст, это допустимо.
        49: # См. features/doc_extract.rs, docs/rag-sources-retrieval.md §B1b.
        52: # Разбор `word/document.xml` внутри DOCX для индексации в RAG (`/rag add`, этап B1b).
        53: # Уже в графе транзитивно (через syntect → plist); версия пришпилена к той, что в
        54: # Cargo.lock, чтобы не плодить дубль. См. features/doc_extract.rs.
        57: # Воспроизведение синтезированной речи (`/tts`) в процессе приложения: очередь
        58: # источников (`Player::append`) даёт конвейер «синтез чанка N+1 во время
        59: # проигрывания N» и мгновенную отмену. `default-features = false` — только нужные
        60: # декодеры (сырой PCM облаков идёт мимо декодера, `SamplesBuffer`). Единственная
        61: # новая системная зависимость — ALSA на Linux (`libasound2-dev` при сборке).
        62: # См. docs/research/tts.md §6.
        69: # Проверка целостности (sha256) скачанных ассетов песочницы (Фаза 2). Чистый Rust.
        74: # Определение языка по локали ОС при свежей установке (нет defaults.json/settings.json).
        75: # Кросс-платформенный (Windows/Linux), чистый Rust. См. shared/i18n::detect_os_language.
        78: # Распаковка tar-архива `wasmer` (см. flate2 выше). Чистый Rust.
        90: # Резервное копирование/восстановление пользовательских данных (zip-архив с
        91: # настраиваемой степенью сжатия deflate). default-features=false отключает bzip2/zstd
        92: # (нативные C-зависимости) — нужен только deflate + store. См. features/backup.rs.
        95: # Windows-специфика:
        96: # * `Win32_System_JobObjects` — OS-level лимит памяти песочницы Python через Job
        97: #   Object (`JOB_OBJECT_LIMIT_PROCESS_MEMORY`), защита хоста от OOM при рантайм-
        98: #   скрипте. Только Windows: на Unix rlimit ненадёжен с бэкендом V8 (он резервирует
        99: #   большое виртуальное адресное пространство). См. ADR 0005 / features/tools/python.rs.
        101: #   для машинно-привязанного шифрования сохранённых API-ключей. См. shared/secrets.rs,
        111: # `test-util` включает паузу/перемотку виртуального времени tokio в тестах
        112: # (`start_paused`) — детерминированные тесты дебаунса рестарта серверов без
        113: # реального ожидания. Только для тест-сборок (feature unification dev-deps).
        116: # Релизный профиль (Windows + Linux): оптимизация размера/скорости.
        117: # panic=unwind сохранён намеренно — нужны drop-guard/panic-hook для восстановления
        118: # терминала при панике (ratatui::init ставит hook; M0).
   51  src/shared/tts/playback.rs
        1: //! Воспроизведение синтезированной речи через `rodio` **в процессе приложения**.
        3: //! Почему в процессе, а не сайдкар-плеером: нужна очередь источников с мгновенной
        4: //! отменой, а на Windows пригодного встроенного CLI-плеера нет (см.
        5: //! docs/research/tts.md §6). `Player::append` играет источники последовательно —
        6: //! отсюда бесплатно получается конвейер «синтезируем чанк N+1, пока играет N», а
        7: //! при опустошении очереди наступает тишина до следующего чанка.
        9: //! Две TUI-ловушки, обе учтены здесь:
        10: //! 1. `log_on_drop(false)` — иначе rodio печатает в stderr **поверх TUI**;
        11: //! 2. на Linux сама libasound шумит в stderr при энумерации устройств (cpal#384) —
        12: //!    поэтому устройство открывается **лениво**, только по первой команде `/tts`
        13: //!    (а не на старте приложения), и ошибка открытия — это `Err`, а не паника
        14: //!    (headless/CI/машина без звуковой карты → мягкая деградация, как у
        25: /// Открытое аудио-устройство с очередью воспроизведения.
        27: /// Хэндл и плеер `Send + Sync` (cpal 0.17), поэтому живут прямо в фоновой
        28: /// tokio-задаче озвучивания — выделенный аудио-поток не нужен. При дропе
        29: /// устройство закрывается, очередь пропадает вместе с задачей.
        31: /// Держим устройство живым: при его дропе звук прекращается.
        37: /// Открывает устройство по умолчанию и заводит очередь.
        39: /// Возвращает `Err` (а не паникует), если звука нет: headless-окружение, CI,
        40: /// отсутствующее/занятое устройство. Вызывающий показывает это заметкой.
        43: .context("не удалось открыть аудио-устройство")?;
        44: // Иначе rodio при дропе пишет в stderr, а его занимает TUI.
        53: /// Ставит клип в конец очереди. Играть он начнёт, когда доиграют предыдущие.
        61: // Сырой PCM идёт мимо декодера: s16le → f32 и прямо в буфер.
        66: let rate = NonZero::new(sample_rate).context("нулевая частота дискретизации")?;
        67: let ch = NonZero::new(channels).context("нулевое число каналов")?;
        76: .context("не удалось разобрать аудио от TTS-сервера")?;
        83: /// Сколько клипов ещё в очереди (включая играющий). По нему конвейер
        84: /// придерживает синтез следующего чанка, чтобы не синтезировать всё вперёд.
        89: /// Всё ли проиграно (очередь пуста).
        94: /// Немедленно останавливает воспроизведение и очищает очередь.
        99: /// Приостанавливает воспроизведение, **сохраняя очередь** (в отличие от
        100: /// [`stop`](Self::stop)). Звуковая карта перестаёт потреблять сэмплы; синтез
        101: /// вперёд сам придерживается (очередь не дренится), а фоновая задача не
        102: /// завершается (`is_drained` остаётся `false`). Идемпотентно.
        107: /// Продолжает воспроизведение после [`pause`](Self::pause). Идемпотентно
        108: /// (возобновление играющего плеера — no-op).
        114: /// Знаковый 16-битный little-endian PCM → сэмплы `f32` в диапазоне −1.0…1.0
        115: /// (внутреннее представление rodio). Нечётный хвостовой байт отбрасывается.
        129: // 0 → 0.0; i16::MIN → −1.0; 16384 → 0.5. Нечётный хвост отбрасывается.
        137: /// Живой смоук воспроизведения (нужна звуковая карта): синтезируем тон 440 Гц
        138: /// в том же формате, что отдают облака (PCM s16le 24 кГц mono), ставим в
        139: /// очередь и ждём, пока доиграет. Проверяет самый рискованный стык — PCM→rodio
        140: /// (частота, знаковость, конверсия в f32) и то, что очередь реально
        141: /// опустошается **в реальном времени**, а не мгновенно. Звучание (тон должен
        142: /// быть слышен ~0.4 с) — ручная проверка.
        174: // Проигрывание идёт в реальном времени: не мгновенно и не бесконечно.
        182: /// В CI/headless звука нет — важно, что открытие устройства возвращает
        183: /// **ошибку, а не панику** (мягкая деградация). На машине со звуком
        184: /// устройство открывается и очередь стартует пустой; оба исхода допустимы.
        191: // Пустой клип не ставится в очередь и не роняет плеер.
   50  src/features/data_migration.rs
        1: //! Оркестрация миграций сохраняемых данных при старте приложения.
        3: //! Чистый Value-уровневый каркас (константы версий, определение версии, прогон шагов) —
        4: //! в [`crate::shared::storage::schema`]; здесь — файловый I/O, гейты (downgrade / битый
        5: //! файл), pre-migration бэкап и control-parse в типизированные структуры. Живёт в
        6: //! `features` (не в `shared`), потому что pre-migration бэкап — это `features::backup`,
        7: //! а `shared` не может зависеть от `features` (FSD). См.
        8: //! [docs/history/release-engineering.md](../../docs/history/release-engineering.md) §3.4 и ADR 0006
        10: //! Поток (release-engineering.md Ф9–Ф11): прочитать каждый файл как `Value` → определить
        11: //! версию → `> current` — **отказ запуска** (данные новее приложения, Ф10); битый
        12: //! `settings.json`/`profiles.json` — **отказ** (Ф11), битый `chats/<id>.json` — пропуск
        13: //! с `warn`; `< current` — в план. План непуст → **один** бэкап перед любой записью
        14: //! (не удался → миграция не начинается) → прогон шагов + control-parse + атомарная запись.
        16: //! Сегодня все схемы = 1, поэтому план всегда пуст: `run` фактически лишь валидирует
        17: //! (гейты downgrade/битости), а движок миграций покрыт тестами на синтетическом артефакте.
        34: /// Мигрирует данные приложения при старте (реальный реестр схем). Вызывается из
        35: /// `main.rs` перед открытием хранилища (TUI и CLI `import`).
        46: /// Какой типизированной структурой валидировать мигрированное значение перед записью.
        54: /// Файл, требующий миграции (`from → art.current`).
        62: /// Ядро с внедрёнными артефактами (DI для тестов: синтетический артефакт с реальным
        63: /// шагом проверяет бэкап+запись независимо от реального реестра, где все схемы = 1).
        112: // Битый файл чата не валит запуск и не теряется — остаётся на диске (Ф11).
        114: "миграция: пропущен повреждённый файл чата"),
        118: // Координация с SQLite: downgrade-guard + учёт в общем pre-migrate моменте. Саму
        119: // миграцию БД (baseline/шаги) выполняет позже `Db::open`; здесь лишь заглядываем в
        120: // `user_version`, чтобы ОДИН бэкап покрыл и JSON, и БД (ADR 0006). БД в этот момент
        121: // ещё не открыта (quiescent) → бэкап-архив её файлов согласован без SQLite backup API.
        129: tracing::debug!("миграция данных не требуется");
        133: // Один бэкап перед любой записью. fs_root песочницы не включаем (конфиг может сам
        134: // требовать миграции — читать его для fs_root преждевременно; критичные данные
        135: // settings/profiles/chats/db бэкап и так захватывает). Сбой → миграция отменяется.
        144: "создан бэкап перед миграцией данных");
        153: // Control-parse: миграция, после которой файл не парсится, — ошибка; файл не
        154: // перезаписываем (запись идёт только после успешной валидации).
        156: tracing::error!(file = %item.path.display(), error = %e, "миграция дала непарсимый результат");
        167: "мигрирован файл данных");
        173: /// Оценивает один файл: `Ok(None)` — нет файла или актуален; `Ok(Some)` — в план;
        174: /// `Err` — битый (по ключу `corrupt_key`) или downgrade (данные новее приложения).
        203: /// Локализованное сообщение об отказе: данные созданы более новой версией приложения.
        215: /// Валидирует мигрированное значение типизированным парсом (без записи).
        231: /// Все `chats/*.json` (отсортированы для детерминизма). Нет каталога → пусто.
        287: // Ни одного pre-migrate бэкапа: план был пуст.
        306: // settings.json из «более новой» версии приложения (схема 999).
        314: // Данные не тронуты.
        334: // data.db из «более новой» версии приложения (user_version = 999).
        356: // Битый чат пропущен с warn, запуск не падает, миграции нет.
        360: // Синтетический артефакт настроек «current = 2» со ступенью 1→2: проверяет полный
        361: // путь бэкап+запись+control-parse на реальном I/O (реальный реестр — всё v1).
        376: // v1 settings (валидный AppConfig, schema_version=1).
        394: // Файл мигрирован до v2 и остаётся валидным AppConfig.
        399: // Создан ровно pre-migrate бэкап.
   48  src/features/doc_extract.rs
        1: //! Извлечение простого текста из бинарных форматов документов (PDF/DOCX) для
        2: //! индексации в базу знаний (RAG, команда `/rag add`, этап B1b —
        3: //! `docs/history/rag-sources-retrieval.md`). Чистые функции над байтами: диспетчеризация по
        4: //! расширению и само извлечение — обязанность слоя `app` (`orchestrator/rag.rs`);
        5: //! здесь лишь разбор форматов внешними крейтами (без кросс-слойных импортов FSD).
        7: //! Философия «лучшее усилие»: сбой извлечения возвращает `Err` (цикл индексации
        8: //! ловит ошибку файла, логирует `warn` и увеличивает счётчик ошибок — не паникует).
        9: //! Сканированный/картиночный PDF может дать пустой/пробельный текст — это допустимо.
        17: /// Извлекает читаемый текст из DOCX (Office Open XML). DOCX — это ZIP-архив с
        18: /// deflate-сжатием; текст документа лежит в `word/document.xml`. Разбираем его
        19: /// потоковым ридером `quick-xml`, собирая содержимое элементов `<w:t>` (по
        20: /// **локальному** имени, игнорируя префикс `w:`); конец абзаца `</w:p>` → перевод
        21: /// строки, `<w:tab/>` → табуляция, `<w:br/>` → перевод строки. Сущности (`&amp;`
        22: /// и т.п.) декодируются. Хвостовые пробелы срезаются.
        24: /// Отсутствие `word/document.xml`, битый ZIP или некорректный XML → `Err` с
        25: /// контекстом (вызывающий пропускает такой файл).
        28: zip::ZipArchive::new(Cursor::new(bytes)).context("DOCX: не удалось открыть ZIP-архив")?;
        31: .context("DOCX: в архиве нет word/document.xml")?
        33: .context("DOCX: не удалось прочитать word/document.xml")?;
        38: // Текст в DOCX лежит только внутри `<w:t>`; вне его — незначимые пробелы между
        39: // тегами. Собираем содержимое лишь пока открыт `<w:t>`.
        44: .context("DOCX: ошибка разбора XML")?
        46: // Совпадение по **локальному** имени (без префикса `w:`).
        54: b"p" => out.push('\n'), // конец абзаца
        63: // Текст декодируется как UTF-8 (эскейпы quick-xml выдаёт отдельными
        64: // событиями GeneralRef, см. ниже — тут их уже нет).
        65: let decoded = e.decode().context("DOCX: не удалось декодировать текст")?;
        68: // quick-xml 0.39 отдаёт ссылки на сущности (`&amp;`, `&#38;`) отдельным
        69: // событием — в BytesRef лежит имя без `&`/`;`. Восстанавливаем и снимаем
        70: // эскейп (unescape понимает и именованные, и числовые ссылки).
        74: .context("DOCX: не удалось декодировать сущность")?;
        77: .context("DOCX: не удалось снять эскейп сущности")?;
        88: /// Извлекает текст из PDF (крейт `pdf-extract`, качество «лучшее усилие»).
        89: /// Сканированный/картиночный PDF может вернуть пустой/пробельный текст — это
        90: /// допустимо (RAG просто не проиндексирует нечего). Битый PDF → `Err` с контекстом.
        93: .map_err(|e| anyhow::anyhow!("PDF: не удалось извлечь текст: {e}"))
        101: /// Собирает минимальный валидный DOCX в памяти: ZIP c `word/document.xml`,
        102: /// содержащим `abs` абзацев (каждый — `<w:p><w:r><w:t>…</w:t></w:r></w:p>`).
        126: // Кириллица + сущность `&amp;` (должна раскрыться в `&`) + скобки в тексте.
        130: // Оба абзаца присутствуют, сущность раскрыта.
        139: // Абзацы разделены переводом строки.
        144: // XML-теги не протекают в вывод.
        151: // Ручная сборка с <w:tab/> и <w:br/> внутри рана.
        168: // Валидный ZIP, но без word/document.xml → ошибка (не паника).
        179: // Совсем не ZIP → ошибка (не паника).
        184: /// Минимальный валидный PDF с текстом «Hello World» (корректный xref,
        185: /// шрифт Helvetica, поток контента `BT … Tj ET`). Проверено на `pdf-extract`.
        199: // Не PDF → ошибка (не паника).
   47  src/features/tools/introspection.rs
        1: //! Инструменты интроспекции: чтение/изменение семплинга и системного сообщения,
        2: //! время последнего user-сообщения. См. spec §9.3 (`get/set_sampling`,
        5: //! Мутирующие (`set_*`) **не** трогают `Chat`, а возвращают [`ChatEffect`];
        6: //! применяет их оркестратор (spec §4.4.2). Изменения вступают в силу со
        7: //! следующего хода/построения запроса (spec §6.6).
        18: /// Имя инструмента чтения семплинга.
        20: /// Имя инструмента изменения семплинга.
        23: /// `get_sampling` — возвращает действующий семплинг (JSON), ограниченный полями,
        24: /// доступными в текущем режиме движка (`provider`). См. [`supported_sampling_fields`].
        26: /// Облачный провайдер chat-движка (`None` — локальный/external: доступны все поля).
        45: "показать семплинг"
        58: // Показываем только поля, доступные в текущем режиме (остальные движок
        59: // всё равно не принял бы), чтобы модель не пыталась их менять.
        65: /// `set_sampling` — переопределяет семплинг чата (частично; со следующего хода).
        66: /// Доступные поля ограничены текущим режимом движка.
        68: /// Облачный провайдер chat-движка (`None` — локальный/external: доступны все поля).
        87: "изменить семплинг"
        97: // Схема несёт только поля, принимаемые движком текущего режима.
        109: // Отбрасываем поля, недоступные в текущем режиме (движок их не примет),
        110: // и сообщаем об этом модели, а не молча применяем неподдержанное.
        129: // Разбираем частичный конфиг (все поля Option, отсутствующие = None).
        138: // В результат кладём только поля, доступные в текущем режиме — иначе в ленту
        139: // (и модели) уезжает полный конфиг с десятками `null`, сбивая с толку.
        157: /// Подсказка модели о доступном наборе полей в текущем режиме (на языке `loc`).
        168: /// Сериализует семплинг, оставляя только поля, доступные в текущем режиме.
        182: /// JSON-схемы значений всех настраиваемых полей семплинга (имя → схема). Порядок
        183: /// совпадает с [`crate::entities::sampling::SETTABLE_SAMPLING_FIELDS`].
        229: /// `get_system_message` — возвращает текущее системное сообщение чата.
        241: "показать сис. сообщение"
        254: /// `set_system_message` — меняет системное сообщение (со следующего построения запроса).
        266: "изменить сис. сообщение"
        291: /// `get_last_user_message_time` — таймстемп последнего user-сообщения + прошедшее время.
        303: "время посл. сообщения"
        333: /// Сливает частичный патч поверх базового семплинга (заданные поля побеждают).
        373: /// JSON Schema пустого объекта параметров (инструмент без аргументов).
        378: /// Грубое человекочитаемое представление длительности в секундах (на языке `loc`).
        440: assert_eq!(s.temperature, Some(0.9)); // переопределено
        441: assert_eq!(s.max_tokens, Some(512)); // сохранено из базы
        443: // Новые расширения llama.cpp тоже мёржатся.
        467: // Gemini (нативный): top_k доступен, а min_p (расширение llama.cpp) — нет.
        478: // OpenAI: недоступна и temperature (GPT 5.5/5.6 её отвергают).
        493: // Claude: доступен только max_tokens; temperature/top_k должны быть отброшены.
        509: // Об отброшенных полях модель уведомляется.
        512: // Но в JSON результата нет полного дампа конфига с десятками `null`-полей —
        513: // только доступные в режиме (для Claude это max_tokens + reasoning).
        521: // Локально — полная схема (все настраиваемые поля).
        530: // Claude — max_tokens + reasoning (thinking/reasoning_effort), но не расширения.
   46  src/features/spellcheck/dict.rs
        1: //! Загрузка Hunspell-словарей (`spellbook`) из каталога `dictionaries/` и
        2: //! персонального словаря. См. spec §11.5.
        4: //! Каталог сканируется на пары `*.aff` + `*.dic` с общим именем (`en_US.aff` +
        5: //! `en_US.dic`). Каждая успешно загруженная пара — отдельный словарь; битые
        6: //! пропускаются с записью в лог (спелл-чек деградирует, не падает). Загрузка
        7: //! тяжёлая (парсинг `.dic`) — вызывается в фоновом потоке (см. `main.rs`).
        17: /// Загружает словари и персональный словарь из `personal_path` согласно настройкам
        18: /// интерфейса (spec §11.6):
        19: /// - `enabled = false` → словари не грузятся (вернётся отключённый чекер);
        20: /// - `selected` непуст → грузятся только пары с базовым именем из списка
        21: ///   (`en_US`/`ru_RU`/…); пустой `selected` → все найденные.
        23: /// Словари ищутся сперва в `dict_dir` (корень данных), затем в `bundled_dir` —
        24: /// портативной раскладке рядом с бинарником, куда инсталлятор/пакет кладёт словари при
        25: /// не-портативном режиме хранения (П1, §4.2 installers.md). Пара, чьё базовое имя уже
        26: /// загружено из `dict_dir`, из `bundled_dir` **не** перегружается (пользовательский
        27: /// словарь того же имени выигрывает). В портативном режиме `bundled_dir` совпадает с
        28: /// `dict_dir` — второй проход ничего не добавляет (все имена уже загружены).
        30: /// Отсутствие каталога/файлов — не ошибка (отключённый чекер). Загрузка тяжёлая
        31: /// (парсинг `.dic`) — вызывается в фоновом потоке (см. `app/runtime.rs`).
        41: tracing::info!("спелл-чек выключен в настройках — словари не загружаются");
        43: // Уже загруженные базовые имена — чтобы `bundled_dir` не дублировал словарь,
        44: // взятый из `dict_dir` (и не перегружал один и тот же в портативном режиме).
        58: /// Загружает пары `*.aff`/`*.dic` из одного каталога, пропуская базовые имена, уже
        59: /// присутствующие в `loaded` (и добавляя загруженные туда же). Фильтрует по `selected`.
        73: // Фильтр по выбранным словарям (по базовому имени файла).
        78: // Имя уже загружено (из приоритетного каталога) — не перегружаем.
        88: tracing::info!(dict = %aff.display(), "словарь загружен");
        93: tracing::warn!(dict = %aff.display(), error = %err, "словарь пропущен");
        99: tracing::info!(dir = %dir.display(), "каталог словарей отсутствует");
        102: tracing::warn!(dir = %dir.display(), error = %err, "не удалось прочитать каталог словарей");
        107: /// Загружает одну пару `.aff`/`.dic`.
        111: Dictionary::new(&aff_text, &dic_text).map_err(|e| anyhow::anyhow!("разбор словаря: {e}"))
        114: /// Читает персональный словарь (по слову на строку; пустые и `#`-комментарии
        115: /// игнорируются). Отсутствие файла — пустой набор.
        129: /// Дописывает слово в файл персонального словаря (создаёт при необходимости).
        146: /// Крошечный английский словарь для тестов (формат Hunspell).
        179: write(dir.path(), "en.aff", AFF); // нет en.dic
        189: // enabled = false → словари не грузятся, чекер выключен.
        207: // Выбран только en_US → ru_RU не загружается.
        215: assert!(checker.check_word("hello")); // из en_US
        221: // Корень данных (system-режим) пуст, словари лежат рядом с бинарником — берём
        222: // их из bundled-каталога (П1).
        240: // Одноимённый словарь есть и в корне данных, и рядом с бинарником — берётся из
        241: // корня данных (пользовательский приоритетнее вшитого); дубля нет.
        245: write(data.path(), "en.dic", "1\nhello\n"); // только hello
        256: // "world" есть только в bundled-версии — раз выиграл словарь корня, его нет.
   45  src/app/orchestrator/impersonation.rs
        1: //! Имперсонация (`Ctrl+U`, spec §11.8): модель пишет следующее сообщение «за
        2: //! пользователя». Системное сообщение заменяется на имперсонационное, роли
        3: //! user/assistant в истории меняются местами. Фоновая задача стримит реплику.
        22: /// Лимит времени на одну имперсонацию (страховка от зависшей задачи). Щедрый:
        23: /// на медленном локальном `llama-server` одна только обработка промпта может занять
        24: /// ~минуту, плюс генерация на CPU идёт ~5 ток/с — при 120с реплику резало на полуслове
        25: /// (см. лог `srv stop: cancel task` ровно на 120с). По таймауту накопленный текст
        26: /// **не теряется** (отдаётся в поле как при достижении лимита) — см. `spawn_impersonation`.
        30: /// Пишет сообщение от лица пользователя (имперсонация, `Ctrl+U`, spec §11.8):
        31: /// системное сообщение ассистента заменяется на имперсонационное из профиля, а
        32: /// роли user/assistant в истории меняются местами — модель продолжает диалог
        33: /// «за пользователя». Текст стримится в предпросмотр поля ввода. Игнорируется
        34: /// во время генерации/другой имперсонации.
        64: // Модель собеседника подмешивается в промпт имперсонации (агент пишет ЗА
        65: // человека) — но только если профиль включил модель себя (тот же opt-in-гейт,
        66: // что у пассивной инъекции в обычный ход).
        114: /// Отменяет текущую имперсонацию (`Esc` в предпросмотре). Завершение придёт
        115: /// через `imp_done` и эмитит `ImpersonationFinished{Cancelled}`.
        122: /// Завершение фоновой задачи имперсонации: чистит состояние и эмитит финал.
        136: /// Строит запрос имперсонации (spec §11.8): системное сообщение — имперсонационное
        137: /// (персона пользователя), роли user/assistant в истории меняются местами (модель
        138: /// продолжает диалог «за пользователя»). Инструментов нет. Если `seed` не пуст,
        139: /// модель просят продолжить уже начатый текст.
        148: // Имперсонация пишет реплику в поле ввода и **отбрасывает** «мысли» (Thoughts
        149: // в `spawn_impersonation` игнорируются), поэтому reasoning ей не нужен. Ключевое
        150: // — `reasoning_budget=0`: для моделей со «вшитым» в шаблон thinking (Gemma
        151: // `peg-gemma4`, Qwen) только он реально гасит «мысли» (+ `enable_thinking=false`
        152: // в `wire.rs`); поля `thinking`/`reasoning_effort` сервер для них игнорирует.
        153: // Без этого модель тратила весь бюджет токенов на reasoning_content, а ответный
        154: // `content` приходил пустым — предпросмотр оставался пустым (тот же класс бага,
        155: // что у авто-названия чата, см. `title.rs`).
        159: // Подсказка о собеседнике (модель того, за кого пишем) — перед seed-продолжением.
        178: /// Меняет роль сообщения местами для имперсонации (user↔assistant). System/Tool и
        179: /// пустые сообщения отбрасываются (в режиме имперсонации инструментов нет).
        191: /// Запускает фоновую задачу имперсонации: стримит текст реплики в предпросмотр
        192: /// (`ImpersonationChunk`), по завершении/таймауту/отмене шлёт `(id, reason)` в
        193: /// `done_tx`. «Мысли» и tool-вызовы игнорируются (в поле ввода идёт только текст).
        227: // Различаем отмену пользователем и таймаут: при отмене (`Esc`) поток внутри
        228: // `run` ловит `cancel.cancelled()` и сам отдаёт `Finished(Cancelled)` — она
        229: // приходит сюда как `Ok(Ok(Cancelled))` и приводит к отбрасыванию текста
        230: // (пользователь передумал). Таймаут — это `Err(_)`: серверную задачу мы
        231: // прерываем (`cancel.cancel()`), но накопленный текст **сохраняем**, отдавая
        232: // его как `Length` (модель писала валидную реплику, просто медленно). Прежний
        233: // безусловный `if cancel.is_cancelled() { Cancelled }` оба случая сводил к
        234: // отбрасыванию — из-за этого реплика, обрезанная таймаутом, исчезала.
   44  src/app/orchestrator/tool_loop.rs
        1: //! Общий «тихий» agentic-loop фоновых задач (авто-рефлексия «модели себя» и
        2: //! авто-консолидация заметок). Обе задачи — мини agentic-loop без стриминга в UI:
        3: //! стрим → аккумулятор вызовов → исполнение разрешённых инструментов → следующий
        4: //! раунд; толерантны к `Thoughts`/`ThoughtsSignature`/`Usage` (игнор). Раньше это
        5: //! тело дублировалось в `reflection.rs` и `consolidation.rs` дословно (различались
        6: //! лишь лимиты и метка лога) — здесь оно одно. Основную петлю генерации сознательно
        7: //! **не** трогаем: у неё стриминг в UI, control-flow-инструменты, thinking-подписи
        8: //! Anthropic, usage, эффекты — её сложность не окупает общий сток сейчас.
        27: /// Async-надстройка дайджеста фоновой задачи (раздел A2): семантическое сравнение
        28: /// абзацев описания себя (`summary`) с наблюдениями (`@self`). Вычисляется **в
        29: /// задаче** до петли — эмбеддинг абзацев summary недоступен в синхронном хендлере
        30: /// оркестратора. См. docs/history/self-model-consolidation.md §A2.
        38: /// Пора ли запускать периодическую фоновую задачу: фича включена (`every > 0`) и
        39: /// накоплено достаточно ответов. Чистая функция — тестируема. Общая для рефлексии и
        40: /// консолидации.
        45: /// Параметры запуска тихой фоновой задачи.
        51: /// Разрешённые инструменты (защита от вызова чего-то вне набора задачи).
        54: /// Бэкстоп от зацикливания (число раундов).
        56: /// Лимит времени на всю задачу.
        58: /// Метка для диагностических логов («авто-рефлексия»/«авто-консолидация»).
        60: /// Профиль (для логов).
        62: /// Вид задачи — уходит в `done_tx` вместе с исходом (петля разбирает одной веткой).
        64: /// Единый канал исхода: `(вид, Ok(()))` при успехе, `(вид, Err(причина))` при
        65: /// ошибке/таймауте.
        67: /// Опциональная async-надстройка дайджеста, вычисляемая в задаче ДО петли
        68: /// (эмбеддинг абзацев summary недоступен в синхронном хендлере): результат
        69: /// дописывается к первому user-сообщению запроса. См.
        74: /// Запускает тихую фоновую задачу: мини agentic-loop под таймаутом. По завершении
        75: /// шлёт исход в `done_tx` (снять флаг «идёт …» и вести наблюдаемость: серия неудач →
        76: /// одна ошибка в UI). Инструменты пишут напрямую в `Storage`; чат/лента не трогаются.
        95: // A2: async-надстройка дайджеста (семантика summary↔наблюдения) — считаем ДО
        96: // петли и дописываем к первому user-сообщению (эмбеддинг абзацев summary в
        97: // синхронном хендлере недоступен). См. docs/history/self-model-consolidation.md §A2.
        123: tracing::warn!(%profile_id, "{label}: ошибка: {e}");
        128: tracing::warn!(%profile_id, "{label}: превышен лимит времени");
        129: Err("превышен лимит времени".to_string())
        136: /// Тело мини agentic-loop: раунды стрим→вызовы→исполнение до `max_rounds` или
        137: /// первого раунда без вызовов. Разрешает только инструменты из `allowed`.
        166: // Раунд без вызовов или достигнут лимит — задача окончена.
        181: Err(e) => format!("Ошибка инструмента {}: {e}", call.name),
        184: format!("Инструмент {} недоступен.", call.name)
        198: assert!(!due(5, 0)); // выключено
        201: assert!(due(3, 3)); // достигли порога
        202: assert!(due(4, 3)); // и выше
   41  .github/workflows/release.yml
        1: # Релиз: по тегу `vX.Y.Z` собирает бинарники (Windows + Linux glibc 2.35),
        2: # упаковывает архивы + sha256sums.txt и публикует GitHub Release с нотами из
        3: # соответствующего раздела CHANGELOG.md. См. docs/release-engineering.md §3.5.
        5: # Тег ставит пользователь после мержа релизного PR (bump версии + CHANGELOG).
        16: # Сборка бинарника на каждой платформе; упаковка централизована в job release
        17: # (на Linux есть и tar, и zip — избегаем различий шелла Windows).
        19: name: Сборка (${{ matrix.artifact }})
        24: - os: ubuntu-22.04       # старая glibc 2.35 — бинарь работает на большинстве дистрибутивов
        33: - name: Установить тулчейн (из rust-toolchain.toml)
        36: # ALSA нужна только Linux-сборке (rodio/cpal — воспроизведение речи /tts).
        37: - name: Системные зависимости (ALSA)
        40: - name: Сборка релиза
        42: - name: Выгрузить бинарник
        49: # Linux-пакеты из уже собранного бинарника (bin-linux) через nfpm.
        51: name: Linux-пакеты (deb/rpm/archlinux)
        56: - name: Скачать бинарник Linux
        61: - name: Установить nfpm
        67: - name: Собрать пакеты
        69: VERSION="${GITHUB_REF_NAME#v}"   # напр. v0.9.0 → 0.9.0
        81: # Windows-инсталлятор (Inno Setup) из уже собранного бинарника (bin-windows).
        83: name: Windows-инсталлятор (Inno Setup)
        88: - name: Скачать бинарник Windows
        93: - name: Установить Inno Setup
        95: - name: Скомпилировать инсталлятор
        104: if (-not $iscc) { throw "ISCC.exe не найден" }
        113: name: Публикация релиза
        117: contents: write        # нужно gh release create
        120: - name: Скачать бинарники
        124: - name: Собрать архивы + sha256 + ноты
        127: TAG="${GITHUB_REF_NAME}"        # напр. v0.9.0
        131: # Общий набор доков в каждый архив.
        134: # Словари спелл-чека кладём в data/dictionaries/ — портативную раскладку,
        135: # которую приложение читает рядом с бинарником (data/ рядом с exe → spell-чек
        136: # из коробки). См. build.rs / shared/paths.rs.
        157: # Linux-пакеты (deb/rpm/archlinux) и Windows-инсталлятор, собранные
        158: # отдельными job'ами.
        162: # Контрольные суммы (по всем артефактам: архивы + пакеты + инсталлятор).
        165: # Ноты релиза = раздел CHANGELOG для этой версии (без заголовка).
        172: echo "Релиз ${TAG}." > notes.md
        175: echo "Артефакты:"; ls -l dist
        176: - name: Создать GitHub Release
   41  src/features/profiles.rs
        1: //! Операции над профилями ИИ-собеседника: создание, редактирование, валидация
        2: //! имени (чистая логика без I/O). Хранение и каскадное мягкое удаление — в
        3: //! `shared/storage`; UI-секция профилей — на M8. См. spec §10.
        10: /// Максимальная длина имени профиля (в символах). Лишнее обрезается.
        13: /// Нормализует имя профиля: схлопывает пробелы, обрезает края и длину.
        14: /// Возвращает `None`, если после нормализации пусто (операция отклоняется).
        36: /// Создаёт новый профиль с валидным именем. `None`, если имя пустое. Новый профиль
        37: /// получает текущий набор инструментов по умолчанию (через [`reconcile_tools`]).
        45: /// Сверяет инструменты профиля с текущим набором по умолчанию: ранее неизвестные
        46: /// профилю инструменты (новые в приложении) **включаются** и записываются в реестр
        47: /// «известных» (`known_tools`). Инструменты, которые пользователь осознанно выключил
        48: /// (они уже в `known_tools`), повторно НЕ включаются. Возвращает `true`, если профиль
        49: /// изменён (требуется сохранение). См. spec §9.4.
        51: /// Для профиля, у которого `known_tools` пуст (создан до появления этого реестра),
        52: /// «новыми» считаются лишь инструменты, которых нет в `enabled_tools`, — то есть к
        53: /// уже включённому набору добавляются недостающие (новые в приложении), а сам набор
        54: /// фиксируется как известный.
        56: // База «известного» при первом запуске миграции — то, что уже включено
        57: // (старый снимок дефолтов). Это не даёт повторно включить инструмент, который
        58: // был включён ранее, но ничего не ломает, если known_tools уже заполнен.
        65: continue; // профиль уже знал инструмент — уважаем выбор пользователя
        76: /// Набор правок профиля (любое поле — опционально). Применяется к существующему
        77: /// профилю; не затрагивает уже созданные чаты (у них свои копии, spec §10).
        82: /// Системное сообщение режима имперсонации (см. [`Profile::impersonation_system_message`]).
        84: /// `Some(None)` — снять приветствие; `Some(Some(..))` — задать.
        87: /// `Some(None)` — убрать дефолты семплинга профиля.
        90: /// Язык служебного каркаса (ось A, docs/history/i18n.md). Правка разрешена только пока у
        91: /// профиля нет данных — гейт **авторитетно** проверяет оркестратор перед
        92: /// применением (`handle_update_profile`), а UI дополнительно рисует поле
        93: /// заблокированным.
        97: /// Применяет правки к профилю. Возвращает `false`, если имя задано, но пустое
        98: /// (правки не применяются — профиль остаётся прежним).
        100: // Валидируем имя до любых мутаций (атомарность правки).
        196: // Профиль со «старым» снимком дефолтов (без новых инструментов).
        199: // known_tools пуст (создан до реестра) — миграция должна добавить новые.
        202: // Новые безопасные инструменты включены.
        205: // Старые сохранены.
        207: // Все текущие дефолты теперь «известны».
        215: // Пользователь осознанно выключил calculate: его нет в enabled, но он в known.
        233: assert!(reconcile_tools(&mut p)); // первый прогон включает дефолты
        235: assert!(!reconcile_tools(&mut p)); // повтор — без изменений
   40  .github/workflows/packaging.yml
        1: # Валидация упаковки на PR и push в main. Linux: собирает пакеты (deb/rpm/archlinux)
        2: # через nfpm и проверяет установку в контейнерах Ubuntu/Fedora/Arch (смоук: раскладка +
        3: # запуск `--version` под обычным пользователем). Windows: компилирует Inno Setup-скрипт
        4: # инсталлятора (проверка синтаксиса .iss + [Code], без реальной сборки бинарника).
        5: # Реальный публичный релиз собирает и прикладывает артефакты отдельно — release.yml.
        6: # См. docs/history/installers.md §8.
        33: name: Собрать пакеты
        34: runs-on: ubuntu-22.04  # glibc 2.35 — бинарь работает на большинстве дистрибутивов
        37: - name: Установить тулчейн (из rust-toolchain.toml)
        40: # ALSA нужна Linux-сборке (rodio/cpal — воспроизведение речи /tts).
        41: - name: Системные зависимости (ALSA)
        43: - name: Сборка релиза
        45: - name: Установить nfpm
        51: - name: Собрать deb/rpm/archlinux
        65: name: Смоук установки (${{ matrix.image }})
        81: - name: Установить пакет и проверить раскладку
        85: # Установка нужным менеджером по наличию бинарника.
        88: # desktop-file-utils даёт desktop-file-validate — проверка записи меню.
        91: # util-linux даёт `su` (в минимальном fedora:latest его нет) — нужен
        92: # ниже для запуска под обычным пользователем.
        95: # `pacman -U` зависимости из репозиториев НЕ тянет (в отличие от apt/dnf),
        96: # поэтому рантайм-зависимости ставим сами: alsa-lib нужна воспроизведению
        97: # речи (`/tts`) — бинарь линкуется с libasound.
        101: echo "нет поддерживаемого пакетного менеджера"; exit 1
        104: # Раскладка: реальный бинарь + defaults.json + словари в /usr/lib/<pkg>/.
        110: # Симлинк из PATH резолвится в реальный бинарь → рядом виден defaults.json.
        116: # Запись в меню приложений и иконки темы (docs/branding.md §4.2).
        119: # Terminal=true критичен: без него запуск из меню не даст терминала.
        123: # Имя файла иконки обязано совпадать с ключом Icon=.
        129: # Штатный валидатор — если есть в образе (ставить ради него не будем).
        134: echo "desktop-file-validate отсутствует — пропуск"
        137: # Запуск под обычным (не root) пользователем: бинарь исполним, ресурсы
        138: # читаемы. `--version` — peek-фаза, каталогов данных не создаёт.
        145: echo "СМОУК OK: ${{ matrix.image }}"
        148: name: Инсталлятор Windows (компиляция .iss)
        152: - name: Установить Inno Setup
        154: - name: Заглушка бинарника + компиляция инсталлятора
        157: # Реальный бинарник тут не собираем — проверяем только, что .iss (и его
        158: # Pascal-код) компилируется. Заглушка удовлетворяет [Files].
        166: if (-not $iscc) { throw "ISCC.exe не найден" }
   39  build.rs
        1: //! Build-скрипт: словари спелл-чека рядом с бинарником + иконка Windows-`.exe`.
        3: //! **Словари.** В портативном режиме приложение читает данные из подкаталога `data/`
        4: //! рядом с исполняемым файлом (в dev — `target/<profile>/data/`, см. shared/paths.rs),
        5: //! значит словари ожидаются в `data/dictionaries/`. Словари лежат в `dictionaries/`
        6: //! корня проекта (в репозитории; их же упаковывает релизный workflow в
        7: //! `data/dictionaries/`); этот скрипт копирует их в выходной каталог, чтобы
        8: //! `cargo run` сразу видел спелл-чек без ручного копирования. Отсутствие словарей —
        9: //! не ошибка (спелл-чек просто выключится).
        11: //! **Иконка.** Под Windows-таргет в `.exe` вшивается ресурс иконки из
        12: //! `artwork/mindfork.ico` — иначе Проводник, таскбар и Alt+Tab показывают дефолтную
        13: //! иконку. Отсюда же её бесплатно подхватывают ярлыки и `UninstallDisplayIcon`
        14: //! инсталлятора (`packaging/windows/mindfork.iss`). См. docs/branding.md §4.1.
        20: // Перекопировать только при изменении исходных словарей.
        28: return; // словарей нет — ничего не копируем
        32: println!("cargo:warning=не удалось определить каталог сборки для словарей");
        35: // Портативный корень данных = `<profile>/data/` (см. shared/paths.rs).
        38: println!("cargo:warning=не удалось скопировать словари: {err}");
        42: /// Вшивает иконку в Windows-`.exe` (ресурс `IDI_ICON1`) — вариант для Windows-хоста.
        44: /// Двойной гейт неизбежен: `winresource` объявлен в
        45: /// `[target.'cfg(windows)'.build-dependencies]`, а у **build**-зависимостей `cfg`
        46: /// вычисляется по **хосту** (build-скрипт исполняется на нём) — значит на Linux-хосте
        47: /// крейта нет и обращение к нему не скомпилируется. Поэтому `#[cfg(windows)]` по хосту
        48: /// (есть ли крейт) плюс проверка `CARGO_CFG_TARGET_OS` по таргету (нужна ли иконка).
        50: /// Сбой намеренно **не валит сборку**, а уходит в `cargo:warning`: `winresource` на
        51: /// MSVC-таргете зовёт `rc.exe` из Windows SDK, и на машине без SDK приложение всё
        52: /// равно должно собираться — иконка косметическая.
        58: return; // Windows-хост, но таргет другой — ресурс неприменим
        63: "cargo:warning=иконка не найдена, .exe будет без неё: {}",
        71: println!("cargo:warning=не удалось вшить иконку в .exe: {err}");
        75: /// Вариант для не-Windows хоста: `winresource` недоступен (см. выше).
        77: /// Релизный workflow собирает Windows на windows-раннере, так что штатный путь не
        78: /// страдает. Предупреждаем только при кросс-сборке Linux → Windows, чтобы молча
        79: /// не выдать `.exe` без иконки.
        86: "cargo:warning=кросс-сборка под Windows с не-Windows хоста: \
        87: иконка в .exe не вшита (winresource доступен только на Windows-хосте)"
        92: /// Каталог с бинарником (`target/<profile>/`), выведенный из `OUT_DIR`:
        93: /// `…/target/<profile>/build/<crate>-<hash>/out` → на 3 уровня вверх.
        99: /// Копирует обычные файлы из `src` в `dst` (без рекурсии и скрытых файлов).
        106: // пропускаем скрытые (например .gitkeep) и подкаталоги
   39  src/app/orchestrator/title.rs
        1: //! Авто-название чата (spec §11.2): фоновая задача просит модель придумать
        2: //! короткий заголовок по переписке; результат применяется к чату в петле.
        18: /// Потолок токенов ответа при генерации авто-названия. Пытаемся выключить «мысли»
        19: /// (`reasoning_budget=0` + `chat_template_kwargs.enable_thinking=false`), но
        20: /// некоторые модели (вшитый в GGUF thinking, напр. Gemma `peg-gemma4`) их
        21: /// игнорируют и всё равно «рассуждают» сотни токенов перед ответом — поэтому
        22: /// бюджет щедрый, чтобы модель успела завершить «мысли» и выдать заголовок.
        25: /// Лимит времени на генерацию авто-названия чата (с запасом на «думающие» модели).
        28: /// Результат фоновой задачи авто-названия чата (внутренний канал).
        31: /// Сырой текст ответа модели (или сообщение об ошибке для показа в UI).
        36: /// Авто-название чата (spec §11.2): модель читает переписку (или её начало и
        37: /// конец, если она длинная) и придумывает короткий заголовок. Запрос идёт
        38: /// фоновой задачей; результат прилетает в [`Orchestrator::handle_title_result`].
        39: /// Чат-сервер должен быть готов (`Ready`) — иначе понятная ошибка.
        44: // Язык служебного каркаса — из профиля чата (ось A): дайджест и системное
        45: // сообщение авто-названия локализуются им (заголовок всё равно просят «на
        46: // языке переписки», см. `prompt.title.system`). Ошибки — для человека (ось B),
        47: // остаются на русском.
        60: // Авто-название — операция списка чатов: ошибку готовности сервера
        61: // показываем в оверлее списка, а не в ленте чата (где её скрыл бы
        62: // полноэкранный оверлей).
        67: // Свежий компактный семплинг (не наследуем override чата): короткий ответ,
        68: // умеренная температура, reasoning выключен (заголовку «мысли» не нужны и
        69: // только съедают бюджет токенов), без инструментов. Ключевое — `reasoning_
        70: // budget=0`: для моделей со «вшитым» в шаблон thinking (Gemma `peg-gemma4`,
        71: // Qwen) только он реально гасит «мысли»; поля `thinking`/`reasoning_effort`
        72: // сервер для таких шаблонов игнорирует (иначе модель тратила весь бюджет на
        73: // «мысли» и ответный текст приходил пустым).
        97: /// Применяет результат фоновой генерации авто-названия: чистит/нормализует
        98: /// заголовок и переименовывает чат (или показывает ошибку).
        125: /// Запускает фоновую задачу авто-названия чата: один независимый запрос к модели
        126: /// (без истории/инструментов), сбор текста, отправка результата в `title_tx`.
        127: /// Лимит времени — [`TITLE_TIMEOUT`].
        144: // Копим «мысли» как запасной источник: если модель так и не
        145: // «завершила мысль» (выдала только reasoning), вытащим заголовок
        146: // из последней содержательной строки рассуждений.
        168: /// Выбирает сырой источник заголовка: основной ответ модели, а если он пуст
        169: /// (модель не «завершила мысль» в рамках бюджета) — последнюю содержательную
        170: /// строку рассуждений. Финальную нормализацию делает `clean_generated_title`.
   37  src/shared/storage/db/self_model.rs
        1: //! Хранилище (SQLite) — модель себя: get/upsert/атомарный update. Часть модуля [`super`]; разбито и
        2: //! монолита db.rs (см. docs/history/refactoring-god-objects.md, этап 5).
        7: // ---------- модель себя (SelfModel) ----------
        9: /// Модель себя профиля (`None`, если ещё не создавалась). Изоляция по PK.
        15: /// Сохраняет модель себя (INSERT OR REPLACE), повышая `version`. Поле `version`
        16: /// в переданной модели игнорируется — авторитетный счётчик ведёт хранилище.
        18: /// Для правки существующей модели предпочтителен [`Self::self_model_update`]:
        19: /// он делает чтение-правку-запись **атомарно** (под одним захватом мьютекса),
        20: /// исключая гонку «прочитал → кто-то записал → записал поверх» между фоновой
        21: /// авто-рефлексией, ручной правкой `F3` и инструментами хода. Прямой upsert
        22: /// оставлен как симметричный примитив хранилища (get/upsert/update) и
        23: /// используется тестами; прикладные писатели идут через `self_model_update`.
        31: /// Атомарное чтение-правка-запись модели профиля: SELECT + `mutate` + upsert
        32: /// под **одним** захватом мьютекса соединения. `mutate` возвращает `true`, если
        33: /// модель изменилась (иначе запись и рост `version` не делаются). Возвращает
        34: /// модель после правки (с авторитетными `version`/`updated_at`, если записана)
        35: /// и признак записи. Так исключается гонка load-modify-save между параллельными
        36: /// писателями (см. док к [`Self::self_model_upsert`]).
        38: /// `mutate` вызывается под захваченным мьютексом БД — внутри него **нельзя**
        39: /// обращаться к другим методам `Db` того же соединения (реентерабельный `lock()`
        40: /// нереентерабельного `Mutex` = дедлок); это чистая правка значения `SelfModel`.
        56: /// Чтение модели по соединению (без захвата мьютекса — вызывается под ним).
        71: /// Запись модели по соединению (без захвата мьютекса — вызывается под ним).
        72: /// Возвращает фактически сохранённую модель (с повышенным `version` и свежим
        73: /// `updated_at`), чтобы вызывающий не перечитывал БД.
        75: // rusqlite не поддерживает u64 в ToSql/FromSql — версию храним как i64.
        119: // Изначально нет.
        130: assert_eq!(loaded.version, 1); // версия выставлена хранилищем
        132: // Профиль B не видит модель A.
        135: // Повторный upsert повышает версию и заменяет данные.
        147: // Два потока параллельно дописывают инсайты в модель одного профиля. При
        148: // неатомарном read-modify-write часть записей терялась бы (гонка «прочитал →
        149: // другой записал → записал поверх»). `self_model_update` держит SELECT+upsert
        150: // под одним захватом мьютекса — ни одна запись не теряется.
        162: // Накапливаем цели — считаем ровно (add_goal без потолка).
        176: assert_eq!(m.version, (2 * n) as u64); // каждая правка = один upsert
        183: // mutate вернул false → записи и роста версии нет, строки в БД не появилось.
   36  packaging/nfpm.yaml
        1: # nfpm-конфиг для Linux-пакетов: один файл → deb + rpm + archlinux (.pkg.tar.zst)
        2: # через `nfpm package --packager deb|rpm|archlinux`. Раскладка — §4.2
        5: # Ключевое: реальный бинарь кладётся НЕ в /usr/bin (туда нельзя класть соседние
        6: # данные — FHS/Debian Policy), а в /usr/lib/mindfork-rs/ рядом с defaults.json и
        7: # словарями; из /usr/bin туда ведёт симлинк. `std::env::current_exe()` на Linux
        8: # резолвит симлинк в реальный путь (/proc/self/exe), поэтому приложение находит
        9: # соседние ресурсы — правок кода не требуется. Словари при mode=system в каталоге
        10: # данных отсутствуют → берутся из /usr/lib/mindfork-rs/data/dictionaries (fallback П1).
        12: # Версию и путь к бинарнику подставляет packaging/linux/build-packages.sh через env.
        29: # Зависимости заданы вручную — nfpm их не вычисляет. Стек: rustls (без OpenSSL) +
        30: # bundled SQLite + x11rb (буфер обмена) → из системного остаётся glibc и ALSA
        31: # (libasound — воспроизведение речи `/tts`; на PipeWire/PulseAudio работает через
        32: # штатный pipewire-alsa). Без звука приложение не падает: команда `/tts` покажет
        33: # «звук недоступен» (docs/research/tts.md §6).
        38: # Альтернатива обязательна, а не косметика: после time_t64-перехода
        39: # (Ubuntu 24.04+/Debian trixie) настоящая ALSA называется `libasound2t64`, а
        40: # старое имя `libasound2` осталось лишь **виртуальным** — и apt удовлетворяет
        41: # его OSS-шимом `liboss4-salsa-asound2`, в котором нет части символов
        42: # (`snd_device_name_get_hint`), т.е. приложение падает при запуске. Поймано
        43: # контейнерным смоуком packaging.yml на ubuntu:24.04. Первая альтернатива
        44: # предпочитается, на старых системах (bookworm) остаётся вторая.
        52: # glibc в rpm/archlinux — часть базовой системы, явно не указываем.
        55: # Реальный бинарь (сайдкар в /usr/lib/<pkg>/, не в /usr/bin).
        61: # Вендорское умолчание установки: данные — в стандартной ОС-папке пользователя
        62: # (~/.local/share/mindfork-rs). config|noreplace → правка пользователя переживёт
        63: # апгрейд (deb-conffile / rpm %config(noreplace) / pacman backup).
        70: # Словари спелл-чека (портативная раскладка рядом с бинарём — резерв П1).
        80: # Пользовательская точка входа: симлинк из PATH на реальный бинарь.
        85: # Запись в меню приложений. Terminal=true обязателен — приложение TUI
        86: # (см. docs/branding.md §4.2). Exec ссылается на симлинк в PATH.
        92: # Иконки темы hicolor. Имя файла обязано совпадать с ключом Icon= в .desktop
        93: # (mindfork-rs), поэтому исходники переименовываются при установке.
        94: # Скриптлеты обновления кэша не добавляем: и Debian (триггер update-icon-caches),
        95: # и Fedora (file trigger на /usr/share/icons/hicolor) делают это сами.
        120: # Векторный вариант — с подложкой, как у растровых (единый вид в меню).
        126: # Документация.
   36  src/shared/storage/db/graph.rs
        1: //! Хранилище (SQLite) — граф связей заметок + замещение + цитирование источников. Часть модуля [`su
        2: //! монолита db.rs (см. docs/history/refactoring-god-objects.md, этап 5).
        7: /// Создаёт направленную связь between двумя заметками (идемпотентно по PK).
        8: /// Изоляция по `profile_id`. Возвращает `true`, если связь действительно создана
        9: /// (`false` — такая связь уже была, `INSERT OR IGNORE` ничего не вставил).
        32: /// Число связей, в которых участвует заметка (в любую сторону). Для предупреждения
        33: /// при ревизии смыслонесущего узла (его рёбра могут стать неверными).
        45: /// Соседи заметки по графу (в обе стороны), исключая замещённые. Опциональный
        46: /// фильтр по типу связи. Возвращает (заметка, тип связи, исходящая ли связь).
        89: /// Помечает заметку замещённой другой (скрывается из активной выдачи, хранится
        90: /// ради «шрама»/трассировки). Идемпотентно (перезапись записи о замещении).
        109: /// Связывает заметку с RAG-источником (по имени источника — стабильно к
        110: /// переиндексации, в отличие от id чанков). Идемпотентно по PK. Возвращает
        111: /// `true`, если связь действительно создана. Изоляция по `profile_id`.
        132: /// RAG-источники, на которые ссылается заметка (для показа при припоминании).
        133: /// Изоляция по `profile_id`.
        146: /// Активные (не замещённые) заметки профиля, ссылающиеся на данный RAG-источник —
        147: /// обратное направление (поиск заметок через RAG, «оба органа»). Изоляция по
        163: /// Все связи профиля `(from, to, relation)` — для обзора консолидации.
        180: /// Переносит связи замещённой заметки на новую (при merge): рёбра, где участвует
        181: /// `old_id`, перенаправляются на `new_id`; самопетли и дубли отбрасываются.
        200: // Удаляем старое ребро, затем вставляем перенацеленное (OR IGNORE от дублей).
        208: continue; // самопетля после переноса — отбрасываем
        240: // Замещённая скрыта и из списка, и из семантики, и из is_active.
        263: // Повтор той же связи не создаётся (false) — дубля в таблице нет.
        267: assert_eq!(nb.len(), 2); // исходящая на n2 + входящая от n3 (дубль не учтён)
        277: // Фильтр по типу связи.
        282: // Замещённый сосед исчезает из выдачи.
        301: // s1→x и s2→x (после переноса станут дублем); s1→s2 (станет самопетлёй).
        310: assert_eq!(all.len(), 1); // дубль схлопнут, самопетля отброшена
        328: // Существование источника (изоляция по профилю).
        333: // Связь идемпотентна.
        337: // Прямое направление: источники заметки.
        342: // Обратное направление: заметки источника (+ изоляция).
        352: // Удаление заметки снимает её ссылки на источники.
        368: // Замещённая заметка не всплывает в обратном пути.
   34  packaging/windows/mindfork.iss
        1: ﻿; Inno Setup-скрипт установщика mindfork-rs для Windows (§3.3 docs/history/installers.md).
        2: ; Две кастомные страницы мастера — «Язык приложения» и «Расположение данных» — и запись
        3: ; выбора в defaults.json рядом с бинарником. Двуязычный UI (ru/en), per-user установка
        4: ; без UAC. Скрипт совместим с Inno Setup 6.7.x.
        6: ; Версия и каталог с бинарником передаются компилятору через /D:
        8: ; Дефолты ниже позволяют скомпилировать скрипт локально/в CI для проверки синтаксиса.
        14: ; Локальный дефолт: релизная сборка из корня репозитория. SourcePath уже несёт
        15: ; завершающий "\", поэтому склеиваем без ведущего слэша.
        29: ; Per-user по умолчанию (без UAC); пользователь может выбрать «для всех» на старте.
        41: ; Брендинг (docs/branding.md §4.1). Иконка самого setup.exe и логотип в шапке
        42: ; мастера. PNG проверен на Inno 6 — принимается наравне с BMP и в 35 раз легче.
        43: ; Иконку установленного .exe вшивает build.rs (winresource), поэтому ярлыки
        44: ; [Icons] и UninstallDisplayIcon получают её без дополнительных настроек.
        53: ; Страница «Язык приложения».
        60: ; Страница «Расположение данных».
        73: ; Страница выбора каталога.
        83: ; Словари спелл-чека — в портативную раскладку рядом с бинарём (резерв П1): при
        84: ; mode=system их в каталоге данных нет, приложение берёт отсюда.
        99: ; defaults.json пишется кодом (см. [Code]); при удалении его тоже убираем. Данные
        100: ; пользователя (чаты/профили в %APPDATA% или портативные) деинсталлятор не трогает.
        113: { Страница выбора языка приложения (радиокнопки). }
        117: LangPage.Add('Русский');
        124: { Страница выбора расположения данных (радиокнопки). Портативный вариант — только
        125: для per-user установки: в Program Files (per-machine) данные рядом с exe писать
        126: нельзя. Индексы вычисляем, т.к. набор опций зависит от режима. }
        147: { Каталог для варианта «Другая папка…». Показывается условно (ShouldSkipPage). }
        158: { Апгрейд: если в целевом каталоге уже есть defaults.json — параметры заданы прошлой
        159: установкой, не спрашиваем снова. Берём путь через WizardDirValue (текущее значение
        160: поля каталога), а НЕ через раскрытие константы app: она на этапе показа страниц
        161: мастера ещё не инициализирована, и ExpandConstant по ней падает Runtime error. }
        168: { Каталог нужен только при выборе «Другая папка…». }
        175: { Порядок важен: сперва обратный слэш, затем кавычка (иначе слэши из \" удвоятся). }
        186: { UTF-8 (с BOM — приложение его отбрасывает, П3): корректно для кириллических путей. }
        197: { Не перезаписываем при апгрейде — выбор пользователя сохраняется. }
   34  src/app/orchestrator/tests/self_model.rs
        1: //! Тесты оркестратора — модель себя: инъекция, F3-правки, сигналы. Часть модуля [`super`]
        2: //! (фикстуры в mod.rs). См. docs/history/refactoring-god-objects.md, этап 3.
        21: // Выключено → система не меняется (протокол тоже не подмешивается).
        35: // Включено, протокол выкл, непустая модель → блок дописывается, протокола нет.
        50: // Включено, протокол выкл, модели нет, наблюдений нет → без изменений.
        64: // Модели нет, но есть наблюдения (self-заметки) → инъекция всё равно происходит.
        79: // Пустая модель + пустые наблюдения, system=None → нечего подмешивать → None.
        94: // Пустой system + непустая модель (протокол выкл) → блок становится системой.
        108: // Протокол вкл + пустая модель → протокол всё равно подмешивается (bootstrap).
        122: // Протокол вкл + непустая модель → и рендер, и протокол.
        140: // Этап 2: при включённом протоколе и раздутом описании (сверх ориентира) в
        141: // инъекцию дописывается data-aware подсказка сократить; при выключенном протоколе
        142: // подсказки нет даже при раздутом описании.
        147: // Ориентир 5 санитизируется до пола 200 — описание берём длиннее 200 символов.
        156: // Протокол вкл → подсказка присутствует.
        169: // Протокол выкл → протокола и подсказки нет (только рендер модели).
        195: // n=3: 2 релевантных + гарантированное самое свежее (f1 не влезает).
        206: // n=2 при 2 релевантных: последнюю релевантную теснит самое свежее.
        214: // Дедуп: если самое свежее уже среди релевантных — не дублируется.
        221: // Ярус 2: инъекция по релевантности поднимает СТАРОЕ, но релевантное запросу
        222: // наблюдение — то, что чистая свежесть потеряла бы.
        234: // X — старое (10 дней назад), тема «xxxx». Затем 4 свежих Y (тема «yyyy»),
        235: // вытесняющих X из свежести (narrative_in_prompt=3).
        266: // Запрос про «xxxx» → старое релевантное наблюдение поднято (хоть не свежайшее).
        272: // Запрос про «yyyy» → нерелевантное старое X не поднимается.
        275: // Инъекция выключена → пусто.
        292: // Правка из UI-редактора (без модели — оркестратор создаёт её на месте).
        299: // Переэмит снимка отражает правку.
        313: // Персистентность: запись видна после перезапуска.
        325: // Наблюдение — self-заметка (@self).
        330: // F3 «удалить наблюдение» → удаление self-заметки (не правка блоба).
        346: // Наблюдение-заметка + непустой блоб модели.
        364: // Self-заметки снесены, блоб очищен.
        391: // Ответ ассистента с вызовом self-model-инструмента → SelfModelChanged.
   32  src/app/orchestrator/tests/mcp.rs
        1: //! Тесты интеграции MCP-хоста в оркестратор: событие `Ready` даёт обёртки в
        2: //! реестре и динамический каталог в снимке `Settings`; крах сервера убирает
        3: //! инструменты. Протокол — в `shared/mcp.rs`, менеджер — в `orchestrator/mcp.rs`.
        6: // Тест-подмодуль `tests::mcp` затеняет сорс-модуль `orchestrator::mcp` — вверх
        7: // до оркестратора (паттерн tests/self_model.rs).
        12: /// Соединение поверх duplex (транспорт в этих тестах не дёргается).
        24: // Несуществующий бинарь: реальная фоновая задача пришлёт Failed в
        25: // канал менеджера (в тестах он отброшен) — события подаём вручную.
        58: // Обёртка в реестре (ход сможет её вызвать), стандартные инструменты целы.
        61: // TOFU: первый подъём авто-пиннит каталог, пин персистится в settings.json.
        66: // Снимок Settings несёт динамический каталог и статус сервера.
        85: // Пин «прежнего» каталога (другое описание) — Ready с изменившимся не пройдёт.
        96: // Инструменты придержаны: в реестр не попали, снимок помечает pending.
        107: // Подтверждение (Enter в настройках → команда): инструменты в реестре,
        108: // новый пин персистится.
        120: // Снимок конфига из UI может не нести пины (устаревшая копия) — правка
        121: // настроек не должна сбрасывать доверие и провоцировать рестарт серверов.
        126: ui_copy.mcp.servers[0].pinned_catalog = None; // устаревший снимок без пина
        135: /// Живой e2e-смоук этапа 3a (docs/research/plugin-system.md §7): полный путь через
        136: /// оркестратор — конфиг с реальным `npx @modelcontextprotocol/server-filesystem`,
        137: /// профиль включает MCP-инструменты, живая модель читает файл его инструментом и
        138: /// использует содержимое в ответе. Требует `MINDFORK_ENGINE_URL` + `npx` в PATH.
        145: // Файл-секрет в каталоге, разрешённом файловому MCP-серверу.
        154: // npx на Windows — .cmd-шим: запуск через `cmd /c` (питфолл §4.6).
        193: // ProfileList эмитится при bootstrap — забираем ДО ожидания MCP-каталога:
        194: // `wait_for` дренирует все события по пути, и более раннее ProfileList
        195: // было бы поглощено ожиданием более позднего Settings (тест бы завис).
        204: // Ждём готовности сервера: снимок Settings с непустым MCP-каталогом
        205: // (npx качает пакет при первом запуске — таймаут щедрый).
        222: // Профиль включает MCP-инструменты (двойной opt-in: мастер-гейт уже вкл).
        235: // Ход: модель должна прочитать файл MCP-инструментом и назвать число.
        265: // Крах процесса: соединение мертво — обёртка уходит из реестра немедленно.
   32  src/features/import.rs
        64: // ---------- wire-типы формата (частичные; неизвестные поля игнорируются) ----------
        83: /// Явный UUID вместо детерминированного (непрерывность с ранее
        84: /// импортированными данными — см. docs/import-format.md §Идентификаторы).
        86: /// Язык служебного каркаса (`ru`/`en`/код внешней локали, ось A).
        108: /// Явный UUID вместо детерминированного (как у профиля).
        114: /// Явное системное сообщение; нет/пусто → первое system-сообщение из `messages`.
        144: // ---------- разбор (чистые функции, тестируемы на фикстурах) ----------
        146: /// Разбирает файл формата mindfork-import (содержимое строкой). Валидация:
        147: /// `format`/`version`, непустые уникальные ключи, ссылки чатов на профили этого
        148: /// же файла, известные роли сообщений. Ведущий BOM отбрасывается.
        179: // Профили: непустые уникальные ключи → детерминированные id.
        199: // Чаты: непустые уникальные ключи + ссылка на профиль ЭТОГО файла.
        237: /// Читает и разбирает файл формата mindfork-import с диска.
        244: // ---------- маппинг в доменные сущности ----------
        246: /// Профиль формата → профиль mindfork. id детерминирован по `key` (или явный).
        259: // Имперсонация форматом v1 не переносится — общий дефолт.
        263: // Импортированные профили получают стандартный набор инструментов;
        264: // реестр известных = тот же набор (новые добавит reconcile_tools,
        265: // не переоткрывая выключенные). См. spec §9.4.
        273: /// Чат формата → чат mindfork, привязанный к `profile_id`. Системное сообщение:
        274: /// явное поле приоритетно, иначе первое system-сообщение истории; system-сообщения
        275: /// в историю не попадают (в mindfork системное хранится отдельно).
        357: /// Временные метки — RFC 3339; нечитаемые не считаются ошибкой (см. формат §Семантика).
        374: /// Референсная локаль для тестов (ru — байт-в-байт с бандлом).
        379: /// Golden-фикстура формата v1 (зеркало docs/import-format.md; неизвестные
        380: /// поля намеренно присутствуют — терпимость к расширению).
        428: // Семплинг: известные поля перенесены, неизвестные игнорированы.
        437: // system из истории (явного поля нет); в сообщения не попал.
        449: // Настройки: частичный перенос (только заданное).
        459: // Одинаковый key у профиля и чата → РАЗНЫЕ id (раздельные namespace);
        460: // повторный разбор → те же id (идемпотентность).
        562: // Имя без префикса ключей бандла: гейт i18n сканирует дотированные литералы.
   32  src/shared/tts/openai.rs
        1: //! Клиент озвучивания по протоколу OpenAI `POST /v1/audio/speech`. Обслуживает
        2: //! **два** режима: облако OpenAI и любой сторонний OpenAI-совместимый TTS-сервер
        3: //! (Kokoro-FastAPI, speaches, LocalAI, …) — различаются base URL, ключом и
        4: //! запрашиваемым форматом ответа. См. docs/research/tts.md §3.1, §3.4.
        12: /// Потолок длины `input` у OpenAI (символов) — жёсткий лимит API.
        14: /// Консервативный потолок для стороннего сервера: лимиты у всех свои и обычно не
        15: /// документированы, а короткий чанк ещё и быстрее даёт первый звук.
        18: /// Частота дискретизации сырого PCM у OpenAI (`response_format:"pcm"`): 24 кГц,
        19: /// s16le, mono — задокументировано, в ответе не сообщается.
        22: /// Клиент `/v1/audio/speech`.
        25: /// Базовый URL с суффиксом `/v1`.
        30: /// Указания по тону/языку/скорости (только облако OpenAI).
        33: /// Запрашиваемый формат ответа: `pcm` у облака (мимо декодера) / `wav` у
        34: /// стороннего сервера (самый переносимый — у части серверов единственный).
        40: /// Облако OpenAI: ключ обязателен, просим сырой PCM.
        62: /// Сторонний OpenAI-совместимый сервер: всё, кроме URL, опционально; просим
        63: /// `wav`. `instructions` не шлём — их поддерживает только облако OpenAI.
        84: /// Тело запроса. Незаданное не отправляется (`skip_serializing_if`): у сторонних
        85: /// серверов набор поддержанных полей разный, лишнее поле может быть отвергнуто.
        93: // Скорость шлём только когда она отличается от обычной. У
        94: // `gpt-4o-mini-tts` поле де-факто игнорируется (известный дефект) —
        95: // там скорость просят словами в `instructions`.
        125: _ = cancel.cancelled() => anyhow::bail!("озвучивание отменено"),
        133: _ = cancel.cancelled() => anyhow::bail!("озвучивание отменено"),
        134: b = resp.bytes() => b.context("чтение аудио-ответа TTS")?,
        176: // Обычная скорость не отправляется вовсе.
        187: // Незаданные поля не отправляются: у сторонних серверов свои наборы.
        194: // Хвостовой слэш URL снят (иначе получилось бы `//audio/speech`).
        199: /// Живой смоук облака OpenAI: синтез короткой фразы (без воспроизведения).
        200: /// Требует `MINDFORK_OPENAI_KEY`; без неё тихо пропускается.
        244: /// Живой смоук стороннего OpenAI-совместимого сервера (Kokoro-FastAPI, speaches,
        245: /// LocalAI…). Требует `MINDFORK_TTS_URL` (например `http://127.0.0.1:8880/v1`).
   31  src/app/orchestrator/consolidation.rs
        1: //! Авто-консолидация заметок («сон», Ярус 3): каждые N ответов ассистента в чате
        2: //! фоновая задача просит модель пересмотреть базу знаний и **самой** её
        3: //! консолидировать — слить дубли, переписать/заместить устаревшее, связать
        4: //! родственное. Как и авто-рефлексия, это **мини agentic-loop**: модель вызывает
        5: //! note-инструменты, петля их исполняет (пишут напрямую в `Storage`). Чат не
        6: //! мутируется, в UI ничего не стримится — консолидация молчалива и опциональна
        7: //! (`config.notes.auto_consolidate_every`, по умолчанию выкл).
        8: //! См. docs/history/notes-connectivity.md (Ярус 3).
        24: /// Потолок токенов ответа на раунд консолидации (с запасом на «мысли» перед вызовом).
        26: /// Лимит раундов мини agentic-loop консолидации (бэкстоп от зацикливания).
        28: /// Лимит времени на всю консолидацию.
        31: /// Инструменты, доступные консолидации (пересекаются с набором профиля).
        42: /// Вызывается после успешной генерации (`handle_done`): считает ответы ассистента
        43: /// и при достижении порога запускает фоновую консолидацию. Тихо ничего не делает,
        44: /// если фича выключена, профиль не включил инструменты заметок, консолидация уже
        45: /// идёт, активных заметок меньше двух или сервер не готов.
        53: let lang; // язык служебного каркаса профиля (ось A)
        66: // Гейт: профиль включает консолидацию (note_merge — ядро операции).
        83: // Счётчик ответов с прошлой консолидации: инкремент; если порог не достигнут —
        84: // выходим (счётчик копится дальше). Сброс — только при фактическом спавне
        85: // (ниже), чтобы пропуск по гейту не терял накопленный цикл.
        94: return; // уже идёт — пропускаем без сброса (повторим на след. ходу)
        96: // Нечего консолидировать, если пользовательских заметок меньше двух (self-заметки
        97: // не в счёт — консолидация над ними не работает). Счётчик не сброшен — повторим.
        109: // Сервер готов? Иначе тихо пропускаем (счётчик не сброшен).
        113: // Все гейты пройдены — сбрасываем счётчик и запускаем.
        121: // Токен отмены — до контекста: его клон едет в `ToolContext.cancel`.
        154: // Спавним задачу и фиксируем слот (флаг «идёт» + тихий индикатор в статус-баре).
        164: label: "авто-консолидация",
        168: // Консолидация заметок — про пользовательские заметки, не про summary модели
        169: // себя; семантика summary↔наблюдения к ней неприменима.
   31  src/app/orchestrator/generation.rs
        55: .send(AppEvent::Error("Нет активного чата".into()));
        510: "Достигнут лимит раундов инструментов ({max_rounds}) — свожу итог из собранного."
        513: // Счётчик токенов финального раунда `stream_round` эмитит сам
        514: // (от `base = total_*`); дальше `break`, накапливать не нужно.
        860: /// Собирает наблюдения (self-заметки) для инъекции в системный промпт (Ярус 2):
        861: /// **релевантные** последней реплике + гарантия свежайшего наблюдения, с откатом на
        862: /// чистую свежесть при недоступном эмбеддере/пустом запросе. Пусто, если инъекция
        863: /// выключена. Async (эмбеддинг запроса) — потому и вынесено из sync-обработчика в
        864: /// задачу генерации. Тестируется поверх temp-хранилища + `MockEmbedder`.
        879: // Релевантные последней реплике наблюдения; пусто → откат на свежесть.
        896: /// Подмешивает «модель себя» в системный промпт хода (SelfModel MVP, см.
        897: /// docs/history/self-model-mvp.md): компактный рендер текущей модели (если непуста) плюс,
        898: /// при `maintenance_protocol`, нейтральный к персоне протокол ведения. Возвращает
        899: /// прежний `system` без изменений, если инъекция выключена (профиль не включил
        900: /// `get_self_model`) либо подмешивать нечего (пустая модель и протокол выключен).
        901: /// Протокол подмешивается даже при пустой модели — чтобы модель начала её вести.
        902: /// Чистая функция — тестируема без движка.
        917: // Наблюдения (self-заметки) могут существовать без блоба модели — тогда рендерим
        918: // пустую модель с наблюдениями. `render_for_prompt` вернёт None, если пусто и
        919: // структурно, и по наблюдениям.
        935: // Собираем подмешиваемые части: рендер модели (если есть) + протокол (если включён).
        941: // Протокол ведения собирается из единого POLICY_CORE (этап 6) — те же
        942: // правила, что у фоновой авто-рефлексии.
        946: // Data-aware приписка: если описание себя разрослось сверх ориентира —
        947: // конкретная подсказка сократить (статичный протокол становится предметным,
        948: // когда summary действительно раздут). См. docs/summary-as-snapshot.md (этап 2).
        954: return system; // подмешивать нечего
        963: /// Доменное tool-сообщение (роль `Tool`) с привязкой к вызову.
        971: /// Финальное assistant-сообщение хода (если есть текст/мысли) со снимком
        972: /// метаданных: режим движка, имя модели и семплинг, **урезанный до полей,
        973: /// доступных в этом режиме** (движок недоступное поле не принял бы — spec §8.3).
   29  src/app/orchestrator/background.rs
        1: //! Реестр слотов «тихих» фоновых задач (авто-рефлексия «модели себя» и
        2: //! авто-консолидация заметок). Обе задачи — мини agentic-loop без UI (общий раннер
        3: //! [`tool_loop::spawn_silent_loop`](super::tool_loop)); их жизненный цикл (флаг
        4: //! «идёт», серия неудач, гашение индикатора, оповещение об ошибке) раньше
        5: //! дублировался полями и обработчиками на каждую задачу. Здесь он один — ключ
        6: //! реестра — существующий [`BackgroundKind`]. Добавление задачи №3 (авто-консолидация
        7: //! «модели себя», roadmap — architecture.md §9.9) не трогает каркас `run()`/`Quit`.
        8: //! См. docs/history/refactoring-solid.md §4.
        17: /// Слот тихой фоновой задачи: токен активного запуска + серия неудач. Серия живёт
        18: /// дольше запуска (переживает завершения) — потому слот, а не отдельная задача.
        21: /// `Some` — задача идёт (одна за раз); токен отмены (для ветки `Quit`).
        23: /// Число подряд идущих неудач; на пороге [`BACKGROUND_FAILURE_ALERT`](super::BACKGROUND_FAILURE_ALE
        24: /// один раз показываем ошибку в UI, дальше молчим до первого успеха.
        29: /// Идёт ли фоновая задача этого вида (гейт «одна за раз»).
        34: /// Фиксирует запуск: слот помечается активным (`cancel = Some`) и в статус-бар
        35: /// уходит тихий индикатор «идёт …». Зовётся спавн-хвостами
        44: /// Общий обработчик исхода фоновой задачи (бывшие `handle_reflect_done`/
        45: /// `handle_consolidate_done`): снимает флаг «идёт», гасит индикатор, ведёт серию
        46: /// неудач (на пороге — одна ошибка в UI, наблюдаемость без спама). При успехе
        47: /// **рефлексии** или **консолидации «модели себя»** дополнительно шлёт
        48: /// `SelfModelChanged` (открытый экран `F3` перезапросит свежий снимок);
        49: /// консолидация *заметок* — нет (меняет заметки, не «модель себя»). Инструменты
        50: /// задачи уже записали изменения в `Storage`; чат/ленту это не трогает.
        52: // Мутируем слот и вычисляем, нужна ли ошибка-оповещение, ДО отправки событий
        53: // (заём `self.bg` не пересекается с `self.evt_tx` при отправке ниже).
        89: /// Отменяет все идущие фоновые задачи семейства (ветка `Quit`).
        98: /// Число подряд идущих неудач задачи (для тестов оповещения об ошибке).
        105: /// Человекочитаемая метка вида задачи (язык интерфейса, ось B) — из неё собираются
        106: /// тексты ошибок (**байт-в-байт** с прежними русскими формулировками).
   29  src/features/rag_command.rs
        1: //! Разбор slash-команд RAG в поле ввода (`/rag add <путь> [-r]`). Чистая,
        2: //! тестируемая логика: экран чата вызывает её при отправке; распознанная команда
        3: //! превращается в намерение, а нераспознанная строка уходит обычным сообщением.
        4: //! См. spec §9.3 (RAG).
        6: /// Распознанная команда RAG.
        9: /// Индексировать файл или директорию в базу знаний.
        11: /// Удалить из базы файл или директорию (со всем, что под ней).
        13: /// Показать источники в базе знаний активного профиля (счётчик чанков, дата).
        15: /// Реиндексировать базу знаний активного профиля (после смены размера чанка/
        16: /// перекрытия или embedding-модели).
        20: /// Краткая подсказка по синтаксису (показывается при ошибке разбора).
        21: pub const USAGE: &str = "использование: /rag add|remove <путь> [-r] · /rag list · /rag rebuild";
        23: /// Пытается разобрать строку ввода как команду RAG.
        25: /// - `None` — строка не является командой RAG (начинается не с `/rag`): её следует
        26: ///   отправить как обычное сообщение.
        27: /// - `Some(Ok(cmd))` — корректная команда.
        28: /// - `Some(Err(msg))` — это команда RAG, но с ошибкой синтаксиса (подсказка `msg`).
        36: return Some(Err(format!("укажите подкоманду. {USAGE}")));
        47: // Подкоманда удаления — только `remove` (слово `delete` намеренно не
        48: // поддерживается: пользователи опасались, что оно сотрёт сам файл на диске).
        49: // Для удаления `-r` не нужен (директория сносится со всем содержимым), но
        50: // принимаем и игнорируем его, чтобы синтаксис был симметричен `add`.
        61: Some(Err(format!("неизвестная подкоманда «{sub}». {USAGE}")))
        65: /// Извлекает путь и флаг рекурсии из токенов после подкоманды. Путь может содержать
        66: /// пробелы (собираем не-флаговые токены), снимаем обрамляющие кавычки.
        79: return Err(format!("укажите путь. {USAGE}"));
        143: // `-r` для remove принимается и игнорируется (директория и так рекурсивна).
        149: // Слово `delete` намеренно не поддерживается — это неизвестная подкоманда.
        161: // Лишние токены после list/rebuild игнорируются (пути им не нужны).
   28  src/features/rename_chat.rs
        1: //! Переименование чата: нормализация заголовка (чистая логика) и сборка
        2: //! пересказа переписки для авто-названия моделью. См. spec §11.2.
        7: /// Максимальная длина заголовка чата (в символах). Лишнее обрезается.
        10: /// Бюджет символов переписки, передаваемый модели для придумывания заголовка.
        11: /// Если переписка длиннее — берём начало и конец (тема обычно задаётся в начале,
        12: /// но может смещаться к концу разговора). См. spec §11.2.
        15: /// Системное сообщение модели для авто-названия чата на языке служебного каркаса
        16: /// (`loc`, ось A). Заголовок всё равно просят «на языке переписки» — поэтому язык
        17: /// каркаса не навязывает язык заголовка (docs/history/i18n.md, развилка 6).
        22: /// Собирает компактный пересказ переписки для запроса авто-названия: помечает
        23: /// роли (на языке каркаса `loc`), пропускает системные/инструментальные и пустые
        24: /// сообщения. Если суммарный объём превышает [`TITLE_CONTEXT_BUDGET`], берёт начало
        25: /// и конец (середина выкидывается). Возвращает `None`, если содержательных
        26: /// сообщений нет.
        46: /// Ограничивает строку `budget` символами (по Unicode-символам, не байтам): если
        47: /// длиннее — берёт начало (≈⅔ бюджета) и конец, заменяя середину маркером.
        60: /// Чистит сгенерированный моделью заголовок: берёт первую непустую строку, срезает
        61: /// обрамляющие кавычки/звёздочки/бэктики и нормализует через [`sanitize_title`].
        62: /// Возвращает `None`, если после очистки строка пуста.
        71: /// Нормализует пользовательский ввод заголовка: убирает крайние пробелы,
        72: /// схлопывает внутренние переводы строк/табы в пробел и ограничивает длину.
        73: /// Возвращает `None`, если после нормализации строка пуста (переименование
        74: /// отклоняется — старый заголовок сохраняется).
        84: // Схлопываем повторяющиеся пробелы для опрятного заголовка.
        102: /// Референсная локаль (ru) — ассерты на русские роли пинят ru-бандл.
        145: /// Per-language (§3.5): роли в дайджесте — из бандла активного языка (en-профиль
        146: /// получает «User:/Assistant:», а не русские роли).
        169: // Длинное сообщение пользователя — должно усечься с маркером середины.
   27  src/app/orchestrator/tests/settings.rs
        1: //! Тесты оркестратора — настройки: снимок, апдейты, дебаунс рестарта. Часть модуля [`super`]
        2: //! (фикстуры в mod.rs). См. docs/history/refactoring-god-objects.md, этап 3.
        50: // Конфиг сохранён на диск.
        86: /// Смена модели перезапускает chat-сервер (spec §11.6 DoD), но с дебаунсом:
        87: /// серия быстрых правок полей движка коалесится в **один** рестарт после паузы
        88: /// тишины (`RestartQueue`), а конфиг сохраняется/переэмитится сразу.
        89: /// `start_paused` — виртуальное время tokio: дедлайн дебаунса доматывается
        90: /// мгновенно и детерминированно, когда обе правки уже обработаны.
        112: // Бутстрап поднял сервер один раз (стартовый путь — немедленный, без дебаунса).
        115: // Две правки движка подряд (смена модели, затем -ngl) — как серия коммитов
        116: // полей на экране настроек. Обе уходят до истечения дебаунса.
        138: // Конфиг переэмичен сразу (обе правки), рестарта ещё не было.
        151: // По истечении паузы тишины — ровно один рестарт с итоговыми значениями
        152: // (флаш эмитит снимок статусов — ждём его как маркер).
        166: /// Ключ, введённый в настройках, сохраняется **зашифрованным**: в `settings.json`
        167: /// нет плейнтекста, но приложение читает его обратно (запись этой машины).
        168: /// Главный инвариант безопасности фичи — см. docs/research/api-key-storage.md.
        172: return; // не-systemd Linux без machine-id: сохранение ключей не поддержано
        195: // На диске — шифротекст, не секрет.
        202: // Приложение читает ключ обратно (эта же машина).
        211: /// Правка любой настройки не стирает сохранённые ключи: снимок конфига из UI их
        212: /// не несёт, оркестратор восстанавливает своё значение (как `last_active_chat`).
        236: // Снимок из UI (ключей не несёт вовсе) — как коммит любого поля настроек.
        262: /// Пустой ключ удаляет сохранённое значение (в UI — очистка поля).
        283: // Пустой ключ — удаление: флаг «настроен» гаснет.
        300: /// Снимок настроек для UI не несёт ключей (даже шифротекстом) — только флаги
        301: /// «настроен на этой машине». См. docs/research/api-key-storage.md §5.
   26  src/app/orchestrator/restart_queue.rs
        1: //! [`RestartQueue`] — дебаунс-очередь отложенного (пере)запуска серверов
        2: //! инференса/эмбеддингов при правках настроек движка (зеркало `SaveQueue`).
        3: //! Экран настроек применяет правку при коммите каждого поля, поэтому серия
        4: //! «бинарник → модель → -ngl» без дебаунса давала бы три тяжёлых рестарта
        5: //! managed `llama-server` подряд. Конфиг при этом сохраняется и переэмитится
        6: //! в UI сразу (см. `Orchestrator::handle_update_config`) — откладывается только
        7: //! дорогой рестарт процесса; сам он остаётся у оркестратора
        8: //! (`Orchestrator::flush_restarts`), очередь лишь учитывает, какие серверы и
        9: //! когда перезапускать. Стартовый подъём серверов (до петли `run`) очередь не
        10: //! проходит — он немедленный.
        16: /// Пауза тишины после последней правки настроек движка до (пере)запуска.
        21: /// Ожидает ли (пере)запуска chat-сервер (`config.engine`).
        23: /// Ожидает ли (пере)запуска embedding-сервер (`config.embed`).
        25: /// Ожидает ли (пере)запуска сервер имперсонации (`config.impersonation_engine`).
        27: /// Ожидают ли (пере)поднятия MCP-серверы (`config.mcp`).
        29: /// Момент срабатывания (продлевается каждой пометкой — дебаунс от последней).
        34: /// Помечает chat-сервер для отложенного (пере)запуска и продлевает дедлайн.
        40: /// Помечает embedding-сервер для отложенного (пере)запуска и продлевает дедлайн.
        46: /// Помечает сервер имперсонации для отложенного (пере)запуска и продлевает дедлайн.
        52: /// Помечает MCP-серверы для отложенного (пере)поднятия и продлевает дедлайн.
        62: /// Текущий дедлайн (для `select!`-таймера петли). `None` — очередь пуста.
        67: /// Забирает флаги `(chat, embed, impersonation, mcp)` и сбрасывает очередь.
        83: /// Повторные пометки коалесируются: `take` отдаёт каждый сервер по одному
        84: /// разу, не помеченные не трогаются, дедлайн сбрасывается.
        103: /// Каждая пометка продлевает дедлайн — рестарт идёт от последней правки,
        104: /// а не от первой (иначе длинная серия правок словила бы рестарт посередине).
   24  src/app/orchestrator/mcp.rs
        543: /// Персистит TOFU-пин каталога сервера в `config.mcp` (пишется напрямую, не
        544: /// через `handle_update_config` — иначе diff `config.mcp` пометил бы серверы
        545: /// на рестарт и цикл повторился бы). Ошибка записи не эскалируется (пин —
        546: /// защита, не данные; повторное одобрение при следующем запуске безвредно).
        571: /// Соединение поверх duplex (сервер-половина сразу закрыта — для событий
        572: /// Ready в тестах транспорт не дёргается).
        597: /// Референсная локаль тестов (тексты ru-бандла байт-в-байт).
        634: // Четвёртый в пределах окна — отказ.
        636: // За пределами окна старые отметки истекают — снова можно.
        664: // Событие чужого поколения — отброшено.
        676: // Актуальное поколение — каталог построен, статус Ready, TOFU-пин
        677: // возвращён для персиста (первое одобрение).
        691: // Полное описание сервера едет в метаданные (нижняя панель настроек).
        701: // Пин от «прежнего» каталога.
        710: // Сервер поднялся с ИЗМЕНИВШИМСЯ каталогом (иное описание) → инструменты
        711: // придержаны, статус — «каталог изменился», пина для персиста нет.
        725: // Подтверждение пользователем: инструменты регистрируются, новый хэш
        726: // возвращён для персиста.
        733: // Повторное подтверждение — нечего подтверждать.
        751: // Каталог совпал с пином → регистрация без нового персиста.
        773: // Крах: инструменты уходят из каталога, статус — Connecting (рестарт).
        784: // Исчерпание бюджета: ещё падения без Ready → Disconnected.
        815: // Выключенный сервер слота не получает; .cmd — Disconnected с причиной.
        822: // Мастер-выключатель: всё гаснет.
   24  src/app/runtime/tests.rs
        1: //! Тесты runtime (батчинг ввода, chunk_batch). См. mod.rs.
        12: // Буфер «hello 😊 world» при потерянном консолью эмодзи реконструируется как
        13: // «hello  world» (😊 > U+FFFF выпал) — проекция совпадает → берём буфер.
        15: // ❤ (U+2764) и селектор U+FE0F — BMP, проходят и остаются в реконструкции.
        17: // Переводы строк нормализуются (буфер \n ↔ реконструкция \r от Enter).
        24: // Устаревший/чужой буфер не совпадает с реконструкцией → НЕ подставляем его.
        26: // Пустая реконструкция никогда не матчится (нет сигнала, что это та же вставка).
        32: // Серия «a Enter b» (как пришла бы вставка "a\nb" на Windows) → одна вставка;
        33: // Enter внутри серии становится переводом строки ('\r' схлопнется при вставке).
        49: // Серия с несколькими Enter внутри (многострочная вставка) → одна вставка,
        50: // все Enter — переводы строк, ни один не уезжает отправкой.
        65: // Одиночный Enter — это отправка, НЕ вставка.
        80: // Стрелка разрывает серию: "ab" (вставка) + стрелка (событие) + "cd" (вставка).
        109: // Ctrl/Alt-комбинации не часть вставки (Ctrl+V, Alt+… — это шорткаты).
        124: // «Отпускание» клавиши отбрасывается (иначе разрывало бы серию вставки).
        141: loader.maybe_reload(true, &[]); // те же настройки — без перезагрузки
        143: loader.maybe_reload(false, &[]); // выключили — перезагрузка
        145: loader.maybe_reload(true, &["en_US".to_string()]); // другой выбор — перезагрузка
        148: // Дожидаемся готового чекера последней загрузки (пустой каталог → выключен).
        163: // `OpenSelfModel` шлёт запрос оркестратору и НЕ открывает экран сразу —
        164: // он откроется по ответному событию `SelfModelView`.
        201: // Экран `F3` закрыт → SelfModelChanged ничего не шлёт.
        212: // Экран `F3` открыт → перезапрос снимка.
        230: // BackgroundTask ставит флаг индикатора у экрана чата (не паникует).
   24  src/features/tts_command.rs
        1: //! Разбор slash-команды озвучивания в поле ввода (`/tts [N|all|stop]`). Чистая,
        2: //! тестируемая логика по образцу [`super::rag_command`]: экран чата вызывает её при
        3: //! отправке; распознанная команда превращается в намерение, нераспознанная строка
        4: //! уходит обычным сообщением. См. docs/research/tts.md §7, spec §11.9.
        6: /// Что озвучивать (снимок переписки берётся на момент команды).
        9: /// Последнее сообщение чата (поведение по умолчанию — голое `/tts`).
        11: /// Последние `n` сообщений (пользователя и модели), хронологически.
        13: /// Вся переписка чата.
        17: /// Распознанная команда озвучивания.
        20: /// Озвучить выбранный объём (прерывает текущее воспроизведение).
        22: /// Остановить воспроизведение (сбросить очередь).
        24: /// Приостановить воспроизведение, сохранив очередь (`/tts resume` продолжит).
        26: /// Продолжить приостановленное воспроизведение.
        30: /// Пытается разобрать строку ввода как команду озвучивания.
        32: /// - `None` — строка не является командой `/tts`: её следует отправить как обычное
        33: ///   сообщение;
        34: /// - `Some(Ok(cmd))` — корректная команда;
        35: /// - `Some(Err(arg))` — это `/tts`, но аргумент не распознан; `arg` — сам аргумент
        36: ///   (сообщение об ошибке формирует UI на языке интерфейса — ось B, docs/i18n-ui.md).
        43: // Голое `/tts` — последнее сообщение (самый частый сценарий).
        59: // Число: сколько последних сообщений озвучить. Ноль бессмыслен — это ошибка,
        60: // а не «ничего не делать» (иначе опечатка выглядела бы как молчаливый no-op).
        109: // Ошибка несёт сам аргумент — текст подсказки строит UI (локализованно).
        118: // Лишние токены после аргумента игнорируются (как у `/rag list`).
   23  deny.toml
        1: # Конфигурация cargo-deny (advisory-аудит зависимостей). Прогон — workflow `audit.yml`
        2: # (еженедельно + вручную, не на PR → merge не блокирует). См. release-engineering.md Ф6.
        3: # Allow-список лицензий выверен прогоном `cargo deny check licenses` на текущем графе.
        6: # База RustSec: уязвимости/unmaintained → ошибка (сигнал разобраться), yanked → warn.
        8: # Известные транзитивные advisory без доступного фикса — все приходят из `syntect`
        9: # (подсветка кода): он парсит **свои вшитые** синтаксисы/темы, не ввод пользователя,
        10: # поэтому DoS-риск для нас не реализуется. Снять, когда syntect обновит эти зависимости
        11: # (или при замене syntect). Аудит остаётся зелёным на известном наборе и краснеет на НОВЫХ.
        15: # quick-xml DoS-advisory. Крейт в графе через syntect (свои вшитые темы/синтаксисы,
        16: # не ввод) И напрямую — разбор `word/document.xml` при индексации DOCX (`/rag add`,
        17: # doc_extract.rs). Риск неактуален: приложение — локальный однопользовательский
        18: # инструмент, DOCX добавляет сам пользователь из своих файлов; худший случай —
        19: # замедленная индексация специально скрафченного пользователем же документа
        20: # (best-effort, сбой одного файла лишь пропускается). Снять при обновлении quick-xml.
        21: "RUSTSEC-2026-0194", # quick-xml quadratic-time на дублирующихся атрибутах
        23: # ttf-parser объявлен автором unmaintained (RUSTSEC-2026-0192, безопасного апгрейда
        24: # нет). Это advisory о заброшенности, не уязвимость (не RCE/порча памяти). Приходит
        25: # транзитивно из pdf-extract → lopdf (чтение шрифтов при извлечении текста из PDF,
        26: # `/rag add`); используется на локальных файлах самого пользователя (best-effort).
        27: # Снять при переходе pdf-extract/lopdf на поддерживаемый парсер шрифтов (skrifa).
        32: # Разрешённые SPDX-лицензии зависимостей. Всё вне списка → ошибка аудита.
        51: # Дубли версий одного крейта — предупреждение, не ошибка (норма для большого графа).
        56: # Все зависимости — из crates.io; сторонние реестры/git — ошибка.
   23  src/main.rs
        142: CliCommand::Help { .. } | CliCommand::Version => unreachable!("обработаны в main"),
        154: tracing::warn!("отказ запуска: другой экземпляр приложения уже работает");
        416: // Гард единственного экземпляра: не переустанавливаем песочницу, пока приложение
        417: // работает (могло бы читать заменяемый бинарь/ассеты во время индексации/запуска).
        433: /// CLI: экспорт бандла локали в файл-шаблон. `i18n::init` уже вызван в `main` (реестр
        434: /// с внешними готов). Вывод — в stdout (TUI не запущен).
        471: /// Одноразовый импорт из файла формата mindfork-import (spec §12.2,
        472: /// docs/import-format.md). Идемпотентно (детерминированные id), исходный файл
        473: /// только читается. Вывод — в stdout (TUI не запущен), не в лог.
        475: // Существующие данные могут требовать миграции (или быть из более новой версии) —
        476: // мигрируем перед открытием хранилища, как при обычном старте (release-engineering.md §3.4).
        490: // Переносим глобальные настройки источника (только заданные поля — частичный
        491: // перенос не затирает настройки пользователя).
        522: /// Посев конфигурации переменными окружения (dev/смоук-workflow). Env имеет
        523: /// приоритет над `settings.json`, чтобы быстрый запуск против сервера не требовал
        524: /// правки файла. Затрагивает только chat/embedding-серверы:
        525: /// - `MINDFORK_ENGINE_URL` — external chat-сервер (любой OpenAI-совместимый);
        529: ///   `MINDFORK_EMBED_PORT`) — embedding-сервер.
        584: // Явный язык из settings.json — сильнейший сигнал (перекрывает умолчание).
        587: // Нет settings → разрешённый язык умолчаний (явный из defaults.json ИЛИ
        588: // определённый по локали ОС в Paths::resolve — сюда приходит уже готовым).
        597: // Локализованный префикс + однострочная цепочка причин (нет многострочного Debug).
        601: // Русский префикс на ru-локали.
   22  src/features/chat_export.rs
        1: //! Экспорт переписки чата в простой текст для копирования в буфер обмена.
        2: //! Чистая, тестируемая без UI логика (вызывается оркестратором). См. spec §11.2.
        8: /// Форматирует всю переписку чата в читаемый текст для буфера обмена: помечает
        9: /// роли (`Пользователь`/`Ассистент`), сохраняет многострочный текст, пропускает
        10: /// системные/инструментальные сообщения. Непустой `title` идёт заголовком.
        12: /// По умолчанию (`CopySettings` все флаги `false`) копируется только текст сообщений.
        13: /// Опционально (`opts`) к блоку ассистента добавляются: «мысли» (CoT, перед текстом),
        14: /// параметры вызовов инструментов (имя + аргументы) и их результаты — берутся из
        15: /// `Message.tool_calls` (отдельные tool-сообщения по-прежнему пропускаются, чтобы не
        16: /// дублировать). Возвращает `None`, если содержательных блоков нет (копировать нечего).
        43: // Системные и инструментальные сообщения в переписку не входят
        44: // (результаты инструментов берутся из `tool_calls` ассистента).
        49: // Только заголовок (или вовсе ничего) — содержательной переписки нет.
        57: /// Собирает блок одного сообщения ассистента: опциональные «мысли», текст и
        58: /// опциональные tool-блоки. `None`, если после фильтрации блок пуст.
        110: /// Копирование только текста (поведение по умолчанию).
        115: /// Сообщение ассистента с «мыслями» и одним вызовом инструмента (имя/аргументы/
        116: /// результат) — для проверки опциональных блоков.
        161: // Только системное/инструментальное/пустое — копировать нечего, даже с заголовком.
        173: // По умолчанию «мысли» и tool-блоки не копируются — только текст.
        231: // Пустой текст, но есть вызов инструмента и опция включена → блок не пропадает.
        246: // Но при выключенных опциях такое сообщение пропускается целиком.
   20  src/app/orchestrator/tests/tts.rs
        1: //! Тесты озвучивания (`/tts`, spec §11.9): понятные отказы при ненастроенном
        2: //! движке и точки остановки воспроизведения (по настройке и безусловные).
        8: /// Ставит «идёт озвучивание» без реальной задачи: токен + поколение — ровно то
        9: /// состояние, которое проверяют точки остановки.
        19: // Дефолтный режим — облако OpenAI с осмысленной моделью, но без ключа: команда
        20: // должна ответить понятной подсказкой, а не молчать.
        41: // Чип озвучивания не зажигался (задача не стартовала).
        69: // Настройка «прерывать при переключении чата» включена по умолчанию.
        87: // Выключенная настройка — озвучка переживает переключение.
        100: // Удаление чата — безусловная остановка (озвучиваемого текста больше нет),
        101: // настройки на неё не влияют.
        117: // Ничего не играет — команда не эмитит лишних событий.
        131: // Звука в CI нет, поэтому `tts_playback` не заводится — проверяем, что команды
        132: // pause/resume при отсутствии активной озвучки не паникуют и не шлют событий.
        139: // Пометка «идёт» без реального устройства: pause/resume всё равно безопасны
        140: // (хэндла нет), а `stop_tts` снимает паузу и гасит чип.
        152: // Гонка «остановили старую → запустили новую»: поздний `done` устаревшей задачи
        153: // не должен гасить чип текущей.
        175: // Кладём сообщение прямо в чат: гейт готовности сервера здесь не при чём —
        176: // проверяем именно отказ настройки озвучивания.
   20  src/features/spellcheck/check.rs
        1: //! Спелл-чекер: набор активных словарей + персональный словарь. Слово корректно,
        2: //! если принято **хотя бы одним** словарём или персональным (поддержка смешанного
        3: //! ru/en текста). См. spec §11.5.
        13: /// Максимум подсказок, отдаваемых на одно слово.
        16: /// Спелл-чекер. Создаётся в фоне через [`dict::load`].
        20: /// Путь к персональному словарю (для дозаписи при «добавить в словарь»).
        37: /// Включён ли спелл-чек (есть хотя бы один словарь). При выключенном
        38: /// [`misspellings`](Self::misspellings) ничего не подчёркивает.
        43: /// Корректно ли слово (персональный словарь или любой из активных).
        51: /// Диапазоны (по индексам символов) слов с ошибками в строке. Пусто, если
        52: /// спелл-чек выключен.
        64: /// Возвращает слово, чей диапазон содержит позицию символа `col` (для попапа
        65: /// подсказок по слову под курсором). `None`, если слово корректно/отсутствует.
        75: /// Подсказки исправления (объединение из всех словарей, без дублей).
        94: /// Добавляет слово в персональный словарь (в память + дозапись в файл).
        97: return Ok(()); // уже есть
        136: // "hello helo world" → ошибка только в "helo" [6,10)
        145: // При отсутствии словарей ничего не подчёркиваем (misspellings — UI-вход).
        154: // курсор внутри "helo" (позиции 6..10)
        157: // курсор внутри корректного "hello" → None
   17  src/app/orchestrator/tests/chats.rs
        1: //! Тесты оркестратора — список чатов, черновики, bootstrap, восстановление активного. Часть модуля 
        2: //! (фикстуры в mod.rs). См. docs/history/refactoring-god-objects.md, этап 3.
        23: // None → первый профиль, без приветствия.
        34: // Чат с перепиской → событие CopyToClipboard с текстом ролей.
        49: // Пустой чат (нет сообщений) → ошибка списка, не текст.
        76: // Повторная установка того же текста — без повторной пометки (no-op).
        99: // Черновик сохраняется в файле чата.
        103: // Отправка очищает черновик.
        162: /// Последний открытый чат запоминается в настройках и восстанавливается при
        163: /// следующем запуске — даже если другой чат изменён позже (обычный fallback выбрал
        164: /// бы самый недавний).
        170: // Запуск 1: bootstrap создаёт дефолтный чат, добавляем второй (он становится
        171: // активным и изменённым позже), затем переключаемся обратно на первый.
        217: // Настройки на диске помнят первый чат.
        222: // Запуск 2 на тех же данных (конфиг загружается с диска, как в main.rs):
        223: // восстанавливается именно первый чат.
        314: // После удаления единственного чата создаётся новый и активируется.
   16  src/app/orchestrator/reflection.rs
        342: // Ярус 2: авто-рефлексии даны note_link/note_neighbors (граф над наблюдениями).
        343: // Ярус 3: + note_recall (id пользовательских заметок для кросс-органных связей).
        352: // Ярус 3: рефлексии предложено связывать наблюдение «о себе» с фактом «о
        353: // собеседнике» (кросс-органное ребро через note_recall + note_link).
        357: // Обзор self-консолидации: рефлексии указано использовать его блок.
        380: mk(base - Duration::days(5), DeletedCause::Rewrite), // до since
        385: cause: None, // старая запись без причины — не считается
        388: // since = 4 ч назад → последние 3 (2 regen + 1 delete); rewrite (5 дн.) отсечён.
        393: // since=None → учитываем всё, собственное поведение (rewrite) — отдельной фразой.
        396: // Нет сигналов → None.
        408: Message::assistant(""), // пустой ответ не считается
        412: // Без ватермарка — считаем все непустые ответы ассистента (a1,a2,a3).
        414: // Ватермарк после a2 (индекс 4): в окне только a3.
        416: // Ватермарк в конце — окно пусто.
        422: // История усечена (Ctrl+R/Ctrl+E) — ватермарк больше длины: кламп к len,
        423: // окно пусто, паники нет.
   16  src/app/orchestrator/tts.rs
        451: /// Тексты реплик без ролей — для проверок, где роль не важна.
        456: /// Реплика ассистента для входа чанкера (роль там не важна, но нужна типу).
        461: /// Тексты чанков без ролей.
        479: // Порядок хронологический, и роли сохранены (для многоголосой озвучки).
        484: // Запрос больше, чем есть, отдаёт всё (кламп, а не ошибка).
        506: // Совсем нечего озвучивать — None (вызывающий покажет понятную ошибку).
        521: // Тумблер «Озвучивать роли» действует и при одиночном `/tts` (решение Р6).
        535: // Сообщение из одного код-блока даёт только пометку — она озвучивается,
        536: // а вот пустой результат экстрактора сообщение бы отбросил.
        545: // Роль реплики передаётся всем её чанкам — по ней выбирается голос.
        552: // Первые чанки — пользователя, последние — ассистента.
        566: // Границы — по предложениям (пунктуация сохранена в конце чанка).
        572: // Ничего не потеряно.
        584: // Граница сообщения — естественная пауза и точка отмены: не склеиваем даже
        585: // короткие реплики.
        600: // Одно слово длиннее лимита режется посимвольно, а не теряется.
   15  .github/workflows/ci.yml
        1: # CI: линты (fmt + clippy) на Linux + тесты на Linux и Windows.
        2: # Тулчейн берётся из rust-toolchain.toml (пин, release-engineering.md Ф4).
        3: # #[ignore]-смоуки (движок/сеть/живая модель) без env-переменных тихо пропускаются —
        4: # сети и живого сервера CI не требует. См. docs/release-engineering.md §3.3.
        12: # Отменяем предыдущие прогоны того же ref (экономия минут раннера).
        22: name: Линты (fmt + clippy)
        26: - name: Установить тулчейн (из rust-toolchain.toml)
        29: - name: Системные зависимости (ALSA — воспроизведение речи /tts)
        31: - name: Форматирование
        33: - name: Clippy (все цели, warnings = ошибки)
        37: name: Тесты (${{ matrix.os }})
        45: - name: Установить тулчейн (из rust-toolchain.toml)
        48: # ALSA нужна только Linux-сборке (rodio/cpal); на Windows — чистый windows-rs.
        49: - name: Системные зависимости (ALSA — воспроизведение речи /tts)
        52: - name: Тесты
   14  src/app/orchestrator/tests/impersonation.rs
        1: //! Тесты оркестратора — имперсонация (построение запроса + поток). Часть модуля [`super`]
        2: //! (фикстуры в mod.rs). См. docs/history/refactoring-god-objects.md, этап 3.
        25: // Роли поменялись местами: assistant↔user.
        45: // Имперсонация отбрасывает «мысли», поэтому запрос обязан гасить reasoning —
        46: // иначе модели со «вшитым» thinking (Gemma/Qwen) тратят весь бюджет на
        47: // reasoning_content, а ответный текст приходит пустым (предпросмотр пуст).
        54: // Пользователь оставил «мысли» включёнными в семплинге имперсонации —
        55: // запрос всё равно должен их выключить.
        102: // Модель собеседника подмешана в системный промпт имперсонации.
        115: // Первый запрос (отправка) → «ответ»; второй (имперсонация) → реплика.
        131: // Нужна хотя бы одна реплика в истории.
        144: // Старт имперсонации.
        150: // Текст реплики приходит дельтами.
        157: // Завершение со Stop.
   11  packaging/linux/build-packages.sh
        2: # Собирает Linux-пакеты (deb + rpm + archlinux) из готового бинарника через nfpm.
        3: # nfpm должен быть в PATH. Используется и в CI (packaging.yml / release.yml), и локально.
        5: # Использование: packaging/linux/build-packages.sh <путь-к-бинарнику> <версия> <каталог-вывода>
        6: # Пример:        packaging/linux/build-packages.sh target/release/mindfork-rs 0.9.0 dist
        9: BIN="${1:?путь к бинарнику}"
        10: export VERSION="${2:?версия (напр. 0.9.0)}"
        11: OUT="${3:?каталог вывода}"
        13: # Корень репозитория (скрипт лежит в packaging/linux/).
        17: # Бинарь стейджится в фиксированный путь, на который ссылается packaging/nfpm.yaml.
        22: # Имена артефактов — по конвенциям форматов (§8 docs/history/installers.md).
        31: echo "Собрано в $OUT:"
   10  src/features/spellcheck/segment.rs
        1: //! Сегментация строки на слова для спелл-чека. См. spec §11.5.
        3: //! Используем собственный посимвольный сканер «буквенных пробегов» вместо полной
        4: //! UAX#29-сегментации: для проверки орфографии нужны именно последовательности
        5: //! букв (ru/en), а соединители (апостроф `'`/`’`, дефис `-`/`‑`) учитываются
        6: //! только ВНУТРИ слова (`don't`, `well-known` — один токен; `слово-` — без
        7: //! хвостового дефиса). Индексы — по символам (совпадают с моделью `InputBox`).
        9: /// Слово в строке: диапазон по индексам символов `[start, end)` и его текст.
        17: /// Является ли символ внутрисловным соединителем (учитывается между буквами).
        22: /// Разбивает строку на слова (буквенные пробеги с внутренними соединителями).
        84: // "ёж и" → "ёж" [0,2), "и" [3,4)
    9  src/app/runtime/clipboard.rs
        1: //! Runtime — чтение/запись системного буфера обмена (arboard-слот). Часть модуля [`super`]; разбито
        2: //! runtime.rs (см. docs/history/refactoring-god-objects.md, этап 7).
        4: /// Читает текст системного буфера обмена (лениво создавая клиент). `None`, если буфер
        5: /// недоступен/пуст/не текстовый. Используется только на Windows для восстановления
        6: /// эмодзи во вставке (см. [`reconcile_paste`]).
        15: /// Пишет текст в системный буфер обмена, создавая клиент лениво и переиспользуя
        16: /// его. Возвращает текст ошибки (вместо паники), если буфер недоступен — на
        17: /// headless-Linux без X11/Wayland конструктор `arboard` может упасть.
        25: // `unwrap` безопасен: только что гарантировали `Some`.
    9  src/app/supervisor.rs
        118: let cloud = settings.cloud().expect("облачный режим");
        120: settings.mode.cloud_provider().expect("облачный режим"),
        153: let cloud = settings.cloud().expect("облачный режим");
        155: settings.mode.cloud_provider().expect("облачный режим"),
        222: tracing::warn!(error = %err, "не удалось запустить embedding-сервер; RAG недоступен");
        230: let cloud = settings.cloud().expect("облачный режим");
        232: settings.mode.cloud_provider().expect("облачный режим"),
        241: tracing::warn!("у Anthropic нет embeddings API; для RAG задайте другой эмбеддер");
        440: tracing::warn!("облачные эмбеддинги не настроены (модель/ключ); RAG недоступен");
    9  src/features/chat_search_sort.rs
        1: //! Фильтрация и сортировка списка чатов (чистая логика, тестируется без UI).
        2: //! См. spec §11.2 (поиск по подстроке, две сортировки).
        6: /// Режим сортировки списка чатов.
        9: /// По дате создания (по убыванию).
        11: /// По дате последнего изменения (по убыванию). Значение по умолчанию.
        17: /// Переключает режим (для горячей клавиши).
        26: /// Фильтрует чаты по вхождению `query` в название (без учёта регистра) и
        27: /// сортирует по убыванию соответствующей даты. Пустой `query` ничего не фильтрует.
        72: // "Альфа" не содержит латинского "alpha"; совпадёт только "alpha draft"
    9  src/features/tools/meta.rs
        77: ToolGroup::Introspection => "Интроспекция",
        78: ToolGroup::Memory => "Память и знания",
        79: ToolGroup::ExternalWorld => "Внешний мир",
        80: ToolGroup::Files => "Файлы",
        81: ToolGroup::Utils => "Утилиты",
        82: ToolGroup::Subagent => "Субагент",
        83: ToolGroup::Conversation => "Управление беседой",
        84: ToolGroup::SelfModel => "Модель себя",
        85: ToolGroup::Plugins => "Плагины (MCP)",
    9  src/screens/chat_list.rs
        138: /// Вставляет текст из буфера обмена в поле переименования (если открыто).
        139: /// Вне режима переименования — no-op. См. spec §11.5.
        144: /// Перепроверяет орфографию в поле переименования (если открыто и изменилось).
        145: /// Возвращает `true`, если подсветка обновлена (нужна перерисовка). Чекер
        146: /// одалживается из экрана чата — владельца (`app` сводит это в петле).
        151: /// Рисует список во весь экран. `&mut self` — поле переименования рисует
        152: /// [`InputBox`](crate::widgets::input_box::InputBox), которому нужен `&mut`.
        234: // Печать символа в строку поиска — обработана внутри (нет намерения).
        243: // Забирается ровно один раз (сбрасывается) — вторая активация закрыть не должна.
    6  src/features/spellcheck/mod.rs
        1: //! Спелл-чек (features/spellcheck): Hunspell-словари (`spellbook`), сегментация
        2: //! и персональный словарь. Слово корректно, если принято хотя бы одним активным
        3: //! словарём. См. spec §11.5.
        5: //! - [`segment`] — разбиение строки на слова (учёт апострофов/дефисов, ru/en).
        6: //! - [`dict`] — загрузка словарей из `dictionaries/` и персонального словаря.
        7: //! - [`check`] — [`SpellChecker`]: проверка, подсказки, добавление в словарь.
    6  src/features/tools/fetch.rs
        123: "загрузить страницу"
        377: // JSON-ответ API (по Content-Type) отдаётся как есть — раньше readability
        378: // возвращала пусто → «не удалось извлечь читаемый текст» (Steam appreviews).
        386: // Нет Content-Type, но тело — JSON-форма (начинается с `{`) → отдаём как есть.
        393: // HTML без читаемого текста (только скрипты) → извлекать нечего.
        408: /// Реальный сетевой смоук (вручную: `cargo test -- --ignored`).
    6  src/features/tools/self_model.rs
        196: "показать модель себя"
        227: "саморефлексия"
        292: "добавить наблюдение"
        355: "обновить модель себя"
        408: let goal = m.goals.last().expect("только что добавлена");
        553: "обновить собеседника"
    6  src/shared/i18n.rs
        71: let mut set = INTERN.lock().expect("интернер кодов языков отравлен");
        91: .expect("набор предупреждений отравлен");
        96: "i18n: ключ отсутствует во всех бандлах цепочки — в вывод уйдёт сам ключ (слаг)"
        143: Lang::Ru => "Русский",
        159: Lang::Ext(_) => unreachable!("внешний язык не имеет вшитого источника"),
        1000: let key = "ui.feed.role.user"; // ru "ВЫ" ≠ en "YOU"
    6  src/shared/storage/db/mod.rs
        124: // `PRAGMA user_version = N` не принимает связанный параметр — форматируем (v: u32,
        125: // инъекция невозможна). Внутри транзакции изменение атомарно с ней.
        325: // Схема применена (одна из таблиц baseline есть).
        352: // Реестр DB_STEPS пуст — реальных миграций (кроме baseline) нет.
        371: // Успешный шаг: таблица создана, версия = 2.
        385: // Падающий шаг: полный откат — таблицы нет, версия прежняя.
    6  src/shared/tts/gemini.rs
        202: anyhow::bail!("Gemini отклонил запрос озвучивания (причина: {reason})");
        211: anyhow::bail!("Gemini не вернул аудио");
        215: .context("разбор base64 аудио от Gemini")?;
        237: _ = cancel.cancelled() => anyhow::bail!("озвучивание отменено"),
        245: _ = cancel.cancelled() => anyhow::bail!("озвучивание отменено"),
        246: j = resp.json() => j.context("разбор ответа Gemini TTS")?,
    5  .github/workflows/audit.yml
        1: # Аудит зависимостей (cargo-deny): уязвимости (advisories), лицензии, дубли версий,
        2: # источники. Запускается по расписанию (еженедельно) и вручную — НЕ на pull_request,
        3: # поэтому merge не блокирует (advisory-режим, release-engineering.md Ф6). Конфигурация —
        4: # deny.toml в корне репозитория.
        9: - cron: "0 6 * * 1" # понедельник 06:00 UTC
    5  packaging/linux/mindfork-rs.desktop
        11: # Обязательно: приложение — TUI, stdout занят интерфейсом. Без Terminal=true
        12: # запуск из меню не даст терминала и окно мгновенно закроется.
        14: # Utility — основная категория; ConsoleOnly помечает текстовый интерфейс.
        15: # Chat намеренно не берём: по спецификации он требует основной категории Network,
        16: # иначе desktop-file-validate выдаёт предупреждение.
    5  rust-toolchain.toml
        1: # Пин тулчейна (release-engineering.md, Ф4). Причина: гейт
        2: # `cargo clippy --all-targets -- -D warnings` каждый новый stable ломал бы
        3: # внезапно новыми линтами. С пином обновление тулчейна — осознанный отдельный PR
        4: # («поднять до 1.NN», чинящий новые линты), как обновление любой зависимости.
        5: # Edition 2024 требует ≥ 1.85; пин совпадает с версией разработки.
    5  src/app/orchestrator/tests/self_consolidation.rs
        35: orch.config.self_model.auto_consolidate_every = 0; // выключено
        43: // Вышли до инкремента — счётчика нет.
        50: // Профиль включил модель себя, но наблюдений < 2 и summary не раздут → нет спавна.
        69: // Счётчик инкрементирован (порог достигнут), но не сброшен — цикл не потерян.
        75: // Успех «сна» модели себя обновляет открытый экран F3 (как рефлексия): SelfModelChanged.
    5  src/shared/api/openai/client.rs
        88: bail!("сервер ещё загружает модель (503)");
        115: anyhow::bail!("движок вернул статус {status}");
        117: anyhow::bail!("движок вернул статус {status}: {detail}");
        230: anyhow::bail!("эмбеддер вернул статус {status}");
        232: anyhow::bail!("эмбеддер вернул статус {status}: {detail}");
    4  src/entities/self_model.rs
        652: let mut out = String::from("Известное о человеке, за которого ты пишешь:");
        654: out.push_str(" черты — ");
        659: out.push_str(" интересы — ");
        664: out.push_str(" отношения с собеседником — ");
    3  src/app/orchestrator/rag.rs
        255: tracing::warn!(file = %file.display(), error = %err, "RAG: не удалось проиндексировать файл");
        343: tracing::warn!(source = %info.source, "RAG rebuild: исходник недоступен, источник пропущен");
        448: tracing::warn!(source = %source, error = %err, "RAG rebuild: не удалось переиндексировать источник")
    3  src/entities/profile.rs
        23: user: "Вы".to_string(),
        24: assistant: "Ассистент".to_string(),
        25: system: "Система".to_string(),
    3  src/features/tools/fs.rs
        140: "прочитать файл"
        201: "записать файл"
        292: "список файлов"
    3  src/features/tools/notes/edit.rs
        21: "переписать заметку"
        105: "заместить заметку"
        172: "слить заметки"
    3  src/shared/api/gemini/client.rs
        86: format!("\n⚠ Gemini не выдал ответ (причина: {reason}).")
        114: anyhow::bail!("движок (Gemini) вернул статус {status}");
        116: anyhow::bail!("движок (Gemini) вернул статус {status}: {detail}");
    3  src/shared/tts/mod.rs
        186: tracing::warn!(%status, body = %detail, "{what} вернул статус ошибки");
        188: anyhow::anyhow!("{what}: статус {status}")
        190: anyhow::anyhow!("{what}: статус {status}: {detail}")
    3  tools/cyrillic_scan.py
        20: CYR = re.compile(r"[Ѐ-ӿ]")
        28: _KEY_MOD = re.compile(r"(?i)(Ctrl|Alt|Shift|Cmd|Win|Super|Meta)\+[Ѐ-ӿ]")
        29: _KEY_LIT = re.compile(r"['`][Ѐ-ӿ]['`]")
    2  src/features/tools/control.rs
        62: "дописать сообщение"
        97: "переписать ответ"
    2  src/features/tools/notes/graph.rs
        19: "связать заметки"
        91: "связи заметки"
    2  src/features/tools/rag.rs
        81: "добавить в базу знаний"
        143: "поиск в базе знаний"
    2  src/shared/api/anthropic/client.rs
        82: anyhow::bail!("движок (Anthropic) вернул статус {status}");
        84: anyhow::bail!("движок (Anthropic) вернул статус {status}: {detail}");
    2  src/shared/api/openai/responses/client.rs
        70: anyhow::bail!("движок (OpenAI Responses) вернул статус {status}");
        72: anyhow::bail!("движок (OpenAI Responses) вернул статус {status}: {detail}");
    2  src/shared/config.rs
        569: PythonMode::Wasmer => "Wasmer-песочница",
        570: PythonMode::Local => "локальный интерпретатор",
    2  src/shared/instance.rs
        29: #[error("приложение уже запущено")]
        32: #[error("не удалось инициализировать блокировку единственного экземпляра: {0}")]
    2  src/shared/secrets.rs
        152: #[error("на этой машине не поддерживается сохранение ключей (нет machine-id)")]
        155: #[error("не удалось зашифровать секрет")]
    1  docs/branding.md
        134: println!("cargo:warning=не удалось вшить иконку в .exe: {err}");
    1  docs/english-source-migration.md
        7: supported locale). Roadmap: `docs/roadmap.md` §"Английский исходник".
    1  src/features/tools/calc.rs
        28: "калькулятор"
    1  src/features/tools/datetime.rs
        26: "текущее время"
    1  src/features/tools/mod.rs
        494: None => anyhow::bail!("неизвестный инструмент: {id}"),
    1  src/features/tools/notes/cite.rs
        22: "сослаться на источник"
    1  src/features/tools/notes/overview.rs
        318: "консолидация заметок"
    1  src/features/tools/notes/recall.rs
        19: "найти заметки"
    1  src/features/tools/notes/save.rs
        19: "сохранить заметку"
    1  src/features/tools/subagent.rs
        61: "запрос суб-агенту"
    1  src/shared/api/contract.rs
        309: anyhow::bail!("embedding-сервер не настроен — RAG недоступен")
    1  src/shared/storage/json.rs
        106: "пропущен повреждённый файл чата");
