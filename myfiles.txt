   74  src/app/runtime/dispatch.rs
        1: //! Runtime — применение AppEvent к экрану + трансляция Intent → AppCommand. Часть модуля [`super`];
        2: //! runtime.rs (см. docs/history/refactoring-god-objects.md, этап 7).
        6: /// Применяет событие оркестратора к экрану чата (read-only-проекция). Снимок
        7: /// настроек при открытом экране настроек дополнительно обновляет его рабочую
        8: /// копию (отражает создание/удаление профилей).
        17: // Статус серверов — в чат (строка статуса) и, если открыт, в экран настроек
        18: // (чипы в секции «Модель/сервер»: подключение → готов видно на месте).
        25: // Снимок списка применяем к чату всегда (для следующего открытия/`Ctrl+N`),
        26: // а при открытом экране списка — ещё и к нему (живое обновление).
        34: // Ошибка операции списка: в его область статуса, если экран открыт; иначе
        35: // (поздний ответ авто-названия при закрытом списке) — заметкой в ленту.
        40: // Запись в буфер обмена — side-effect UI-слоя; сама запись и маршрутизация
        41: // подтверждения/ошибки вынесены в `deliver_clipboard` (`apply_event` не знает
        42: // про `arboard`).
        58: // Тема/режим совместимости/язык UI могли смениться — обновим палитру
        59: // и локаль открытых overlay-экранов (список/модель себя) одним broadcast.
        76: // Пришла активация только что созданного чата (`Ctrl+N` в
        77: // списке) — закрываем список и показываем новый чат. Так
        78: // переход прежний→новый атомарен, без промежуточного мигания.
        81: // Удаление активного чата при открытом списке меняет активный —
        82: // обновим его метку в списке.
        130: // Ответ на запрос/правку модели себя (`F3`): открываем экран либо обновляем
        131: // уже открытый на месте (сохраняя выделение — важно при правках).
        142: // «Модель себя» изменилась фоном/инструментами — обновляем ТОЛЬКО открытый
        143: // экран `F3` (перезапрос свежего снимка); при закрытом — игнор.
        159: /// Пишет переписку в буфер обмена и направляет подтверждение/ошибку в статус
        160: /// экрана списка чатов (его открывали для копирования) либо, если он закрыт, —
        161: /// заметкой в ленту. Инкапсулирует единственное обращение к `arboard`
        162: /// ([`write_clipboard`]), чтобы `apply_event` не знал про буфер обмена.
        172: Ok(()) => list.set_notice("Переписка скопирована в буфер обмена".into()),
        173: Err(err) => list.set_error(format!("Не удалось скопировать в буфер обмена: {err}")),
        176: Ok(()) => screen.push_note("Переписка скопирована в буфер обмена"),
        177: Err(err) => screen.push_error(&format!("Не удалось скопировать в буфер обмена: {err}")),
        182: /// Намерение любого из экранов — единый тип, чтобы снятое из активного экрана
        183: /// намерение можно было диспетчеризовать одним владением (без 4 параллельных
        184: /// `Option` и 4 почти одинаковых `if`-блоков, конфликтовавших по заимствованиям).
        192: /// Диспетчеризует намерение активного экрана в соответствующий транслятор.
        193: /// Возвращает `true`, если запрошен выход.
        208: /// Транслирует намерение чата в команду оркестратору (или открывает экран
        209: /// настроек/списка чатов). Возвращает `true` для [`ChatIntent::Quit`].
        239: // Начальный снимок статусов серверов (чипы в секции «Модель/сервер»);
        240: // дальше их обновляет `apply_event` из события `ServerStatus`.
        242: // Снимок MCP-хоста (каталог инструментов + статусы серверов);
        243: // дальше его обновляет `apply_event` из события `Settings`.
        245: // Какие ключи сохранены на этой машине (поле-статус «API-ключ»).
        251: // Список чатов открывается из снимка, который чат держит актуальным.
        261: // Просмотр модели себя: данными владеет оркестратор — запрашиваем снимок,
        262: // экран откроется по событию `SelfModelView` (см. `apply_event`).
        267: // Тумблер прокрутки колесом: включаем/выключаем захват мыши терминала.
        268: // Это чисто терминальный side-effect (FSD: экран про терминал не знает,
        269: // только сообщает желаемое состояние). При включённом захвате выделение
        270: // текста доступно с зажатым Shift.
        279: // Копирование в буфер обмена перехватывается раньше — в `process_input_batch`
        280: // (там есть слот `arboard`, а здесь `screen` иммутабелен). Сюда не доходит.
        287: /// Транслирует намерение экрана списка чатов в команду (или управление экранами).
        288: /// Возвращает `true` для [`ChatListIntent::Quit`] (петля завершается).
        296: // Закрытие/переход к чату возвращает базовый экран.
        306: // Создание чата запускает поток нового чата на экране чата (там живёт
        307: // выбор профиля — оверлей при >1 профиле).
        310: // Один профиль: чат создаёт оркестратор (round-trip). Список
        311: // ОСТАВЛЯЕМ открытым до прихода `ChatActivated` нового чата —
        312: // иначе на время round-trip мигнул бы прежний активный чат. По
        313: // приходу активации `apply_event` переключит на новый чат.
        320: // >1 профиля: `request_new_chat` открыл оверлей выбора профиля в
        321: // экране чата — показываем чат, чтобы оверлей был виден.
        330: // Копирование/удаление/переименование/авто-название не закрывают список:
        331: // подтверждение/ошибка прилетят в его область статуса, обновлённый набор —
        332: // событием `ChatList`.
        342: /// Транслирует намерение экрана настроек в команду (или закрывает его).
        343: /// Возвращает `true` для [`SettingsIntent::Quit`] (петля завершается).
        372: /// Транслирует намерение экрана просмотра модели себя: закрытие возвращает к чату,
        373: /// `Quit` завершает петлю (`true`). Команд оркестратору не шлёт (read-only).
        385: // Правка: команда оркестратору; экран остаётся открытым и обновится по
        386: // ответному `SelfModelView` (см. `apply_event`).
   71  artwork/build-wordmarks.py
        2: """Генератор вордмарков mindfork: текст → кривые (self-contained SVG).
        4: Зачем: SVG с `<text>` зависит от шрифта на машине зрителя. GitHub, чужой браузер
        5: и просмотрщики в Linux нужного моноширинного шрифта не имеют — начертание
        6: подменяется, и вордмарк выглядит сломанным. Поэтому глифы переводятся в `<path>`:
        7: файл становится самодостаточным и рендерится одинаково везде.
        9: Шрифт: **JetBrains Mono ExtraBold** (SIL Open Font License 1.1) — определён по
        10: эталону `wordmark-example.png` подгонкой метрик (см. `artwork/README.md §Шрифт`).
        11: OFL разрешает использование шрифта для создания артворка и распространение
        12: полученных кривых; сам файл шрифта в репозиторий не кладём — он нужен только для
        13: перегенерации.
        15: Геометрия лockup'а (пропорции иконки к тексту) измерена с эталона — константы
        16: `ASC_RATIO`/`GAP_RATIO`/`BASE_RATIO`/`TRACKING` ниже.
        18: Запуск:
        19: python artwork/build-wordmarks.py [--font <путь к JetBrainsMono-ExtraBold.ttf>]
        21: Без `--font` шрифт ищется в стандартных местах (системные шрифты, бандлы IDE
        22: JetBrains). Перезаписывает `artwork/mindfork-wordmark*.svg`.
        36: # --- Палитра бренда (см. artwork/README.md) ---------------------------------
        37: ORANGE = "#c25a27"  # акцент: «fork» и ствол глифа
        38: GRAY = "#5c6370"  # ветви глифа
        39: PLATE = "#09090b"  # подложка иконки
        40: LIGHT_TEXT = "#e4e4e7"  # «mind» на тёмном
        41: DARK_TEXT = "#18181b"  # «mind» на светлом
        42: MUTED = "#71717a"  # тэглайн
        44: # --- Глиф иконки: 5 прямоугольников на сетке 16×16 ---------------------------
        45: # Единственный источник истины — mindfork-icon-transparent.svg; здесь дубль для
        46: # генерации. Тест widgets/logo.rs сверяет таблицу в коде с тем же SVG.
        55: # --- Геометрия лockup'а, измеренная с wordmark-example.png --------------------
        56: # Иконка в эталоне: 388×388 px; базовая линия текста, высота выносного элемента и
        57: # зазор пересчитаны в доли от размера иконки, поэтому масштабируются на любой S.
        58: ASC_RATIO = 0.5227  # высота выносного элемента ('d','f','k') / размер иконки
        59: GAP_RATIO = 0.3608  # зазор «правый край иконки → левый край чернил текста» / S
        60: BASE_RATIO = 0.7418  # базовая линия ниже верха иконки / S
        61: TRACKING = -35  # межбуквенный трекинг, единицы шрифта (≈ -0.035em)
        63: ASCENDER = 730  # высота 'd'/'f'/'k' в единицах шрифта
        65: NAT_ADV = 600  # натуральный шаг моноширинного шрифта
        66: ADV = NAT_ADV + TRACKING  # фактический шаг с трекингом
        67: LSB_M = 38  # левый боковой отступ 'm' — начало чернил
        81: sys.exit(f"шрифт не найден: {explicit}")
        88: "JetBrainsMono-ExtraBold.ttf не найден.\n"
        89: "Скачать: https://github.com/JetBrains/JetBrainsMono/releases (OFL 1.1)\n"
        90: "или указать путь: --font <путь>"
        95: """Кривые подстроки одним `d`, в единицах шрифта, с учётом трекинга.
        97: `start_index` — позиция подстроки в целом слове (чтобы «fork» встал на свои
        98: места, а не начинался с нуля).
        110: """Ширина чернил слова в единицах шрифта (для раскладки и viewBox)."""
        111: # 'm'.xMin=38 … 'k'.xMax=580 (замерено по шрифту; см. artwork/README.md)
        116: """Иконка: опциональная скруглённая подложка + глиф на сетке 16×16."""
        143: """Текст «mindfork» кривыми.
        145: `split=True` — два пути: «mind» цветом `text_fill` и «fork» акцентом; иначе
        146: одно слово в `currentColor` (одноцветный вариант). Кривые остаются в единицах
        147: шрифта, а масштаб и переворот оси Y задаёт трансформация группы — так `d`
        148: читаем и правится вручную.
        151: # Перо в нуле даёт чернила от LSB_M — сдвигаем так, чтобы они легли на ink_x.
        174: """Горизонтальный лockup: иконка слева, «mindfork» справа."""
        188: # Вертикальный лockup: в отличие от горизонтального, эталона нет — пропорции
        189: # выведены. Ширина слова = 2× размера иконки (иначе моноширинное слово из 8 букв
        190: # уезжает в 3.2× и лockup становится нижне-тяжёлым); вертикальный зазор взят в той
        191: # же доле от высоты выносного элемента, что и горизонтальный (0.3608/0.5227).
        197: """Вертикальный лockup: иконка сверху, «mindfork» под ней по центру."""
        208: # запас снизу под выносной вылет 'o'/'d' (-10 единиц), иначе их срежет viewBox
        213: """Горизонтальный лockup + тэглайн под словом, выключенный по его ширине."""
        221: # Тэглайн: подбираем кегль и трекинг так, чтобы строка выключилась ровно по
        222: # ширине вордмарка (классический приём — тэглайн «под словом», край в край).
        228: # ширина = (n-1)*(600+track) + xMax_last - xMin_first, решаем относительно track
        259: # Консоль Windows по умолчанию cp1252/cp866 — русский вывод её роняет.
        266: ap = argparse.ArgumentParser(description="Сборка вордмарков mindfork (текст → кривые)")
        267: ap.add_argument("--font", help="путь к JetBrainsMono-ExtraBold.ttf")
        268: ap.add_argument("--tag-font", help="путь к JetBrainsMono-Bold.ttf (тэглайн)")
        276: print(f"шрифт:   {path}")
        277: print(f"тэглайн: {tag_path}")
        293: print(f"  ✓ {name}  ({len(content)} байт)")
=====
   67  src/app/runtime/input.rs
        1: //! Runtime — батчинг ввода и вставка из буфера (Windows-путь): коалесинг клавиш, Chunk, сверка с бу
        2: //! runtime.rs (см. docs/history/refactoring-god-objects.md, этап 7).
        6: /// Порог «всплеска» событий за один дренаж, при котором подозреваем вставку и
        7: /// добираем её хвост (см. `run_loop`). Человек не набирает столько за один
        8: /// zero-timeout-дренаж — несколько событий разом означают вставку.
        11: /// Пауза-детектор конца вставки. На Windows крупная вставка приходит несколькими
        12: /// порциями через консольный буфер, и петля дренирует их за разные итерации — на
        13: /// стыке порций серия символов рвалась бы, а одиночный `Enter` на стыке уезжал бы
        14: /// как отправка. Пока события идут с зазором < этого порога — считаем их одной
        15: /// вставкой; реальный ввод человека имеет паузы много больше (>100мс реакции).
        18: /// Сверяет реконструированную из key-событий вставку с буфером обмена и, если это та
        19: /// же вставка, возвращает её полную версию из буфера (с восстановленными эмодзи).
        21: /// **Зачем (Windows):** при вставке из буфера обмена консоль Windows доставляет текст
        22: /// как обычные key-события, а crossterm 0.29 **теряет** символы supplementary-плоскости
        23: /// (эмодзи вроде 😊, U+1F60A): они кодируются UTF-16 суррогатной парой, а записи
        24: /// key-down/key-up консоли ломают сборку пары в crossterm — символ пропадает ещё до
        25: /// нашего слоя. BMP-символы (буквы, ❤ U+2764, селектор U+FE0F) проходят. Поэтому
        26: /// реконструкция = вставка без supplementary-эмодзи.
        28: /// Чтобы вернуть эмодзи, читаем буфер обмена и сверяем: если выбросить из него ровно те
        29: /// символы, что теряет консоль (кодпойнты > U+FFFF), и нормализовать переводы строк/
        30: /// табы, совпадает ли он с реконструкцией? Совпал → это та же вставка, отдаём полный
        31: /// текст буфера. Не совпал (буфер устарел/не та вставка/недоступен) → реконструкцию
        32: /// (без эмодзи, но без риска вставить чужое). На не-Windows — тождественно (там приходит
        33: /// корректная bracketed-вставка `Event::Paste`).
        45: /// На не-Windows вставка приходит корректным UTF-8 (`Event::Paste`) — сверка не нужна.
        54: /// Совпадает ли буфер обмена с реконструкцией вставки по «BMP-проекции» (чистая
        55: /// функция, тестируема без буфера/терминала). Из буфера выбрасываются символы, которые
        56: /// консоль Windows теряет (кодпойнты > U+FFFF — суррогатные пары), затем обе стороны
        57: /// нормализуются по переводам строк/табам (как [`InputBox::insert_str`]). См.
        73: /// Кладёт событие в пачку, отбрасывая key-события «отпускания»/повтора: приложение
        74: /// их и так игнорирует (`handle_key` берёт только `Press`), а при коалесинге вставки
        75: /// они разрывали бы серию символов между нажатиями (на Windows вставка идёт как
        76: /// пары down/up). Мышь/ресайз/`Paste` пропускаются как есть.
        86: /// Символ для коалесинга вставки: печатная клавиша / Enter / Tab без Ctrl/Alt.
        87: /// `Enter → '\r'` (CRLF из буфера затем схлопывается в `InputBox::insert_str`),
        88: /// `Tab → '\t'`. Возвращает `None` для всего остального (стрелки, Ctrl-шорткаты,
        89: /// функц. клавиши) — оно разрывает серию вставки.
        106: /// Кусок пачки ввода: либо собранная вставка (серия текстовых клавиш ≥2), либо
        107: /// одиночное событие (обычная клавиша/мышь/unix-`Paste`).
        113: /// Разбивает пачку событий на куски, коалесируя подряд идущие текстовые клавиши
        114: /// (см. [`paste_char`]) в одну вставку, если их ≥2. Чистая функция — тестируема без
        115: /// экрана/терминала. Серия длиной 1 (обычный ввод символа / одиночный Enter)
        116: /// остаётся одиночным событием, чтобы Enter работал как отправка.
        139: // Серия из одной клавиши — отдаём обычным событием (ниже).
        146: /// Обрабатывает пачку терминальных событий за один проход. Коалесированные вставки
        147: /// идут в активный редактор (экран настроек) или в поле ввода чата текстом — без
        148: /// отправки, даже если содержат переводы строк. Одиночные события — обычным путём.
        149: /// Возвращает `true`, если запрошен выход.
        160: // Вставка из буфера: на экране настроек — в активный редактор поля; в
        161: // чате — в поле ввода (никогда не отправляет); в списке цели вставки нет.
        162: // `Chunk::Paste` — реконструкция из key-событий (Windows); сверяем её с
        163: // буфером обмена, чтобы восстановить потерянные crossterm эмодзи
        164: // supplementary-плоскости (см. [`reconcile_paste`]). `Event::Paste` —
        165: // настоящая bracketed-вставка (unix), уже корректный UTF-8.
        167: // `Chunk::Paste` — реконструкция из key-событий (Windows); сверяем её с
        168: // буфером, чтобы восстановить эмодзи (см. [`reconcile_paste`]).
        169: // Маршрутизацию по экранам держит `ActiveScreen::handle_paste`.
        174: // Снимаем намерение из активного экрана (борроу заканчивается на
        175: // owned-значении `AnyIntent`), затем диспетчеризуем одним владением —
        176: // иначе конфликт заимствований `active`/`screen`.
        186: // Копирование выделения в буфер обмена (`Ctrl+C`/`Ctrl+X`) —
        187: // side-effect UI-слоя: текст уже у нас, в оркестратор не идём.
        188: // Слот `arboard` есть тут (в `dispatch` его нет). Успех молчалив,
        189: // сбой (headless-Linux без X11) — заметкой в ленту.
        193: .push_error(&format!("Не удалось скопировать в буфер обмена: {e}"));
        204: // Колесо мыши прокручивает ленту чата. На списке/настройках (своя
        205: // навигация) прокрутку игнорируем.
=====
   63  src/app/orchestrator/engines.rs
        1: //! [`EngineManager`] — жизненный цикл серверов инференса/эмбеддингов: владеет
        2: //! движками (`backend`/`imp_backend`/`embedder`), опорами на managed-процессы
        3: //! (`*_handle`, `kill_on_drop`), статусами готовности и каналами фонового probe.
        4: //! Выделен из оркестратора (Фаза 3): группирует ~11 полей и серверную логику в
        5: //! когезивную единицу, пара к трейту [`ServerSupervisor`]. Оркестратор остаётся
        6: //! единственным владельцем `Chat`; здесь — только серверы, без доменного состояния.
        22: /// Расшифровывает сохранённый ключ провайдера (запись **этой** машины). `None` —
        23: /// локальный режим (провайдера нет), ключ не сохранён или запись чужая → супервайзер
        24: /// возьмёт env-фолбэк. Резолв живёт здесь, чтобы супервайзер не знал про формат
        25: /// хранения секретов (`shared::secrets`). См. docs/research/api-key-storage.md.
        31: /// Супервайзер серверов (для перезапуска при смене модели/сервера).
        33: /// Движок ассистента. `None` — external/не настроен.
        35: /// Опора на managed chat-процесс (drop → kill).
        37: /// Опора на managed embedding-процесс.
        39: /// Движок имперсонации для режимов managed/external (`None` в режиме `shared` —
        40: /// тогда используется `backend` ассистента). См. spec §11.8.
        42: /// Опора на managed-процесс сервера имперсонации.
        44: /// Текущий статус chat-сервера. Генерация стартует только в `Ready`: запрос к
        45: /// ещё загружающемуся (`Connecting`) managed-серверу вернул бы 503 («error
        46: /// status»), а для перегенерации — ещё и снёс бы прежний ответ впустую.
        48: /// Статус сервера имперсонации (для managed/external; в `shared` —
        49: /// `NotConfigured`, чип в строке статуса скрыт).
        51: /// Статус сервера эмбеддингов. Probe пока нет (RAG ленив) — двухзначен:
        52: /// `Ready` (эмбеддер настроен) / `NotConfigured` (`UnavailableEmbedder`).
        54: /// Канал статуса chat-сервера для фонового probe супервайзера.
        56: /// Канал статуса сервера имперсонации (фоновый probe).
        58: /// Токен инвалидации фонового probe текущего chat-сервера: смена режима/модели
        59: /// помечает прежний probe устаревшим, чтобы его поздний результат (напр. таймаут
        60: /// промежуточного external при перещёлкивании managed→external→openai) не
        61: /// перезаписал статус нового сервера.
        63: /// Токен инвалидации фонового probe сервера имперсонации (аналогично).
        65: /// Источник эмбеддингов для RAG (выделенный сервер — ADR 0002).
        70: /// Создаёт менеджер без поднятых серверов (статусы `NotConfigured`, эмбеддер —
        71: /// [`UnavailableEmbedder`]). Серверы поднимаются последующими `apply_*`.
        95: /// (Пере)поднимает chat-сервер по настройкам: гасит прежний managed-процесс,
        96: /// просит супервайзер настроить новый, сохраняет немедленный статус. Снимок
        97: /// статусов для UI вызывающий берёт через [`Self::statuses`]. `api_keys` —
        98: /// сохранённые ключи из конфига (см. [`stored_key`]).
        105: self.chat_handle = None; // drop старого managed-процесса (kill_on_drop)
        106: // Инвалидируем probe прежнего сервера и заводим новый токен.
        125: /// (Пере)поднимает embedding-сервер по настройкам.
        135: /// (Пере)поднимает сервер имперсонации. В режиме `shared` отдельный сервер не
        136: /// нужен — переиспользуется chat-сервер ассистента.
        143: self.imp_handle = None; // drop прежнего managed-процесса (kill_on_drop)
        144: // Инвалидируем probe прежнего сервера имперсонации (как у chat-сервера).
        171: /// Обновляет статус chat-сервера (из фонового probe).
        176: /// Обновляет статус сервера имперсонации (из фонового probe).
        181: /// Снимок статусов всех серверов для строки статуса (чат всегда, эмбеддинги/
        182: /// имперсонация — чипами, скрытыми при `NotConfigured`). См. spec §11.1.
        191: /// Возвращает движок ассистента, если chat-сервер готов (`Ready`); иначе — `Err`
        192: /// с понятным текстом (не настроен / ещё подключается / недоступен). Сам ничего
        193: /// не эмитит — вызывающий решает, куда направить ошибку. См. spec §7.
        199: .ok_or_else(|| "LLM-сервер не настроен".to_string()),
        201: Err("Сервер ещё подключается — дождитесь готовности и повторите".into())
        203: ServerStatus::NotConfigured => Err("LLM-сервер не настроен".into()),
        204: ServerStatus::Disconnected(reason) => Err(format!("Сервер недоступен: {reason}")),
        208: /// Возвращает движок имперсонации, если он готов; иначе — `Err` с понятным
        209: /// текстом. В режиме `shared` используется chat-сервер ассистента.
        221: .ok_or_else(|| "Сервер имперсонации не настроен".to_string()),
        223: Err("Сервер имперсонации ещё подключается — повторите позже".into())
        225: ServerStatus::NotConfigured => Err("Сервер имперсонации не настроен".into()),
        227: Err(format!("Сервер имперсонации недоступен: {reason}"))
        232: /// Клон источника эмбеддингов для фоновых задач RAG / контекста инструментов.
=====
   56  src/app/orchestrator/self_consolidation.rs
        1: //! Авто-консолидация «модели себя» («сон» модели себя, этап A1): каждые N ответов
        2: //! ассистента в чате фоновая задача просит модель пересмотреть свою «модель себя» и
        3: //! **самой** её консолидировать — слить дубли наблюдений (`@self`-заметок), сжать
        4: //! раздутое описание (`summary`), связать противоречия. Как авто-рефлексия и
        5: //! авто-консолидация заметок, это **мини agentic-loop**: модель вызывает
        6: //! self-model/note-инструменты, петля их исполняет (пишут напрямую в `Storage`). Чат не
        7: //! мутируется, в UI ничего не стримится — «сон» молчалив и опционален
        8: //! (`config.self_model.auto_consolidate_every`, по умолчанию выкл). Отдельный тумблер
        9: //! от авто-рефлексии: гейты/данные модели себя и заметок уже разведены, свой счётчик
        10: //! точнее. См. docs/history/self-model-consolidation.md (этап A1).
        27: /// Потолок токенов ответа на раунд «сна» модели себя (с запасом на «мысли» перед вызовом).
        29: /// Лимит раундов мини agentic-loop «сна» модели себя (бэкстоп от зацикливания).
        31: /// Лимит времени на всю консолидацию модели себя.
        34: /// Инструменты, доступные «сну» модели себя (пересекаются с набором профиля). Над
        35: /// наблюдениями-заметками (`@self`): переписать почти-дубль (`note_revise`), заместить
        36: /// со «шрамом» (`note_supersede`), слить (`note_merge`); граф — связать
        37: /// противоречащие/уточняющие (`note_link`/`note_neighbors`). Плюс `update_self_model`
        38: /// (сжать раздутый `summary`) и `update_user_model` (привести собеседника).
        39: /// `get_self_model` даёт полные id наблюдений и текущий `summary`. `note_recall`
        40: /// **не даём** — он скрывает `@self`; полные id модель берёт из `get_self_model`
        41: /// (как рефлексия). См. docs/history/self-model-consolidation.md (этап A1).
        53: /// Системное сообщение фоновой консолидации модели себя: обрамление + единый
        54: /// `POLICY_CORE` (те же правила ведения, что у протокола/рефлексии). Строится в
        55: /// рантайме, поскольку склеивает `const`-фрагмент с константой правил.
        64: /// Вызывается после успешной генерации (`handle_done`): считает ответы ассистента
        65: /// и при достижении порога запускает фоновую консолидацию «модели себя». Тихо
        66: /// ничего не делает, если фича выключена, профиль не включил инструменты модели
        67: /// себя, «сон» уже идёт, нечего консолидировать (наблюдений < 2 и `summary` не
        68: /// раздут) или сервер не готов. Счётчик каденции сбрасывается **только при
        69: /// фактическом спавне** — пропуск по гейту не теряет накопленный цикл.
        77: let lang; // язык служебного каркаса профиля (ось A)
        90: // Гейт: профиль включает инструменты модели себя (как и инъекция/рефлексия).
        107: // Счётчик ответов с прошлого «сна»: инкремент; если порог не достигнут — выходим
        108: // (счётчик копится дальше). Сброс — только при фактическом спавне (ниже), чтобы
        109: // пропуск по гейту не терял накопленный цикл.
        118: return; // уже идёт — пропускаем без сброса (повторим на след. ходу)
        121: // Нечего консолидировать? Сигнал есть, если наблюдений (`@self`) ≥ 2 (обзор
        122: // self-консолидации непуст) ИЛИ описание себя раздуто сверх ориентира. Иначе —
        123: // выходим без сброса счётчика (повторим позже).
        143: // Сервер готов? Иначе тихо пропускаем (счётчик не сброшен).
        147: // Все гейты пройдены — сбрасываем счётчик и запускаем.
        150: // Дайджест: обзор self-консолидации (похожие пары наблюдений / contradicts / без
        151: // связей; `None` при наблюдениях < 2) + подсказка о раздутом описании (если есть).
        157: // Гейт выше это исключает; защитно — нечего консолидировать.
        161: // Токен отмены — до контекста: его клон едет в `ToolContext.cancel`.
        188: // Спавним задачу и фиксируем слот (флаг «идёт» + тихий индикатор в статус-баре).
        198: label: "авто-консолидация себя",
        202: // A2: семантика summary↔наблюдения (эмбеддинг абзацев summary на лету в
        203: // задаче). См. docs/history/self-model-consolidation.md §A2.
        219: /// Референсная локаль (ru) для ассертов на русские подстроки (пинят ru-бандл).
        227: // Собрано из единого POLICY_CORE (те же правила, что у протокола ведения).
        229: // Плюс специфичное обрамление «сна» модели себя.
        235: /// Per-language (§3.5 docs/history/i18n.md): системное сообщение собирается на КАЖДОМ
        236: /// вшитом языке, встраивает `policy_core` того же языка, плейсхолдер `{core}`
        237: /// подставлен (без остатка), несёт tool-имена (стабильны, не переводятся).
        262: // Наблюдения (граф/слияние) + сжатие summary — есть; note_recall нет (скрывает @self).
=====
   55  src/app/orchestrator/settings.rs
        1: //! Конфигурация приложения и (пере)запуск серверов инференса/эмбеддингов через
        2: //! [`ServerSupervisor`]. Оркестратор — единственный писатель в `settings.json`.
        10: /// Применяет правки конфигурации: сохраняет, перезапускает сервер/реестр при
        11: /// необходимости и переэмитит настройки. Единственный писатель в `settings.json`.
        14: // «Последний открытый чат» — свойство оркестратора, а не редактируемая на
        15: // экране настроек величина. Снимок конфига из UI может нести устаревшее
        16: // значение (например `None` со старта) — сохраняем актуальное, чтобы правка
        17: // настроек не стёрла память о чате.
        19: // Сохранённые API-ключи — тоже свойство оркестратора (`handle_set_api_key`):
        20: // экран настроек шлёт сам ключ отдельной командой, а в снимке конфига их
        21: // не несёт. Без восстановления правка любой настройки стёрла бы ключи.
        23: // TOFU-пины каталогов MCP — тоже свойство оркестратора (persist_mcp_pin),
        24: // в UI не редактируются: наследуем по id сервера, если снимок из UI их не
        25: // несёт (устаревшая копия) — правка настроек не сбрасывает доверие и не
        26: // провоцирует ложный diff `config.mcp` (лишний рестарт серверов).
        39: self.config = old; // откат к прежнему состоянию
        42: // Смена настроек chat-сервера (модель/режим/порт/…) — перезапуск (spec
        43: // §11.6), но отложенный: экран настроек применяет правку при коммите
        44: // каждого поля, и дебаунс коалесит серию быстрых правок в один рестарт
        45: // ([`super::restart_queue::RestartQueue`], флаш по дедлайну в петле).
        48: // Смена режима меняет набор доступных параметров семплинга
        49: // (get_sampling/set_sampling) — пересобираем реестр под новый
        50: // провайдер немедленно (дёшево, in-memory; схема должна быть
        51: // актуальна уже со следующего хода).
        56: // Смена настроек сервера имперсонации — отложенное пере-подключение.
        60: // Смена настроек embedding-сервера — отложенное пере-подключение.
        64: // Смена настроек MCP-серверов — отложенное пере-поднятие (дебаунс, как
        65: // движки): гашение/спавн процессов — дорогая операция.
        69: // Смена параметров инструментов — пересборка реестра (python_path, лимиты).
        76: /// Сохраняет введённый в настройках API-ключ провайдера: шифрует машинным
        77: /// ключом (`shared::secrets`) и кладёт в `config.api_keys` записью **этой**
        78: /// машины. Пустой ключ — удаление. Затрагивает все слоты (чат/имперсонация/
        79: /// эмбеддинги), у которых активен этот провайдер: они помечаются на отложенный
        80: /// (пере)подъём — ключ подхватится следующим `flush_restarts`.
        82: /// Плейнтекст живёт только в аргументе и в HTTP-клиенте: на диск уходит
        83: /// шифротекст, в UI-снимок конфига ключи не попадают вовсе (UI получает лишь
        84: /// признак «настроен»). См. docs/research/api-key-storage.md.
        111: self.config.api_keys = old; // откат к прежнему состоянию
        114: // Пере-поднимаем только те серверы, чей активный провайдер сменил ключ
        115: // (дебаунс тот же, что у правок движка — см. `flush_restarts`).
        129: /// Применяет отложенные дебаунсом (пере)запуски серверов (дедлайн истёк):
        130: /// по одному `apply_*` на каждый помеченный сервер и один общий снимок
        131: /// статусов. Читает **финальный** `self.config` — конфиг заменяется ещё при
        132: /// правке, так что серия правок даёт один рестарт с итоговыми значениями.
        159: /// (Пере)поднимает chat-сервер по `config.engine` и эмитит снимок статусов.
        160: /// Немедленный путь стартового подъёма (до петли `run`); правки настроек
        161: /// идут через дебаунс-очередь `restarts` → [`Self::flush_restarts`].
        169: /// (Пере)поднимает embedding-сервер по `config.embed` и эмитит снимок статусов
        170: /// (чип эмбеддингов в строке статуса появляется/исчезает по настройке).
        177: /// (Пере)поднимает сервер имперсонации по `config.impersonation_engine`. В режиме
        178: /// `shared` отдельный сервер не нужен — переиспользуется chat-сервер ассистента.
        189: /// (Пере)поднимает MCP-серверы по `config.mcp`: прежние гасятся, включённые
        190: /// спавнятся заново; их инструменты придут событиями `Ready` (см.
        191: /// [`super::mcp::McpManager`]). Реестр пересобирается сразу — обёртки прежнего
        192: /// поколения (мёртвые соединения) уходят из него немедленно.
=====
   45  src/app/orchestrator/impersonation.rs
        1: //! Имперсонация (`Ctrl+U`, spec §11.8): модель пишет следующее сообщение «за
        2: //! пользователя». Системное сообщение заменяется на имперсонационное, роли
        3: //! user/assistant в истории меняются местами. Фоновая задача стримит реплику.
        22: /// Лимит времени на одну имперсонацию (страховка от зависшей задачи). Щедрый:
        23: /// на медленном локальном `llama-server` одна только обработка промпта может занять
        24: /// ~минуту, плюс генерация на CPU идёт ~5 ток/с — при 120с реплику резало на полуслове
        25: /// (см. лог `srv stop: cancel task` ровно на 120с). По таймауту накопленный текст
        26: /// **не теряется** (отдаётся в поле как при достижении лимита) — см. `spawn_impersonation`.
        30: /// Пишет сообщение от лица пользователя (имперсонация, `Ctrl+U`, spec §11.8):
        31: /// системное сообщение ассистента заменяется на имперсонационное из профиля, а
        32: /// роли user/assistant в истории меняются местами — модель продолжает диалог
        33: /// «за пользователя». Текст стримится в предпросмотр поля ввода. Игнорируется
        34: /// во время генерации/другой имперсонации.
        64: // Модель собеседника подмешивается в промпт имперсонации (агент пишет ЗА
        65: // человека) — но только если профиль включил модель себя (тот же opt-in-гейт,
        66: // что у пассивной инъекции в обычный ход).
        114: /// Отменяет текущую имперсонацию (`Esc` в предпросмотре). Завершение придёт
        115: /// через `imp_done` и эмитит `ImpersonationFinished{Cancelled}`.
        122: /// Завершение фоновой задачи имперсонации: чистит состояние и эмитит финал.
        136: /// Строит запрос имперсонации (spec §11.8): системное сообщение — имперсонационное
        137: /// (персона пользователя), роли user/assistant в истории меняются местами (модель
        138: /// продолжает диалог «за пользователя»). Инструментов нет. Если `seed` не пуст,
        139: /// модель просят продолжить уже начатый текст.
        148: // Имперсонация пишет реплику в поле ввода и **отбрасывает** «мысли» (Thoughts
        149: // в `spawn_impersonation` игнорируются), поэтому reasoning ей не нужен. Ключевое
        150: // — `reasoning_budget=0`: для моделей со «вшитым» в шаблон thinking (Gemma
        151: // `peg-gemma4`, Qwen) только он реально гасит «мысли» (+ `enable_thinking=false`
        152: // в `wire.rs`); поля `thinking`/`reasoning_effort` сервер для них игнорирует.
        153: // Без этого модель тратила весь бюджет токенов на reasoning_content, а ответный
        154: // `content` приходил пустым — предпросмотр оставался пустым (тот же класс бага,
        155: // что у авто-названия чата, см. `title.rs`).
        159: // Подсказка о собеседнике (модель того, за кого пишем) — перед seed-продолжением.
        178: /// Меняет роль сообщения местами для имперсонации (user↔assistant). System/Tool и
        179: /// пустые сообщения отбрасываются (в режиме имперсонации инструментов нет).
        191: /// Запускает фоновую задачу имперсонации: стримит текст реплики в предпросмотр
        192: /// (`ImpersonationChunk`), по завершении/таймауту/отмене шлёт `(id, reason)` в
        193: /// `done_tx`. «Мысли» и tool-вызовы игнорируются (в поле ввода идёт только текст).
        227: // Различаем отмену пользователем и таймаут: при отмене (`Esc`) поток внутри
        228: // `run` ловит `cancel.cancelled()` и сам отдаёт `Finished(Cancelled)` — она
        229: // приходит сюда как `Ok(Ok(Cancelled))` и приводит к отбрасыванию текста
        230: // (пользователь передумал). Таймаут — это `Err(_)`: серверную задачу мы
        231: // прерываем (`cancel.cancel()`), но накопленный текст **сохраняем**, отдавая
        232: // его как `Length` (модель писала валидную реплику, просто медленно). Прежний
        233: // безусловный `if cancel.is_cancelled() { Cancelled }` оба случая сводил к
        234: // отбрасыванию — из-за этого реплика, обрезанная таймаутом, исчезала.
=====
   44  src/app/orchestrator/tool_loop.rs
        1: //! Общий «тихий» agentic-loop фоновых задач (авто-рефлексия «модели себя» и
        2: //! авто-консолидация заметок). Обе задачи — мини agentic-loop без стриминга в UI:
        3: //! стрим → аккумулятор вызовов → исполнение разрешённых инструментов → следующий
        4: //! раунд; толерантны к `Thoughts`/`ThoughtsSignature`/`Usage` (игнор). Раньше это
        5: //! тело дублировалось в `reflection.rs` и `consolidation.rs` дословно (различались
        6: //! лишь лимиты и метка лога) — здесь оно одно. Основную петлю генерации сознательно
        7: //! **не** трогаем: у неё стриминг в UI, control-flow-инструменты, thinking-подписи
        8: //! Anthropic, usage, эффекты — её сложность не окупает общий сток сейчас.
        27: /// Async-надстройка дайджеста фоновой задачи (раздел A2): семантическое сравнение
        28: /// абзацев описания себя (`summary`) с наблюдениями (`@self`). Вычисляется **в
        29: /// задаче** до петли — эмбеддинг абзацев summary недоступен в синхронном хендлере
        30: /// оркестратора. См. docs/history/self-model-consolidation.md §A2.
        38: /// Пора ли запускать периодическую фоновую задачу: фича включена (`every > 0`) и
        39: /// накоплено достаточно ответов. Чистая функция — тестируема. Общая для рефлексии и
        40: /// консолидации.
        45: /// Параметры запуска тихой фоновой задачи.
        51: /// Разрешённые инструменты (защита от вызова чего-то вне набора задачи).
        54: /// Бэкстоп от зацикливания (число раундов).
        56: /// Лимит времени на всю задачу.
        58: /// Метка для диагностических логов («авто-рефлексия»/«авто-консолидация»).
        60: /// Профиль (для логов).
        62: /// Вид задачи — уходит в `done_tx` вместе с исходом (петля разбирает одной веткой).
        64: /// Единый канал исхода: `(вид, Ok(()))` при успехе, `(вид, Err(причина))` при
        65: /// ошибке/таймауте.
        67: /// Опциональная async-надстройка дайджеста, вычисляемая в задаче ДО петли
        68: /// (эмбеддинг абзацев summary недоступен в синхронном хендлере): результат
        69: /// дописывается к первому user-сообщению запроса. См.
        74: /// Запускает тихую фоновую задачу: мини agentic-loop под таймаутом. По завершении
        75: /// шлёт исход в `done_tx` (снять флаг «идёт …» и вести наблюдаемость: серия неудач →
        76: /// одна ошибка в UI). Инструменты пишут напрямую в `Storage`; чат/лента не трогаются.
        95: // A2: async-надстройка дайджеста (семантика summary↔наблюдения) — считаем ДО
        96: // петли и дописываем к первому user-сообщению (эмбеддинг абзацев summary в
        97: // синхронном хендлере недоступен). См. docs/history/self-model-consolidation.md §A2.
        123: tracing::warn!(%profile_id, "{label}: ошибка: {e}");
        128: tracing::warn!(%profile_id, "{label}: превышен лимит времени");
        129: Err("превышен лимит времени".to_string())
        136: /// Тело мини agentic-loop: раунды стрим→вызовы→исполнение до `max_rounds` или
        137: /// первого раунда без вызовов. Разрешает только инструменты из `allowed`.
        166: // Раунд без вызовов или достигнут лимит — задача окончена.
        181: Err(e) => format!("Ошибка инструмента {}: {e}", call.name),
        184: format!("Инструмент {} недоступен.", call.name)
        198: assert!(!due(5, 0)); // выключено
        201: assert!(due(3, 3)); // достигли порога
        202: assert!(due(4, 3)); // и выше
   41  .github/workflows/release.yml
        1: # Релиз: по тегу `vX.Y.Z` собирает бинарники (Windows + Linux glibc 2.35),
        2: # упаковывает архивы + sha256sums.txt и публикует GitHub Release с нотами из
        3: # соответствующего раздела CHANGELOG.md. См. docs/release-engineering.md §3.5.
        5: # Тег ставит пользователь после мержа релизного PR (bump версии + CHANGELOG).
        16: # Сборка бинарника на каждой платформе; упаковка централизована в job release
        17: # (на Linux есть и tar, и zip — избегаем различий шелла Windows).
        19: name: Сборка (${{ matrix.artifact }})
        24: - os: ubuntu-22.04       # старая glibc 2.35 — бинарь работает на большинстве дистрибутивов
        33: - name: Установить тулчейн (из rust-toolchain.toml)
        36: # ALSA нужна только Linux-сборке (rodio/cpal — воспроизведение речи /tts).
        37: - name: Системные зависимости (ALSA)
        40: - name: Сборка релиза
        42: - name: Выгрузить бинарник
        49: # Linux-пакеты из уже собранного бинарника (bin-linux) через nfpm.
        51: name: Linux-пакеты (deb/rpm/archlinux)
        56: - name: Скачать бинарник Linux
        61: - name: Установить nfpm
        67: - name: Собрать пакеты
        69: VERSION="${GITHUB_REF_NAME#v}"   # напр. v0.9.0 → 0.9.0
        81: # Windows-инсталлятор (Inno Setup) из уже собранного бинарника (bin-windows).
        83: name: Windows-инсталлятор (Inno Setup)
        88: - name: Скачать бинарник Windows
        93: - name: Установить Inno Setup
        95: - name: Скомпилировать инсталлятор
        104: if (-not $iscc) { throw "ISCC.exe не найден" }
        113: name: Публикация релиза
        117: contents: write        # нужно gh release create
        120: - name: Скачать бинарники
        124: - name: Собрать архивы + sha256 + ноты
        127: TAG="${GITHUB_REF_NAME}"        # напр. v0.9.0
        131: # Общий набор доков в каждый архив.
        134: # Словари спелл-чека кладём в data/dictionaries/ — портативную раскладку,
        135: # которую приложение читает рядом с бинарником (data/ рядом с exe → spell-чек
        136: # из коробки). См. build.rs / shared/paths.rs.
        157: # Linux-пакеты (deb/rpm/archlinux) и Windows-инсталлятор, собранные
        158: # отдельными job'ами.
        162: # Контрольные суммы (по всем артефактам: архивы + пакеты + инсталлятор).
        165: # Ноты релиза = раздел CHANGELOG для этой версии (без заголовка).
        172: echo "Релиз ${TAG}." > notes.md
        175: echo "Артефакты:"; ls -l dist
        176: - name: Создать GitHub Release
=====
   39  src/app/orchestrator/title.rs
        1: //! Авто-название чата (spec §11.2): фоновая задача просит модель придумать
        2: //! короткий заголовок по переписке; результат применяется к чату в петле.
        18: /// Потолок токенов ответа при генерации авто-названия. Пытаемся выключить «мысли»
        19: /// (`reasoning_budget=0` + `chat_template_kwargs.enable_thinking=false`), но
        20: /// некоторые модели (вшитый в GGUF thinking, напр. Gemma `peg-gemma4`) их
        21: /// игнорируют и всё равно «рассуждают» сотни токенов перед ответом — поэтому
        22: /// бюджет щедрый, чтобы модель успела завершить «мысли» и выдать заголовок.
        25: /// Лимит времени на генерацию авто-названия чата (с запасом на «думающие» модели).
        28: /// Результат фоновой задачи авто-названия чата (внутренний канал).
        31: /// Сырой текст ответа модели (или сообщение об ошибке для показа в UI).
        36: /// Авто-название чата (spec §11.2): модель читает переписку (или её начало и
        37: /// конец, если она длинная) и придумывает короткий заголовок. Запрос идёт
        38: /// фоновой задачей; результат прилетает в [`Orchestrator::handle_title_result`].
        39: /// Чат-сервер должен быть готов (`Ready`) — иначе понятная ошибка.
        44: // Язык служебного каркаса — из профиля чата (ось A): дайджест и системное
        45: // сообщение авто-названия локализуются им (заголовок всё равно просят «на
        46: // языке переписки», см. `prompt.title.system`). Ошибки — для человека (ось B),
        47: // остаются на русском.
        60: // Авто-название — операция списка чатов: ошибку готовности сервера
        61: // показываем в оверлее списка, а не в ленте чата (где её скрыл бы
        62: // полноэкранный оверлей).
        67: // Свежий компактный семплинг (не наследуем override чата): короткий ответ,
        68: // умеренная температура, reasoning выключен (заголовку «мысли» не нужны и
        69: // только съедают бюджет токенов), без инструментов. Ключевое — `reasoning_
        70: // budget=0`: для моделей со «вшитым» в шаблон thinking (Gemma `peg-gemma4`,
        71: // Qwen) только он реально гасит «мысли»; поля `thinking`/`reasoning_effort`
        72: // сервер для таких шаблонов игнорирует (иначе модель тратила весь бюджет на
        73: // «мысли» и ответный текст приходил пустым).
        97: /// Применяет результат фоновой генерации авто-названия: чистит/нормализует
        98: /// заголовок и переименовывает чат (или показывает ошибку).
        125: /// Запускает фоновую задачу авто-названия чата: один независимый запрос к модели
        126: /// (без истории/инструментов), сбор текста, отправка результата в `title_tx`.
        127: /// Лимит времени — [`TITLE_TIMEOUT`].
        144: // Копим «мысли» как запасной источник: если модель так и не
        145: // «завершила мысль» (выдала только reasoning), вытащим заголовок
        146: // из последней содержательной строки рассуждений.
        168: /// Выбирает сырой источник заголовка: основной ответ модели, а если он пуст
        169: /// (модель не «завершила мысль» в рамках бюджета) — последнюю содержательную
        170: /// строку рассуждений. Финальную нормализацию делает `clean_generated_title`.
=====
   31  src/app/orchestrator/consolidation.rs
        1: //! Авто-консолидация заметок («сон», Ярус 3): каждые N ответов ассистента в чате
        2: //! фоновая задача просит модель пересмотреть базу знаний и **самой** её
        3: //! консолидировать — слить дубли, переписать/заместить устаревшее, связать
        4: //! родственное. Как и авто-рефлексия, это **мини agentic-loop**: модель вызывает
        5: //! note-инструменты, петля их исполняет (пишут напрямую в `Storage`). Чат не
        6: //! мутируется, в UI ничего не стримится — консолидация молчалива и опциональна
        7: //! (`config.notes.auto_consolidate_every`, по умолчанию выкл).
        8: //! См. docs/history/notes-connectivity.md (Ярус 3).
        24: /// Потолок токенов ответа на раунд консолидации (с запасом на «мысли» перед вызовом).
        26: /// Лимит раундов мини agentic-loop консолидации (бэкстоп от зацикливания).
        28: /// Лимит времени на всю консолидацию.
        31: /// Инструменты, доступные консолидации (пересекаются с набором профиля).
        42: /// Вызывается после успешной генерации (`handle_done`): считает ответы ассистента
        43: /// и при достижении порога запускает фоновую консолидацию. Тихо ничего не делает,
        44: /// если фича выключена, профиль не включил инструменты заметок, консолидация уже
        45: /// идёт, активных заметок меньше двух или сервер не готов.
        53: let lang; // язык служебного каркаса профиля (ось A)
        66: // Гейт: профиль включает консолидацию (note_merge — ядро операции).
        83: // Счётчик ответов с прошлой консолидации: инкремент; если порог не достигнут —
        84: // выходим (счётчик копится дальше). Сброс — только при фактическом спавне
        85: // (ниже), чтобы пропуск по гейту не терял накопленный цикл.
        94: return; // уже идёт — пропускаем без сброса (повторим на след. ходу)
        96: // Нечего консолидировать, если пользовательских заметок меньше двух (self-заметки
        97: // не в счёт — консолидация над ними не работает). Счётчик не сброшен — повторим.
        109: // Сервер готов? Иначе тихо пропускаем (счётчик не сброшен).
        113: // Все гейты пройдены — сбрасываем счётчик и запускаем.
        121: // Токен отмены — до контекста: его клон едет в `ToolContext.cancel`.
        154: // Спавним задачу и фиксируем слот (флаг «идёт» + тихий индикатор в статус-баре).
        164: label: "авто-консолидация",
        168: // Консолидация заметок — про пользовательские заметки, не про summary модели
        169: // себя; семантика summary↔наблюдения к ней неприменима.
=====
   31  src/app/orchestrator/generation.rs
        55: .send(AppEvent::Error("Нет активного чата".into()));
        510: "Достигнут лимит раундов инструментов ({max_rounds}) — свожу итог из собранного."
        513: // Счётчик токенов финального раунда `stream_round` эмитит сам
        514: // (от `base = total_*`); дальше `break`, накапливать не нужно.
        860: /// Собирает наблюдения (self-заметки) для инъекции в системный промпт (Ярус 2):
        861: /// **релевантные** последней реплике + гарантия свежайшего наблюдения, с откатом на
        862: /// чистую свежесть при недоступном эмбеддере/пустом запросе. Пусто, если инъекция
        863: /// выключена. Async (эмбеддинг запроса) — потому и вынесено из sync-обработчика в
        864: /// задачу генерации. Тестируется поверх temp-хранилища + `MockEmbedder`.
        879: // Релевантные последней реплике наблюдения; пусто → откат на свежесть.
        896: /// Подмешивает «модель себя» в системный промпт хода (SelfModel MVP, см.
        897: /// docs/history/self-model-mvp.md): компактный рендер текущей модели (если непуста) плюс,
        898: /// при `maintenance_protocol`, нейтральный к персоне протокол ведения. Возвращает
        899: /// прежний `system` без изменений, если инъекция выключена (профиль не включил
        900: /// `get_self_model`) либо подмешивать нечего (пустая модель и протокол выключен).
        901: /// Протокол подмешивается даже при пустой модели — чтобы модель начала её вести.
        902: /// Чистая функция — тестируема без движка.
        917: // Наблюдения (self-заметки) могут существовать без блоба модели — тогда рендерим
        918: // пустую модель с наблюдениями. `render_for_prompt` вернёт None, если пусто и
        919: // структурно, и по наблюдениям.
        935: // Собираем подмешиваемые части: рендер модели (если есть) + протокол (если включён).
        941: // Протокол ведения собирается из единого POLICY_CORE (этап 6) — те же
        942: // правила, что у фоновой авто-рефлексии.
        946: // Data-aware приписка: если описание себя разрослось сверх ориентира —
        947: // конкретная подсказка сократить (статичный протокол становится предметным,
        948: // когда summary действительно раздут). См. docs/summary-as-snapshot.md (этап 2).
        954: return system; // подмешивать нечего
        963: /// Доменное tool-сообщение (роль `Tool`) с привязкой к вызову.
        971: /// Финальное assistant-сообщение хода (если есть текст/мысли) со снимком
        972: /// метаданных: режим движка, имя модели и семплинг, **урезанный до полей,
        973: /// доступных в этом режиме** (движок недоступное поле не принял бы — spec §8.3).
=====
   29  src/app/orchestrator/background.rs
        1: //! Реестр слотов «тихих» фоновых задач (авто-рефлексия «модели себя» и
        2: //! авто-консолидация заметок). Обе задачи — мини agentic-loop без UI (общий раннер
        3: //! [`tool_loop::spawn_silent_loop`](super::tool_loop)); их жизненный цикл (флаг
        4: //! «идёт», серия неудач, гашение индикатора, оповещение об ошибке) раньше
        5: //! дублировался полями и обработчиками на каждую задачу. Здесь он один — ключ
        6: //! реестра — существующий [`BackgroundKind`]. Добавление задачи №3 (авто-консолидация
        7: //! «модели себя», roadmap — architecture.md §9.9) не трогает каркас `run()`/`Quit`.
        8: //! См. docs/history/refactoring-solid.md §4.
        17: /// Слот тихой фоновой задачи: токен активного запуска + серия неудач. Серия живёт
        18: /// дольше запуска (переживает завершения) — потому слот, а не отдельная задача.
        21: /// `Some` — задача идёт (одна за раз); токен отмены (для ветки `Quit`).
        23: /// Число подряд идущих неудач; на пороге [`BACKGROUND_FAILURE_ALERT`](super::BACKGROUND_FAILURE_ALE
        24: /// один раз показываем ошибку в UI, дальше молчим до первого успеха.
        29: /// Идёт ли фоновая задача этого вида (гейт «одна за раз»).
        34: /// Фиксирует запуск: слот помечается активным (`cancel = Some`) и в статус-бар
        35: /// уходит тихий индикатор «идёт …». Зовётся спавн-хвостами
        44: /// Общий обработчик исхода фоновой задачи (бывшие `handle_reflect_done`/
        45: /// `handle_consolidate_done`): снимает флаг «идёт», гасит индикатор, ведёт серию
        46: /// неудач (на пороге — одна ошибка в UI, наблюдаемость без спама). При успехе
        47: /// **рефлексии** или **консолидации «модели себя»** дополнительно шлёт
        48: /// `SelfModelChanged` (открытый экран `F3` перезапросит свежий снимок);
        49: /// консолидация *заметок* — нет (меняет заметки, не «модель себя»). Инструменты
        50: /// задачи уже записали изменения в `Storage`; чат/ленту это не трогает.
        52: // Мутируем слот и вычисляем, нужна ли ошибка-оповещение, ДО отправки событий
        53: // (заём `self.bg` не пересекается с `self.evt_tx` при отправке ниже).
        89: /// Отменяет все идущие фоновые задачи семейства (ветка `Quit`).
        98: /// Число подряд идущих неудач задачи (для тестов оповещения об ошибке).
        105: /// Человекочитаемая метка вида задачи (язык интерфейса, ось B) — из неё собираются
        106: /// тексты ошибок (**байт-в-байт** с прежними русскими формулировками).
=====
   26  src/app/orchestrator/restart_queue.rs
        1: //! [`RestartQueue`] — дебаунс-очередь отложенного (пере)запуска серверов
        2: //! инференса/эмбеддингов при правках настроек движка (зеркало `SaveQueue`).
        3: //! Экран настроек применяет правку при коммите каждого поля, поэтому серия
        4: //! «бинарник → модель → -ngl» без дебаунса давала бы три тяжёлых рестарта
        5: //! managed `llama-server` подряд. Конфиг при этом сохраняется и переэмитится
        6: //! в UI сразу (см. `Orchestrator::handle_update_config`) — откладывается только
        7: //! дорогой рестарт процесса; сам он остаётся у оркестратора
        8: //! (`Orchestrator::flush_restarts`), очередь лишь учитывает, какие серверы и
        9: //! когда перезапускать. Стартовый подъём серверов (до петли `run`) очередь не
        10: //! проходит — он немедленный.
        16: /// Пауза тишины после последней правки настроек движка до (пере)запуска.
        21: /// Ожидает ли (пере)запуска chat-сервер (`config.engine`).
        23: /// Ожидает ли (пере)запуска embedding-сервер (`config.embed`).
        25: /// Ожидает ли (пере)запуска сервер имперсонации (`config.impersonation_engine`).
        27: /// Ожидают ли (пере)поднятия MCP-серверы (`config.mcp`).
        29: /// Момент срабатывания (продлевается каждой пометкой — дебаунс от последней).
        34: /// Помечает chat-сервер для отложенного (пере)запуска и продлевает дедлайн.
        40: /// Помечает embedding-сервер для отложенного (пере)запуска и продлевает дедлайн.
        46: /// Помечает сервер имперсонации для отложенного (пере)запуска и продлевает дедлайн.
        52: /// Помечает MCP-серверы для отложенного (пере)поднятия и продлевает дедлайн.
        62: /// Текущий дедлайн (для `select!`-таймера петли). `None` — очередь пуста.
        67: /// Забирает флаги `(chat, embed, impersonation, mcp)` и сбрасывает очередь.
        83: /// Повторные пометки коалесируются: `take` отдаёт каждый сервер по одному
        84: /// разу, не помеченные не трогаются, дедлайн сбрасывается.
        103: /// Каждая пометка продлевает дедлайн — рестарт идёт от последней правки,
        104: /// а не от первой (иначе длинная серия правок словила бы рестарт посередине).
=====
   24  src/app/orchestrator/mcp.rs
        543: /// Персистит TOFU-пин каталога сервера в `config.mcp` (пишется напрямую, не
        544: /// через `handle_update_config` — иначе diff `config.mcp` пометил бы серверы
        545: /// на рестарт и цикл повторился бы). Ошибка записи не эскалируется (пин —
        546: /// защита, не данные; повторное одобрение при следующем запуске безвредно).
        571: /// Соединение поверх duplex (сервер-половина сразу закрыта — для событий
        572: /// Ready в тестах транспорт не дёргается).
        597: /// Референсная локаль тестов (тексты ru-бандла байт-в-байт).
        634: // Четвёртый в пределах окна — отказ.
        636: // За пределами окна старые отметки истекают — снова можно.
        664: // Событие чужого поколения — отброшено.
        676: // Актуальное поколение — каталог построен, статус Ready, TOFU-пин
        677: // возвращён для персиста (первое одобрение).
        691: // Полное описание сервера едет в метаданные (нижняя панель настроек).
        701: // Пин от «прежнего» каталога.
        710: // Сервер поднялся с ИЗМЕНИВШИМСЯ каталогом (иное описание) → инструменты
        711: // придержаны, статус — «каталог изменился», пина для персиста нет.
        725: // Подтверждение пользователем: инструменты регистрируются, новый хэш
        726: // возвращён для персиста.
        733: // Повторное подтверждение — нечего подтверждать.
        751: // Каталог совпал с пином → регистрация без нового персиста.
        773: // Крах: инструменты уходят из каталога, статус — Connecting (рестарт).
        784: // Исчерпание бюджета: ещё падения без Ready → Disconnected.
        815: // Выключенный сервер слота не получает; .cmd — Disconnected с причиной.
        822: // Мастер-выключатель: всё гаснет.
=====
   24  src/app/runtime/tests.rs
        1: //! Тесты runtime (батчинг ввода, chunk_batch). См. mod.rs.
        12: // Буфер «hello 😊 world» при потерянном консолью эмодзи реконструируется как
        13: // «hello  world» (😊 > U+FFFF выпал) — проекция совпадает → берём буфер.
        15: // ❤ (U+2764) и селектор U+FE0F — BMP, проходят и остаются в реконструкции.
        17: // Переводы строк нормализуются (буфер \n ↔ реконструкция \r от Enter).
        24: // Устаревший/чужой буфер не совпадает с реконструкцией → НЕ подставляем его.
        26: // Пустая реконструкция никогда не матчится (нет сигнала, что это та же вставка).
        32: // Серия «a Enter b» (как пришла бы вставка "a\nb" на Windows) → одна вставка;
        33: // Enter внутри серии становится переводом строки ('\r' схлопнется при вставке).
        49: // Серия с несколькими Enter внутри (многострочная вставка) → одна вставка,
        50: // все Enter — переводы строк, ни один не уезжает отправкой.
        65: // Одиночный Enter — это отправка, НЕ вставка.
        80: // Стрелка разрывает серию: "ab" (вставка) + стрелка (событие) + "cd" (вставка).
        109: // Ctrl/Alt-комбинации не часть вставки (Ctrl+V, Alt+… — это шорткаты).
        124: // «Отпускание» клавиши отбрасывается (иначе разрывало бы серию вставки).
        141: loader.maybe_reload(true, &[]); // те же настройки — без перезагрузки
        143: loader.maybe_reload(false, &[]); // выключили — перезагрузка
        145: loader.maybe_reload(true, &["en_US".to_string()]); // другой выбор — перезагрузка
        148: // Дожидаемся готового чекера последней загрузки (пустой каталог → выключен).
        163: // `OpenSelfModel` шлёт запрос оркестратору и НЕ открывает экран сразу —
        164: // он откроется по ответному событию `SelfModelView`.
        201: // Экран `F3` закрыт → SelfModelChanged ничего не шлёт.
        212: // Экран `F3` открыт → перезапрос снимка.
        230: // BackgroundTask ставит флаг индикатора у экрана чата (не паникует).
=====
   16  src/app/orchestrator/reflection.rs
        342: // Ярус 2: авто-рефлексии даны note_link/note_neighbors (граф над наблюдениями).
        343: // Ярус 3: + note_recall (id пользовательских заметок для кросс-органных связей).
        352: // Ярус 3: рефлексии предложено связывать наблюдение «о себе» с фактом «о
        353: // собеседнике» (кросс-органное ребро через note_recall + note_link).
        357: // Обзор self-консолидации: рефлексии указано использовать его блок.
        380: mk(base - Duration::days(5), DeletedCause::Rewrite), // до since
        385: cause: None, // старая запись без причины — не считается
        388: // since = 4 ч назад → последние 3 (2 regen + 1 delete); rewrite (5 дн.) отсечён.
        393: // since=None → учитываем всё, собственное поведение (rewrite) — отдельной фразой.
        396: // Нет сигналов → None.
        408: Message::assistant(""), // пустой ответ не считается
        412: // Без ватермарка — считаем все непустые ответы ассистента (a1,a2,a3).
        414: // Ватермарк после a2 (индекс 4): в окне только a3.
        416: // Ватермарк в конце — окно пусто.
        422: // История усечена (Ctrl+R/Ctrl+E) — ватермарк больше длины: кламп к len,
        423: // окно пусто, паники нет.
=====
   16  src/app/orchestrator/tts.rs
        451: /// Тексты реплик без ролей — для проверок, где роль не важна.
        456: /// Реплика ассистента для входа чанкера (роль там не важна, но нужна типу).
        461: /// Тексты чанков без ролей.
        479: // Порядок хронологический, и роли сохранены (для многоголосой озвучки).
        484: // Запрос больше, чем есть, отдаёт всё (кламп, а не ошибка).
        506: // Совсем нечего озвучивать — None (вызывающий покажет понятную ошибку).
        521: // Тумблер «Озвучивать роли» действует и при одиночном `/tts` (решение Р6).
        535: // Сообщение из одного код-блока даёт только пометку — она озвучивается,
        536: // а вот пустой результат экстрактора сообщение бы отбросил.
        545: // Роль реплики передаётся всем её чанкам — по ней выбирается голос.
        552: // Первые чанки — пользователя, последние — ассистента.
        566: // Границы — по предложениям (пунктуация сохранена в конце чанка).
        572: // Ничего не потеряно.
        584: // Граница сообщения — естественная пауза и точка отмены: не склеиваем даже
        585: // короткие реплики.
        600: // Одно слово длиннее лимита режется посимвольно, а не теряется.
   15  .github/workflows/ci.yml
        1: # CI: линты (fmt + clippy) на Linux + тесты на Linux и Windows.
        2: # Тулчейн берётся из rust-toolchain.toml (пин, release-engineering.md Ф4).
        3: # #[ignore]-смоуки (движок/сеть/живая модель) без env-переменных тихо пропускаются —
        4: # сети и живого сервера CI не требует. См. docs/release-engineering.md §3.3.
        12: # Отменяем предыдущие прогоны того же ref (экономия минут раннера).
        22: name: Линты (fmt + clippy)
        26: - name: Установить тулчейн (из rust-toolchain.toml)
        29: - name: Системные зависимости (ALSA — воспроизведение речи /tts)
        31: - name: Форматирование
        33: - name: Clippy (все цели, warnings = ошибки)
        37: name: Тесты (${{ matrix.os }})
        45: - name: Установить тулчейн (из rust-toolchain.toml)
        48: # ALSA нужна только Linux-сборке (rodio/cpal); на Windows — чистый windows-rs.
        49: - name: Системные зависимости (ALSA — воспроизведение речи /tts)
        52: - name: Тесты
=====
    9  src/app/runtime/clipboard.rs
        1: //! Runtime — чтение/запись системного буфера обмена (arboard-слот). Часть модуля [`super`]; разбито
        2: //! runtime.rs (см. docs/history/refactoring-god-objects.md, этап 7).
        4: /// Читает текст системного буфера обмена (лениво создавая клиент). `None`, если буфер
        5: /// недоступен/пуст/не текстовый. Используется только на Windows для восстановления
        6: /// эмодзи во вставке (см. [`reconcile_paste`]).
        15: /// Пишет текст в системный буфер обмена, создавая клиент лениво и переиспользуя
        16: /// его. Возвращает текст ошибки (вместо паники), если буфер недоступен — на
        17: /// headless-Linux без X11/Wayland конструктор `arboard` может упасть.
        25: // `unwrap` безопасен: только что гарантировали `Some`.
=====
    9  src/app/supervisor.rs
        118: let cloud = settings.cloud().expect("облачный режим");
        120: settings.mode.cloud_provider().expect("облачный режим"),
        153: let cloud = settings.cloud().expect("облачный режим");
        155: settings.mode.cloud_provider().expect("облачный режим"),
        222: tracing::warn!(error = %err, "не удалось запустить embedding-сервер; RAG недоступен");
        230: let cloud = settings.cloud().expect("облачный режим");
        232: settings.mode.cloud_provider().expect("облачный режим"),
        241: tracing::warn!("у Anthropic нет embeddings API; для RAG задайте другой эмбеддер");
        440: tracing::warn!("облачные эмбеддинги не настроены (модель/ключ); RAG недоступен");
=====
    3  src/app/orchestrator/rag.rs
        255: tracing::warn!(file = %file.display(), error = %err, "RAG: не удалось проиндексировать файл");
        343: tracing::warn!(source = %info.source, "RAG rebuild: исходник недоступен, источник пропущен");
        448: tracing::warn!(source = %source, error = %err, "RAG rebuild: не удалось переиндексировать источник")